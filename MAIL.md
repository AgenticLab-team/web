# 邮箱：域名池、地址与收信

> **读者**：要做邮箱模块的人
> **性质**：⚠ **混合**。**P0 已经写完并跑通**（见下面那张表）；P1–P4 仍然是打算
> **最后核对**：2026-08-13，对着生产库和线上服务器逐条查过

## P0 做完了什么

| 东西 | 在哪 |
|---|---|
| 八张表 + 迁移 | `src/lib/db/schema/mail.ts` · `drizzle/0060_*` |
| 纯规则层（前缀、禁用词、punycode、验证码抽取） | `src/lib/mail/{address-rules,otp,domain-catalog,expiry-rules}.ts` |
| 100 个域名的归类与种子（幂等、只补不改） | `src/lib/mail/{domain-catalog,seed-domains}.ts` |
| 一次性箱（随机 / 自选 / 销毁 / 到期回收） | `src/lib/mail/burner.ts` |
| 收信入口（HMAC、去重、配额、留痕） | `src/app/api/mail/inbound/` · `src/lib/mail/ingest.ts` |
| 网关问路（快照 + 单地址现查） | `src/app/api/mail/routes/` · `src/lib/mail/routing.ts` |
| 开放 API（`mail:burner` scope + 五条接口） | `src/app/api/v1/mail/burners/**` |
| 取件码屏幕（模式①） | `src/components/mail/BurnerScreen.tsx` · `/mail/burner` |
| 后台域名表 + 到期告警 + 被拒日志 | `/admin/mail` · `src/lib/mail/{admin-queries,admin-actions,settle}.ts` |
| SMTP 网关（独立进程，同机/分机同一份代码） | `ops/mail-gateway/` |
| 网关↔站点的协议版本（跨机升级不会静默断） | `src/lib/mail/protocol.ts` |

`temp_mailbox` 这个功能开关**从 `planned` 转成 `wired`** 了 ——
它是 `flags/registry.ts` 顶上那段骂的「按下去什么都不会发生」的最后一个样本。

**还没做的**：DNS 体检任务（三个灯现在全是「还没体检过」）、
catch-all 的自动前缀发现、槽位与积分、靓号年租、长轮询 `wait`、附件落盘。

---

# 零、现在长什么样

先把能查证的事实摆出来，后面所有的设计都踩在这上面。

| 事实 | 值 | 从哪查到的 |
|---|---|---|
| 域名发放第一期 | 60 个名额，**30 个已登记**，7 个撤回，活动已 `completed` | `activities` / `activity_applications` |
| 登记的域名 | 全是 `.icu`，`aetherstudio.icu`…`ethanzhang.icu` | `normalized_key` |
| 这些域名的 DNS | **27 个在 DNSPod**（= 已经注册到我们名下）；**3 个不是**，见下 | `dig NS` |
| ⚠ 三个对不上的 | `selenium.icu`（NS 在 OVH）· `niuniu.icu`（NS 在万网，**RDAP 显示 2023-07-12 就注册了、到期 2033**）· `ethanzhang.icu`（**查无 NS，RDAP 404 —— 根本还没注册**） | `dig` + RDAP |
| **域名总盘** | 站长给的清单 **100 个**，全是 `.icu`，**100 个的 NS 全部在 DNSPod** —— 一个不漏，都已注册到名下。其中 27 个是上面那批有主的，**73 个未分配** | 逐个 `dig NS` |
| 里面有 **5 个中文域名** | SMTP 只认 punycode，要存 A 标签。最长的 `我真的特别…想你.icu` → `xn--6qqw1eaaaa206xh6b9z6dbabbb595logc.icu`（41 字符） | `idna` 编码 |
| 最长的域名 | `pneumonoultramicroscopicsilicovolcanoconiosis.icu`，49 字符 —— 仍远低于 254 的地址上限 | 数的 |
| 站内账号 | 141 个，全部 `member` | `users` |
| **填了邮箱的账号** | **0 个** | `users.email` |
| 积分 | 流通 1420 分，累计发行 1435 分，**最高的人累计 392** | `users` |
| 等级分布 | L1 133 人，L2 7 人，L4 1 人 | `users.level` |
| 商店 | 三件商品（补签卡 200 / 自定义称号 300 / 置顶 500），**全部 `enabled = 0`** | `shop_items` |
| 服务器 | 2 核 3.7G，根分区剩 46G | `df` / `free` |
| **出站 25 端口** | **被云厂商封了**（连 gmail 的 MX 直接超时） | 实测 |
| 入站 25 端口 | ufw 只放行 22 / 7000 / CF 网段的 80,443 | `ufw status` |
| 邮件相关配置 | 一个都没有（`.env.local` 里没有 SMTP、没有 R2、没有 UPLOAD_API_KEY） | 线上 env |

两条最要紧的：

1. **积分经济现在非常小。** 最有钱的人累计 392 分，30 个登记域名的主人里 20 个人分数低于 40，
   10 个人是 0。任何定价如果照着商店现有的 200–500 分抄，等于**这套东西发布当天就没人买得起** ——
   而商店那三件正好是全部关着的，所以它没被人发现。
2. **这台机器发不出邮件，也不该收邮件。** 出站 25 封死是云厂商的策略，改不了；
   入站 25 要放开的话，MX 记录会把源站 IP 公开出去 —— 而「躲在 Cloudflare 后面、
   没人知道源站在哪」是 `DEPLOY.md` 里明写的一条防线。见第五节。

---

# 一、一条原则：命名空间的稀缺性决定价格

三类邮箱的差别不是「功能多少」，是**前缀（@ 左边那半截）有没有人跟你抢**。
这一条能把定价、期限、门槛全部推导出来，不用一条条拍脑袋：

| | 域名归谁 | 前缀抢不抢 | 于是 |
|---|---|---|---|
| **自有域名** | 你 | 没人跟你抢 —— `admin@你的域名` 只有你想得到 | **免费、无限、不到期** |
| **临时池** | 公共 | 抢，但你只占 24 小时 / 30 天 | 免费，**但到期就还回去** |
| **靓号池** | 公共 | 抢，而且你想一直占着 | **要花分，而且是年租** |

反过来说：一个人在自己域名上开一百个别名，**不消耗任何别人也想要的东西**，
所以对他收费是没道理的；而 `hi@某个好域名` 全站只有一个，
不收费就会在开放的头五分钟被手快的人扫光，剩下的 130 个人永远没机会 ——
这正是限量活动最典型的死法。

---

# 二、四类域名

域名进池子时定好类别，这是后面一切的根。**一个域名只能属于一类**。

### `owned` —— 有主域名（现在的 30 个 + 以后每期活动发的）

发给谁就是谁的。主人拿到的是**整个域名的收件权**：

- **catch-all 默认开**：`任意前缀@他的域名` 全部落进他的收件箱。
  这就是「可以自己创建任意别名」的最诚实实现 —— **不用先登记再使用**，
  他在某个网站的注册框里当场编一个 `netflix-2026@自己的域名`，
  下一秒就能在站内收到。要求他先来网站点「新建别名」的设计，
  等于把最方便的那一点弄丢了。
- **具名别名**是可选的整理手段：给某个前缀起标签、单独静音、单独停用、单独看流量。
  它是给已经在用的前缀**贴标签**，不是使用的前提。
- catch-all 可以关。关掉之后只有具名别名收信 —— 挡字典式垃圾邮件用。

### `temp` —— 一次性池（名字不重要的那批）

**这个池子同时干两件事**（站长追加的一条，原设计里它只做一次性箱）：

1. 跑一次性箱 —— 随机或自选前缀，24 小时
2. **也能被正常申领** —— 临时箱、长期箱都能开在这批域名上

也就是说池子之间**不是按「能不能申领」分的**，是按**跑不跑一次性箱**分的：

| | 跑一次性箱 | 能申领 | 代价 | 声誉 |
|---|---|---|---|---|
| `temp` | **是** | 是 | 免费（占槽位） | **会被拉黑，要明说** |
| `reserved` | **否** | 是 | 年租 | 干净 |

这个划法比原来那个好，因为它把「靓号为什么值钱」讲清楚了：
**你花 400 分买的地址，不会因为别人在同一个域名上注册了一百个账号而被某个网站拒收。**
靓号池不跑一次性箱，这是它唯一真正卖的东西 —— 名字好听只是附带的。

反过来，`temp` 池的域名申领是免费的，代价是**界面上必须明写**
「这个域名同时在做一次性邮箱，部分网站可能拒收」。
不写的话，某天有人发现自己的常用邮箱注册不了东西，
而他根本不知道原因出在这里 —— 那是最难受的一种坑。

选进 `temp` 的标准是**这个域名要能被牺牲掉**：一次性邮箱迟早会被列进黑名单
（这是这类服务的宿命，不是能避免的事），被拖下水的应该是这十几个。

### `reserved` —— 靓号池（名字好的那批）

放临时箱和长期箱，**不跑一次性箱**。域名按名字好坏分 S / A / B 三档，
档位决定年租价（见第四节）。分档在后台手工标，不做算法 ——
「哪个域名算好」是审美判断，算出来的分数只会被人拿来抬杠。

### `blocked` —— 留着但**一个邮箱都不开**

⚠ 这一类是看完真实的 100 个域名之后加的，原设计里没有。

有些域名注册下来是对的（**防的就是别人注册它**），但把它放进任何一个池子
都等于免费送人一套工具。它们要在数据模型里是一等公民 ——
`kind = 'blocked'` 的域名连 MX 都不配，收信直接在网关拒掉。

留下来的只有两种情况（具体名单见附录 A）：

1. **商标近似**（10 个）。`githubusercontent.icu` 是最扎眼的一个：
   `raw.githubusercontent.com` 是每个开发者都眼熟的域名，
   而 `security@githubusercontent.icu` 发出来的信，收信人**几乎不可能分辨**。
   `huggingface` / `airtable` / `opencart` / `openreview` 同理。
   注意**发信默认关死并不能解决这件事** —— 光是能收信就够了：
   拿 `admin@某商标.icu` 去申请「域名所有权验证」「企业账号找回」的路是通的。
2. **真人姓名**（1 个，`余承东.icu`）。给陌生人开一个署着真人名字的邮箱，
   是替他把这个名字交出去了。姓名权写在民法典里（1012–1014 条），
   而承担后果的是域名注册人。

> **站长 8-13 决定**：`马嘉祺.icu` `华立.icu` `teensintimes.icu` `camhub.icu`
> 四个**不封禁，放进一次性池**。
>
> 我原来把它们和商标那一类摆在一起，那是过了 —— 放进**一次性池**
> 比放进靓号池风险小得多：地址 24 小时就没、**只收不发**、
> 前缀带最短长度限制，而且这个池子本来就配了禁用词表。
> 三条约束叠在一起，剩下的风险确实在可接受范围内。
>
> `babycam.icu` 你没提，我先留在封禁里 —— 要一起挪过去说一声就行。

留着不开邮箱，成本是每年几块钱续费，收益是那几件事都不会发生。

---

# 三、三类邮箱与槽位

## 3.1 三类

| | 域名 | 前缀 | 期限 | 占槽位 | 代价 |
|---|---|---|---|---|---|
| **别名** `alias` | 自己的 `owned` | 任意（catch-all）| 跟域名走 | 否 | 免费 |
| **一次性箱** `burner` | `temp` 池 | **随机 或 自选**（自选有最短长度）| 24 小时 | 否（另有并发上限）| 免费 |
| **临时箱** `temp` | `temp` / `reserved` 池 | 自选 | 30 天，到期回收 | 是 | 免费 / 年租 |
| **长期箱** `reserved` | `temp` / `reserved` 池 | 自选，全站唯一 | 一年，可续 | 是 | 同上 |

一次性箱不占槽位是故意的：**它必须是零摩擦的**。
「要收个验证码 → 先想想还剩几个槽位」的产品，用户第二次就不会来了。
它的约束是并发数（同时最多 3 个）和 24 小时的寿命，不是配额。

## 3.1.1 一次性箱的前缀：随机 或 自选

站长追加：**自选也要能用**（原设计只给随机）。随机是默认，自选是一个开关。

自选的那条有一个**最短长度**（默认 10 字符，进设置），理由不是防滥用，
是**保护同一个域名上的申领**：`temp` 池现在既跑一次性箱又能被申领，
两者共用一个命名空间。不设下限的话，有人可以用一次性箱
反复占着 `hi@rickroll.icu` —— 每次 24 小时，续着占，
而正经申领那条路上他一分钱都不用花。

划一条长度线之后，两条路各归各的：**短前缀只能靠申领拿到，
长前缀随便你在一次性箱上编。** 随机生成的那个不受这条限制
（它本来就是 12 位随机串）。

三条附带的规则：

- 自选前缀**不能撞上已被申领的地址**，也不能撞上正在生效的另一个一次性箱
- 反过来，**申领时不能抢一个正被一次性箱占着的前缀** —— 等它 24 小时到期
- 自选前缀要过禁用词表（见第十一节）；随机的不用过

## 3.1.2 申领 `temp` 池域名时必须说的一句话

界面上要明写：**「这个域名同时在做一次性邮箱，部分网站可能拒收」**。

这是把一个我们知道、用户不知道的事实交出去。不说的话，
某天他发现自己的常用邮箱注册不了东西，而**他根本无从知道原因在这**。

## 3.1.3 临时箱到期直接回收，不提供续费

收钱续期的话它就变成了长期箱，和靓号那一档打架，
而且会有人用一个不值钱的域名永久占着 `admin`。
想长期占，就去申领长期箱 —— 这两档之间的界线要清楚。

## 3.2 槽位

```
槽位 = min(等级, 5)  +  买来的（最多 3）        上限 8
```

`mail_slots` 一行一个槽位，**它是真值**；用户身上的计数是缓存列，后台能重算比对 ——
和活动名额、`points` 余额同一个办法（`SCHEMA.md` 零节）。名额算错在这种东西上是事故。

L5 之后不再涨，是防止早期用户把好前缀囤成资产。
这和 `ECONOMY.md` 里「新人永远追不上老人」是同一个病：
额度无上限的话，两年后 `mail@某好域名` 全在最早那批人手里，
而他们中的多数已经不来了。

## 3.3 等级解锁什么

要求是 `level-rules.ts` 顶上那句：**只列真的在管事的**，
不许编一张「L7 解锁 XX」的漂亮表格然后没有代码去读它。所以下面每一条都要有对应的判定点：

| 等级 | 解锁 |
|---|---|
| **L1** | 一次性箱（同时 3 个，随机或自选）· 1 个槽位 · 正文留 30 天 |
| **L2** | +1 槽位 · 可申领 **B 档**长期箱 · 一次性箱同时 5 个 |
| **L3** | +1 槽位 · 正文留 **60 天** · 可申领 **A 档** |
| **L4** | +1 槽位 · **附件可以落盘**（单封 ≤2M，个人 ≤50M）· 可申领 **S 档** |
| **L5** | +1 槽位（封顶）· **转发到自己的私人邮箱** |
| 域名主人 | 不看等级：自己域名 catch-all + 无限别名 |

两条设计上的取舍：

- **等级解锁「能力」，积分买「数量和好地址」。** 混在一起的话，
  两条曲线会互相抵消 —— 攒分的人自动升级，于是花分买的东西他本来也会有。
- 最贵的资源是**磁盘**，所以「正文留多久」「附件能不能落盘」放在等级上，
  而不是放在积分上。用积分买磁盘意味着有人可以一次性买爆磁盘。

---

# 四、和积分怎么挂

## 4.1 定价必须贴着现在的经济体量

`ECONOMY.md` 的口径：日发行上限 60 分／人，实际一天正常参与大约 **10–25 分**。
所以下面全部同时标成「≈ 几天」—— 这正是 `ROADMAP.md` 五节里那条还没做的事，
在这里先用上：

| 花什么 | 分 | ≈ | 性质 |
|---|---|---|---|
| 额外槽位（永久 +1，最多 3 次） | **60** | 约 3 天 | 一次性 |
| 长期箱 **B 档** 年租 | **60 / 年** | 约 3 天 | **周期性** |
| 长期箱 **A 档** 年租 | **150 / 年** | 约一周多 | **周期性** |
| 长期箱 **S 档** 年租 | **400 / 年** | 约三周 | **周期性** |

S 档定成「现在全站最有钱的人（392）也差一点」是**故意的**。
`ECONOMY.md` 第一条要防的就是「早期用户随手买空」——
一个开服当天就被扫光的靓号池，比没有靓号池糟。

年租是这套东西里唯一的**周期性回收口**，也是它存在的意义之一：
`ECONOMY.md` 说得很直白，一次性商品买完回收就归零，
必须有按期扣费的东西。称号已经是一个（`rentDays`），长期箱是第二个。

## 4.2 续期与回收，照抄称号那一套

这三条在称号上已经想清楚了，不要重新发明：

- **提前续费从原到期日顺延**，不是从今天 —— 否则是在惩罚提前付钱的人。
- **到期 ≠ 立刻没收**：到期后进 30 天**宽限期**，地址仍归你、但只收不给别人抢；
  30 天内补交年租原样恢复。宽限期过了才真正放回池子。
  邮箱和称号不一样的地方在这里：称号到期只是不能佩戴，
  而邮箱到期被别人抢走的话，**别人会开始收到本该给你的邮件** ——
  所以宽限期是必需的，不是体贴。
- 放回池子时，**原主有 7 天优先权**（原价可赎回），之后才真正公开。

## 4.3 不做的事

- **不做拍卖、不做竞价。** 名额抢起来的爽感值不了它带来的怨气，
  而且积分是能刷的 —— 竞价会立刻把「谁最会刷分」变成「谁拿到最好的域名」。
  先到先得 + 等级门槛已经够了。
- **不做转让、不做交易。** 一旦地址可以转让，它就是资产，
  接下来就会有人囤。转赠手续费那套是给积分设计的，不适用于唯一命名的东西。

---

# 五、邮件从哪进来 —— ✅ 已定：方案 A（自建网关）

> **2026-08-13 站长拍板：走方案 A。** B 留作以后要发信时的升级路径。
> 下面两个方案都保留，因为「为什么不是 B」以后一定会有人问。

这是整件事唯一的硬约束。

**已经确认的两件事**：出站 25 端口被云厂商封死（实测超时）；
入站 25 要在源站放开的话，MX 记录必然把源站 IP 公开 ——
而 `DEPLOY.md` 写着「这套防护的前提就是没人知道它」。
所以**在现在这台机器上直接收信这个选项是不存在的**，只剩两条路。

## 方案 A：自建 SMTP 网关（一个独立进程）

> **8-13 追加**：网关和站点**代码上完全拆开**（不连库、不 import、只走
> HTTPS + HMAC + 协议版本号），但**部署上可以先放在同一台机器**。
>
> | | 网关在哪 | `SITE_URL` | 源站 IP |
> |---|---|---|---|
> | ① 同机（现在） | 源站上 | `http://127.0.0.1:3000` | ⚠ **会被 MX 公开** |
> | ② 分机 | 独立机器 | `https://agenticlab.sh` | 藏得住 |
> | ③ 多机 | 好几台 | 同上 | 藏得住 |
>
> **从①到③不用改一行代码**，只改环境变量和 DNS —— 网关不存任何状态。
> ①那一格的代价是有意接受的，不是疏忽：见 `ops/mail-gateway/README.md`。


```
70 个域名  ──MX 10 mx.agenticlab.sh──▶  网关 VPS （另一个 IP，只跑 SMTP）
                                            │ RCPT 时查地址白名单，不认的**当场拒**
                                            │ HTTPS + HMAC
                                            ▼
                                   Cloudflare ──▶ agenticlab.sh /api/mail/inbound
```

- **你说的「批量改 MX」一次就够了** —— 所有域名指向同一个 `mx.agenticlab.sh`，
  以后加域名只加一条 MX，不碰网关。DNSPod 有批量接口，这是一个脚本的事。
- 源站 IP 不外泄（暴露的是网关的 IP，那台机器上什么都没有）。
- 代价：**多一台机器**（3–5 美元/月），以及**反垃圾要自己扛**（rspamd 之类）。
- 关键实现点：**不认识的地址要在 RCPT 阶段就拒**，不能收下来再退信 ——
  先收后退会让我们变成退信轰炸的帮凶，也是被列黑最快的路。

## 方案 B：Cloudflare Email Routing

```
每个域名托管到 CF ──▶ Email Routing catch-all ──▶ Email Worker ──▶ agenticlab.sh
```

- 零运维、免费、自带反垃圾和 TLS，源站永不暴露；
  以后要发信时 Email Sending 会**自动给每个域名配好 SPF/DKIM** ——
  70 个新域名各自建立发信信誉这件事，自己做是很难做对的。
- 代价：**每个域名都要托管到 Cloudflare**（NS 从 DNSPod 改到 CF），
  ~70 次注册商侧操作 + 建 zone + 开 Routing + 建规则。后三步能脚本化，
  改 NS 那一步在 DNSPod 控制台可以批量做，但**它是真的要做的一步**。
- 另外要新增一个 Worker，而这个项目现在完全不用 Workers。

## 为什么选了 A

理由是你自己给的那句「我可以将所有的域名的 MX 记录批量指向这里」——
A 正好只需要这个，而且新域名进来的边际成本是**一条 MX 记录**；
B 的边际成本是**一次 NS 迁移**。域名会越来越多，这个差别会一直在。

B 也完全站得住，只是前期一次性重一些。而且要注意：
**要发信的那天，无论走哪条路都得回到 B（或等价的发信 API）** —— 见第六节。
所以 A 不是「不用 Cloudflare」，是「现在还不需要 Cloudflare」。

## 无论哪条路，每个域名都要配的记录

| 记录 | 值 | 为什么 |
|---|---|---|
| `MX` | `10 mx.agenticlab.sh`（A 方案）| 收信 |
| `TXT @` | `v=spf1 -all` | **这些域名不发信，就要明说**。不写的话，任何人都可以拿你的域名伪造发件人，而这会连累整批域名的声誉 |
| `TXT _dmarc` | `v=DMARC1; p=reject; rua=mailto:dmarc@agenticlab.sh` | 同上，并且能收到伪造报告 |
| `mx.agenticlab.sh` 的 `A` | 网关 IP，**必须是灰云 DNS only** | 橙云会把它代理掉，而 CF 不代理 SMTP —— 邮件会直接收不到 |

`-all` 而不是 `~all`：这些域名**确实**一封信都不发，没有留软化余地的理由。
将来某个域名要发信了，再单独把它改掉。

---

# 六、发信：默认关死

**只收不发。** 这不是保守，是这套东西能不能活下去的前提：

- 一旦能发，就要面对 IP 信誉、SPF/DKIM/DMARC、退信处理、被列黑名单的一整套麻烦；
- 而这些域名**共用一套信誉**。一个人拿临时箱群发一次，
  代价是**另外 69 个域名一起进黑名单** —— 包括别人的自有域名。
  这是唯一一个「一个人犯错、所有人受害」的功能，所以它的默认值只能是关。
- 而且这台机器物理上也发不出去（出站 25 封死），
  真要发只能走 Cloudflare Email Sending / Resend 这类 HTTPS 接口。

会开的只有一个口子，而且要等到很后面：**对已经收到的邮件回信**
（有 `In-Reply-To`、有原始 `Message-ID`、限速、留痕）。
群发、主动发新邮件，一律不做。

顺带：**站内自己的验证邮件不要发到站内邮箱**，也不许拿站内邮箱做账号找回 ——
那是一个能把自己锁在门外的循环。

---

# 七、数据模型

沿用全局约定（ULID 主键、毫秒时间戳、软删、`status` 显式枚举、外部触发带幂等键）。

```
mail_domains      domain PK, kind(owned|temp|reserved|blocked), tier(s|a|b) NULL,
                  punycode,                      -- IDN 的 A 标签，SMTP 只认这个
                  allow_burner INTEGER,          -- 跑不跑一次性箱（reserved 恒为 0）
                  allow_claim INTEGER,           -- 能不能被申领
                  allow_custom_local INTEGER,    -- 一次性箱能不能自选前缀
                  in_random_rotation INTEGER,    -- 随机分配时会不会选到它（IDN 默认 0）
                  owner_user_id NULL,            -- kind=owned 时必填
                  catch_all INTEGER, catch_all_box_id NULL,
                  registrar, registered_at, domain_expires_at,   -- 域名本身的到期日
                  mx_ok, spf_ok, dmarc_ok, dns_checked_at,
                  status(pending|active|paused|retired), enabled,
                  source_application_id NULL,    -- 从哪个活动申请来的
                  note, created_by, created_at/updated_at

mail_boxes        id, user_id, local_part, domain, address,
                  kind(alias|burner|temp|reserved),
                  label, muted INTEGER,
                  expires_at, grace_until, renewed_at, renew_count,
                  slot_id NULL,                  -- burner/alias 为空
                  order_id NULL,                 -- 年租那一笔
                  quota_bytes, used_bytes, message_count, last_received_at,
                  status(active|full|expiring|grace|expired|disabled|revoked),
                  created_at/updated_at
    UNIQUE(address) WHERE status NOT IN ('expired','revoked')
    -- 部分唯一索引，不靠应用层查重（和活动名额同一个办法）

mail_slots        id, user_id, source(level|purchase|grant),
                  order_id NULL, ledger_id NULL, granted_by, revoked_at,
                  created_at
    -- 槽位的真值。用户身上的计数是缓存列，后台可重算比对

mail_messages     id, box_id, rfc_message_id, in_reply_to,
                  from_addr, from_name, to_addr, subject,
                  body_text,                     -- 纯文本进库，供搜索
                  body_html_path,                -- HTML 落文件，便于批量清理
                  size, has_attachments, attachment_meta TEXT,
                  spam_score, spf_pass, dkim_pass, dmarc_pass,
                  otp_code,                      -- 抽出来的验证码，见第八节
                  received_at, read_at, expires_at, purged_at
    UNIQUE(box_id, rfc_message_id)               -- 网关重投不会存两份

mail_attachments  id, message_id, filename, mime, size,
                  stored INTEGER, path NULL, expires_at

mail_ingress_log  id, envelope_from, envelope_to, matched_box_id NULL,
                  verdict(accepted|rejected|dropped|quarantined),
                  reason, size, source_ip, created_at
    -- ★ 被拒的也要留痕。没有这张表，「我朋友说发了我没收到」这个问题永远查不了，
    --   而它一定会被问到

mail_blocks       id, scope(global|domain|box), target, kind(sender|sender_domain),
                  reason, created_by, created_at

mail_events       id, box_id NULL, domain NULL,
                  event(created|renewed|expired|reclaimed|disabled|quota_full|...),
                  actor_id, actor_kind(user|admin|system), detail, created_at
```

几处是照着这个仓库已经踩过的坑写的：

- **`mail_ingress_log` 记被拒的信**。收信这件事最常见的支持请求就是「怎么没收到」，
  没有这张表只能猜。
- **`UNIQUE(address)` 是部分唯一索引**，已回收的地址不占用唯一性 —— 否则地址永远不能重发。
- **`mail_slots` 是行不是计数**，`quota_used` 那个教训的直接套用。
- **HTML 正文落文件、纯文本进库**：裁剪时删文件很快，删完库还小；
  而搜索只需要纯文本。

---

# 八、一次性箱的 API

> 站长追加：**最底层那批（随用随扔的一次性箱）要能用 API 调。**

不新造一套认证。`api_tokens` 那一套已经有：`al_` 前缀（扫描器认得出）、
只存 SHA-256、按 scope 授权、按令牌限流、每次调用留痕。全部复用。

## 8.1 两个 scope，必须分开

`api-tokens/rules.ts` 给 `groups:send` 定 danger 2 的理由是
「泄漏的后果不由泄漏者承担，由这个社区承担」。用同一把尺子量邮箱，
量出来的结果是**它必须拆成两个 scope**：

| scope | danger | 能做什么 |
|---|---|---|
| `mail:burner` | 1 | 开一次性箱、读**这把令牌自己开的**箱子收到的信、提前销毁 |
| `mail:read` | 2 | 读你所有的箱子 —— 临时箱、长期箱、自有域名。**默认不给** |

为什么不能合成一个：一次性箱里装的是「我刚才在某个网站注册」这一件事，寿命 24 小时；
而自有域名的收件箱里装的是这个人**真正在用的邮箱**。
一把泄漏的令牌读得到后者，等于把他在所有网站上的找回密码通道一起交出去 ——
这比 `groups:send` 还重，只是受害的是他本人。

而 API 的真实用途（注册脚本、测试、CI 里收个验证码）**只需要前者**。
合成一个 scope 的话，等于逼每个想自动化的人交出全部邮件，
于是安全的默认值变成「谁都别用」，而这不是安全，是把功能关掉。

`mail:burner` 还要再收窄一层：**它只能读这把令牌自己开的箱子**。
你在网页上开的一次性箱，令牌看不见。这样一把令牌泄漏的爆炸半径
就是它自己造出来的那几个地址。

## 8.2 接口

```
POST   /api/v1/mail/burners                 开一个，可选 {domain, ttl}
                                             → {id, address, expiresAt}
GET    /api/v1/mail/burners                 列出这把令牌开的、还活着的
DELETE /api/v1/mail/burners/{id}            提前销毁（「用完就扔」的那个扔）
GET    /api/v1/mail/burners/{id}/messages   收到的信，?since= 增量拉
GET    /api/v1/mail/messages/{id}           单封正文
GET    /api/v1/mail/burners/{id}/wait?timeout=60
                                             ★ 长轮询：等下一封，最长 60 秒
```

`wait` 是这套 API 好不好用的分水岭。没有它，每个人都会写
`while true: sleep 2; get messages` —— 收一个验证码打三十次接口，
而这三十次全落在我们的限流上，最后是双输。给一条长轮询之后，
正常用法就是一次请求，我们省资源，对方的脚本也短得多。

返回体里直接带 `otpCode`（第九节抽出来的那个）。
调用方九成只要那六位数字，让他们自己写正则去解 HTML 邮件是浪费。

## 8.3 限流：卡开箱，不卡读信

| | 上限 |
|---|---|
| 开箱 | 5 个/小时、20 个/天（每令牌） |
| 同时在手 | 3 个 —— **和网页共用同一个额度** |
| 读信 / 列表 | 120 次/分钟 |
| `wait` | 每个箱子同时只挂 1 条 |

分开的理由：**开箱消耗的是全站共有的东西**（池域名的命名空间，以及那个域名的声誉），
读信只消耗我们自己的 CPU。用同一个数字管两者，结果要么是脚本根本没法用，
要么是有人一晚上开出一万个地址。

「同时在手 3 个」必须和网页共用，否则「网页 3 个 + 每把令牌 3 个 + 令牌可以建很多把」
等于没有上限 —— 这是限流最常见的漏法。

开箱和收信都写 `mail_events`，**令牌 id 记进去**。
理由和 `api_sends` 一模一样：「这个地址是谁开的」在出事那天是唯一要问的问题。

## 8.4 API 开的箱子有什么不一样

- **不占槽位**（和网页上的一次性箱一致）
- **默认不发站内通知** —— 脚本收信不需要有人被叮一下
- **到期直接销毁，没有宽限期**。一次性就是一次性

---

# 九、体验上最要紧的一件事：验证码

临时邮箱九成的用途是**收一个验证码**。所以收件箱列表那一行上，
直接把验证码抽出来、放一个「复制」按钮，点开邮件都不用点。

抽取规则用保守正则（`\b[0-9]{4,8}\b` / `\b[A-Z0-9]{6}\b`，配合主题里的
verification / code / 验证码 之类的词），**抽不出来就什么都不显示** ——
抽错一个数字比不抽更糟。抽到的存进 `mail_messages.otp_code`，
列表页不用二次解析正文。

这一条的成本大概是半天，而它是这个功能好不好用的分水岭。

---

# 十、反滥用与磁盘

反滥用（`MODULES.md` 5.3 已有的，加两条）：

- 只有已绑定的 `member` 能用（这个站本来也只有他们）
- 发信默认关死（第六节）
- 每人箱子数受槽位管；一次性箱同时 ≤3
- **单个箱子收信频率上限**（比如 100 封/小时），超了自动暂停并通知 ——
  临时箱最常见的滥用是被拿去做转发靶子
- **单个域名收信频率上限**，尤其是开了 catch-all 的自有域名 ——
  字典式扫描能一晚上塞进几万封
- **禁用词表**（站长维护，见 11.3）挡自选前缀 —— 随机生成的不用过

磁盘（46G 可用，`MODULES.md` 5.2 的铁律照抄，按等级放宽）：

| | 值 |
|---|---|
| 正文保留 | 30 天（L3+ 60 天） |
| 附件 | **默认不落盘**，只存文件名/大小/类型；L4+ 才落盘 |
| 落盘时 | 单封 ≤2M，个人 ≤50M，全局池 ≤1G，LRU 淘汰 |
| 单个箱子 | `used_bytes` 配额，超限**拒收并通知**，不静默丢 |

清理挂进 `agenticlab-health` 那一轮（每 5 分钟，步骤之间互相隔开）：
到期箱回收、宽限期结算、正文过期清理、附件 LRU。

---

# 十一、后台：站长能做什么

> 站长要求：**看到所有域名 / 到期时间 / 绑新域名 / 指定归属；
> 任意设置邮箱和后缀、任意认领时间；查询和收回别人的申领；设禁用词。**

必备三件套照旧，一条都不能省（`ARCHITECTURE.md` 四节）：
`requireWritableAdmin("mail.xxx")` + `audit(...)` + 权限点在 `rbac/permissions.ts`
注册并按危险等级分级。取当前用户一律 `getRealUser()` —— 预览态下
`getCurrentUser()` 返回的是被预览的那个人，这个坑这个项目已经踩过三次。

## 11.1 权限点

| 权限点 | 危险 | 管什么 |
|---|---|---|
| `mail.domain.read` | 0 | 看域名池：归属、到期、用量、DNS 体检 |
| `mail.domain.write` | 1 | 加域名、改类别 / 档位、**指定归属**、开停 |
| `mail.box.read` | 1 | 查任何人的地址（**元数据**） |
| `mail.box.write` | 2 | 替人开箱、改到期、**收回别人的申领** |
| `mail.banword` | 1 | 禁用词表 |
| `mail.content.read` | **3** | ★ 看别人邮件的主题与正文 —— 单独一级，见 11.4 |

## 11.2 `/admin/mail`

**① 域名表**（一行一个域名，这是「整套系统可复用」的落点）

| 列 | 说明 |
|---|---|
| 域名 · punycode | IDN 两个都显示 |
| 类别 / 档位 | `owned` / `temp` / `reserved` / `blocked`，可直接改 |
| 归属 | 空 = 公共池；可**指定给某个人**，也可以收回变公共 |
| **域名到期日** | 见下，这一列有告警 |
| MX / SPF / DMARC | 三个红绿灯，定时体检 |
| 箱子数 · 近 7 天收信 | 判断这个域名活不活 |

- **绑新域名**：单个加，或粘一大段（一行一个，可带类别）批量导入。
  这一步要能反复跑 —— 域名会一直加，不能是一次性脚本。
- **导出**：`域名 → 归属 → 到期 → 类别`。`DONE.md` 里那条教训直接套用 ——
  原来的导出只有裸域名，答不上「这个域名是谁的」。
- ⚠ **域名到期日必须有告警**。域名过期 = 挂在上面的**所有邮箱同时消失**，
  比磁盘满严重得多，而且**没有任何征兆** —— 邮件只是不再来了。
  挂进 `agenticlab-health` 那一轮，提前 30 / 14 / 7 天各报一次。
  100 个域名分散在不同的注册时间上，靠人记是记不住的。

**② 地址表**：地址 · 主人 · 类别 · 到期 · 用量 · 收信量 · 状态。
可按人 / 按域名 / 按状态筛。每一行能做三件事：**改到期时间**（任意，含永不过期）、
**转移归属**、**收回**。收回要填理由，理由进通知 ——
「我的邮箱怎么没了」必须有一句能给出去的答复。

**③ 禁用词表**（11.3）　**④ 被拒日志**（`mail_ingress_log`，收不到信时唯一查得动的地方）

## 11.3 禁用词

沿用 `sensitive_words` 那张表的四种匹配（exact / prefix / contains / regex），
但**是另一张表**：邮箱前缀和帖子正文要挡的东西完全不同 ——
这边挡的是 `admin` `postmaster` `noreply` `security` `billing` 这类
**会让收信人误判身份**的词，以及品牌名。

两条必须写死、不做成配置的：

- `postmaster` 和 `abuse` **永远保留给系统**。RFC 5321 要求域名能收这两个地址，
  而它们是收「你们家域名在发垃圾邮件」这种投诉的唯一通道。
  发给用户的话，我们会在完全不知情的情况下被投诉、被拉黑。
- 禁用词**只挡自选**，不挡随机生成的一次性前缀（那是 12 位随机串，撞不上）。

站长自己不受禁用词管（11.4）。

## 11.4 「任意」到什么程度，以及一条我建议留着的线

站长可以：**任意前缀 + 任意后缀**（含 `blocked` 类域名）、绕过最短长度、
绕过禁用词、绕过池归属、**任意到期时间**（含永不过期）、
查询和收回任何人的申领。

这些走同一个入口，但**每一次都写 `audit_logs` + `mail_events`，actor 记站长本人**。
`ARCHITECTURE.md` 说管理端不做限流、不做双人复核（「我有数」），
留痕这一条没有例外。

### ⚠ 一条我自己划的线：元数据和正文分开

你没要求读别人的邮件正文，而我把它单独拎成 `mail.content.read`（危险 3），
默认**不给**，理由值得写下来：

这个站的邮箱会被拿去收**验证码和找回密码的链接**。一个能静默读任意用户
邮件正文的后台，等于一把能登录他们所有第三方账号的万能钥匙。
风险不在于你会用它 —— 在于**你的账号被盗时它一起被盗**，
而那一刻没有任何东西会发出声音。

所以：

| 默认看得到 | 默认看不到 |
|---|---|
| 地址、主人、到期、收了多少封、发件人地址、大小、时间 | **主题**、正文、附件、抽出来的验证码 |

主题算正文那一边 —— 一半的验证码邮件直接把码写在主题里。

要看正文时走一个显式动作：**填理由 → 写 audit → 同时给当事人发一条站内通知**。
通知那一条是关键，它是唯一一个在你自己都不知道的时候还能发声的机制。

嫌重的话可以只留 audit、去掉通知，改一行的事。但我建议留着。

---

# 十二、界面：像个邮件客户端，三种模式各有各的重点

> 站长要求：**符合 Apple HIG 和现有页面风格，同时要像一个邮件客户端；
> 不同的模式有不同的重点。**

## 12.1 先说不变的部分

`globals.css` 顶上写的取向就是「Apple HIG 的克制 + 材质感」，
而 `primitives.tsx` 顶上写着这个站的割裂感从哪来：
**同一个构件被各页手写了一遍**（卡片 36 次、Pill 横滚条 11 次、返回链接 12 次）。

邮箱是全站第一个「三栏」页面，最容易变成又一次手写大会。所以：

- 卡片 / 分组 / 空态 / 按钮 / 搜索框**一律用 `primitives.tsx`**，一个都不新写
- 圆角只用 `--radius-chip/control/card/sheet`，动效只用三档时长 + `--ease-spring`
- 字号只走 iOS 那把阶梯（34/28/22/20/17/16/15/13/12/11）
- **不新增一个数字**。真的需要新尺度，就往 `@theme` 里加一档并写清楚为什么 ——
  那正是 `--radius-chip` 当初被加进去的过程

## 12.2 骨架

```
┌──────────┬────────────────┬──────────────────────┐
│ 地址栏    │  邮件列表       │  正文                 │
│          │                │                      │
│ 未读数    │  发件人         │  主题                 │
│ 到期倒计时 │  主题 · 摘要    │  正文（sandbox iframe）│
│          │  验证码 chip    │                      │
└──────────┴────────────────┴──────────────────────┘

  ≥1280px   三栏
  ≥768px    两栏（地址栏收成 rail，用 --sidebar-width-rail）
  手机       一栏，逐层进入
```

手机上**逐层进入**而不是把三栏压扁 —— 这是 Mail.app 的做法，
也是这个站已有的模式。回退靠 `BackLink` / `FloatingBack`：
装成 App（`display: standalone`）之后浏览器那条 chrome 整个没了，
**连返回按钮一起没了**，站内这个链接是唯一的出口。

## 12.3 ① 一次性箱 —— 重点是「等一封信」，不是「管理邮件」

它不该长得像邮件客户端，该长得像一个**取件码屏幕**：

```
┌────────────────────────────────────────┐
│  k7m2xq8p4v@rickroll.icu        [复制] │
│  ────────────────────────────────────  │
│  23:41:07 后销毁                        │
│                                        │
│            ◍  正在等待邮件…             │
│                                        │
│  ┌──────────────────────────────────┐  │
│  │  GitHub                          │  │
│  │      8 2 4 1 9 3        [复制]    │  │  ← 34px 那一档
│  │  12 秒前                          │  │
│  └──────────────────────────────────┘  │
└────────────────────────────────────────┘
```

- **地址本身和验证码本身是页面上最大的两个东西**，各带一个复制按钮
- 长轮询（第八节的 `wait`）直接驱动界面：**不要轮询，不要「刷新」按钮**。
  用户在这一页上唯一想做的事就是等，那就让他等到
- 倒计时用 `tabular-nums`，否则数字跳动时整行会左右抖
- **收到之后不跳转**。他多半还要等第二封（改密码、二次验证）

## 12.4 ② 自有域名 —— 重点是「这个域名在替我做什么」

像个控制台多过像收件箱：

- 顶部一行 DNS 体检：MX / SPF / DMARC 三个灯。红的时候直接给出
  **能粘贴的记录值**，不是一句「请配置 MX 记录」。
  域名多了以后「收不到信」十次有九次是某条记录没配好，
  而这是**唯一**能让用户自己看出来的地方
- catch-all 开关摆在最显眼处，旁边写「现在有 N 个前缀在收信」
- ★ **自动发现的前缀列表**：catch-all 收到过的前缀自动列出来，
  每个后面跟着「收了多少封 / 最近一次 / 起个名 / 静音 / 停用」。

  这是 catch-all 真正的杀手锏 —— 用户不用登记就能用，
  而系统事后告诉他**「你在这些地方用过这个域名」**。
  那张表本身就是一份「我在哪些网站注册过」的清单，
  别的邮箱产品给不了这个东西。

## 12.5 ③ 长期箱 / 临时箱 —— 重点是「这是我的正经邮箱」

这里才是标准三栏邮件客户端：会话列表、已读未读、搜索、按发件人筛。

- 未读用左侧一个 6px 圆点，**不是加粗整行** —— 加粗会让列表在两种状态间跳动
- 列表行固定三行高（发件人 / 主题 / 摘要），不做自适应高度：
  高度不齐的列表滚起来眼睛没有着力点
- 正文里的 HTML **在 sandbox iframe 里渲染**，**默认不加载远程图片**（追踪像素），
  给一个「显示图片」的按钮。这条是隐私也是安全，没有商量余地
- 搜索走现成的 FTS5

## 12.6 共通

- 每个箱子头上一条到期倒计时。**长期箱进宽限期时整条变琥珀色** ——
  这是全模块唯一会用到警示色的地方，用多了就没人看了
- 空态用 `Empty` / `EmptyAction`
- 附件默认不落盘（第十节），所以附件区显示的是
  **「文件名 · 大小 · 未保存」**，不是一个点了没反应的下载按钮 ——
  那是最糟的一种谎

## 12.7 路径

| 路径 | 内容 |
|---|---|
| `/mail` | 收件箱（模式 ③） |
| `/mail/burner` | 一次性箱（模式 ①） |
| `/mail/new` | 开新箱：一次性 / 临时 / 长期，带价格与「这个域名也在做一次性邮箱」的提示 |
| `/mail/boxes` | 我的地址：期限、用量、静音、续期、停用 |
| `/mail/domains` | 我的域名（模式 ②） |
| `/admin/mail` | 第十一节那四张表 |

---

# 十三、分几步做

| | 做什么 | 完成时能干嘛 |
|---|---|---|
| **P0** ✅ | 域名池表（100 个导进去）+ `/admin/mail` 域名表 + DNS 体检 + 到期告警；网关打通；`/api/mail/inbound`；一次性箱（随机 + 自选 + 禁用词）；`/mail/burner`（模式①）；验证码抽取；**`mail:burner` scope + `/api/v1/mail/burners`**（长轮询 `wait` 放 P1） | 全站 141 个人当天就能收验证码，脚本也能调；你手上 100 个域名第一次有一张能看的表 |
| **P1** | 自有域名 catch-all + 自动发现前缀 + `/mail/domains`（模式②）；`wait` 长轮询；后台地址表（查询 / 收回 / 改到期 / 转归属） | 30 个域名主人拿到「域名到底有什么用」的第一个答案 |
| **P2** | 槽位（等级 + 积分买）、临时箱、到期回收、`/mail` 三栏客户端（模式③） | 积分第一次有了**天天都会用到**的去处 |
| **P3** | 靓号池、分档定价、年租与宽限期、续期结算挂进 health | 周期性回收口开始工作 |
| **P4** | 转发到私人邮箱（L5）、附件落盘（L4）、对来信回复、`mail.content.read` 那条带通知的通道 | —— |

后台在 P0 就要有，不能拖到最后：**100 个域名没有一张表就是管不了的**，
而域名到期告警这一条比功能本身还急 —— 域名过期是无声的，
等到有人说「我收不到信了」的时候已经晚了。

P0 之后就该开一个功能开关 `temp_mailbox` 从 `planned` 改成 `wired`，
并且把它真的接上（现在全站零调用点，是 `flags/registry.ts` 顶上骂的那个病）。

---

# 十四、还需要你定的

> **已定**（8-13）：
> - 第五节走**方案 A**（自建网关）
> - 第四节那组价格照写的来（60 / 60 / 150 / 400）
> - 域名清单 **100 个**，全部核过，归类见附录 A
> - `马嘉祺` `华立` `teensintimes` `camhub` 放**一次性池**，不封禁
> - 一次性箱前缀**随机 + 自选都要**，自选有最短长度
> - 一次性池的域名**同时也能被申领**
> - `muran.icu` `jiangmuran.icu` **留给站长**，不进任何池
> - 后台要能管全部域名 / 到期 / 归属 / 收回 / 禁用词，站长权限最高（第十一节）
> - 界面要像邮件客户端，三种模式各有重点（第十二节）

1. **30 个登记里有 3 个没落地**，这三位现在拿的是空头支票，而系统里他们的申请
   还是 `submitted`、活动已经 `completed`：
   - `niuniu.icu` —— RDAP 显示 **2023-07-12 就被别人注册了，到期 2033**，抢不回来
   - `selenium.icu` —— NS 在 OVH，也是别人的（这名字太有名了）
   - `ethanzhang.icu` —— **RDAP 404，还没人注册**，现在去注册就能拿到

   前两个要走 `failed` 回填让人改名重提（框架里有这条路），第三个补注册就行。
   为什么当时没拦住：`actions.ts:236` 那段注释自己写着「这一步之前**从来没有被调用过**」——
   可用性检查是活动跑完之后才接上的。也就是说这三个是**已知缺口的遗留**，不是新 bug，
   现在再报名同样的名字会被当场拦下。
2. **网关那台机器放哪家、什么规格**？只跑 SMTP 的话 1 核 512M 就够，
   要求只有两条：**入站 25 不被封**（大多数厂商不封入站，封的是出站），
   以及**不要和源站同一个 IP 段** —— 同段等于把源站的位置也说出去了一半。
3. **`mx.agenticlab.sh` 这个名字可以吗**？它会出现在所有域名的 MX 记录里，
   也就是说**这批域名之间的关联关系是公开的** —— 谁都能看出这 100 个域名是一家的。
   介意的话可以用一个中性的域名当 MX 主机名（比如从一次性池里挑一个）。
4. **附录 A 里那 22 个「看起来是给具体人的」到底给谁**？我只能从名字形状猜，
   猜错了就是把某人的域名放进公共池让别人抢走 —— 这一类必须你点头才动。
   代码里它们先按 `blocked` 占位（不导入的话它们在系统里不存在，
   于是没有任何东西拦得住以后有人把它们当公共域名加回来）。
5. **每个域名的到期日要补进去**。现在 100 个全是空的 ——
   而空的那些**不会触发任何告警**，也就是说这一批比快到期的还危险：
   会在完全没有预警的情况下过期，然后挂在上面的邮箱一起消失。
   后台那一页第二条横幅就是在说这个。

---

# 附录 A：100 个域名怎么分

> NS 逐个查过，**100 个全部在 DNSPod，都已注册到名下**。
> 下面的归类是建议，**除了「有主」那 27 个是库里查出来的事实，其余都要你过一遍**。

> **8-13 站长定的规矩**：`A` 里那批「看起来是给某人的」——
> **能匹配上就归那个用户，匹配不上就扔公共池**。到期日统一 **2027-08-08**。
>
> 拿生产库跑过一遍，实际结果：
>
> | | 个数 | |
> |---|---|---|
> | `owned` | **39** | 28 活动认领 + **9 认出来的** + 2 站长自己的 |
> | `reserved` | **35** | 22 靓号 + 13 判不出来的（含 `cliproxyapi`）|
> | `temp` | **15** | 14 + `babycam` |
> | `blocked` | **11** | 10 商标 + `余承东` |
>
> **认出来的 9 个**分两类，理由都写进了 `note` 和审计：
>
> | 域名 | 归给 | 凭什么 |
> |---|---|---|
> | `ashipowner` | ShipOwner | 算的：`shipowner` 的变体 |
> | `carleight` / `carleightwu` | Carleight Wu | 算的：昵称 |
> | `tripfzjmr` | 嗷呜嗷呜小狗 | 算的：`tripfz-jmr` 去连字符 |
> | `yintins` | 婴源 | 算的：`yintins-01` 去后缀 |
> | `lay621` | Lay | **站长确认**：和 `layopc` 同一人 |
> | `cjl2726` | Fly By Midnight | **站长确认**：和 `cjlworks` 同一人 |
> | `10kjs` | 10科技说 | **站长确认**：和 `tech10` 同一人 |
> | `techerng` | 你懂得 | **站长确认**：和 `techcheng` 同一人 |
>
> 后四个**匹配器有道理地挡下来了**（核心串只有 3 个字符 / 改的是中间的字母），
> 所以规则**一个字没放松** —— 放松到能捞起这四个，就会顺带把别人的域名判错。
> 它们走 `CONFIRMED_PAIRS` 这张人工确认表，而且写的是
> **域名 → 域名**不是域名 → 人名：人仍然从库里查（谁拥有配对的那个），
> 所以他改昵称、换账号都不影响，「凭什么是他的」照样答得上。
> 一条测试盯着这张表里**不许出现匹配器本来就能算出来的**条目 ——
> 否则它会变成绕过规则的后门。
>
> **仍判不出来的 13 个**留在靓号池：`borancui` `ryanzhu` `sunyuchen` `daiyu1`
> `z091127` `tatumisin` `cacinie` `msadream` `qific` `awmcap` `m78ai`
> `unknownuserfrommars` `cliproxyapi`。
>
> 落在**靓号池而不是一次性池**：靓号要花积分、由认得出是谁的成员申领、全程留痕；
> 一次性池是匿名的 24 小时地址。这批名字里不少像真人姓名，两者差别在那种情况下很要紧。

下面这张表是**原始归类**（人看的六格），代码里的四类见上。

| 类别 | 个数 | 处置 |
|---|---|---|
| ① 已有主 `owned` | **28** | owner 从 `activity_applications` 带过来 |
| ② 看起来是给某人的 | **22** | 种子跑匹配 + 人工确认表：中了转 `owned`，没中留 `reserved` |
| ③ 封禁 `blocked` | **11** | 留着续费，一个邮箱都不开，连 MX 都不配 |
| ④ 靓号池 `reserved` | **22** | S 5 / A 9 / B 8。**不跑一次性箱** |
| ⑤ 一次性池 `temp` | **15** | 跑一次性箱，**同时也能申领**（含 `babycam`）|
| ⑥ 站长自己的 `owned` | **2** | `muran.icu` `jiangmuran.icu` —— **直接指给站长本人**，走和别人一样的 `owned` 路径 |

（28 + 22 + 11 + 22 + 15 + 2 = **100**，逐个核过，无重复、无遗漏。）

> **代码里只有四类**（`mail_domains.kind`），上面这六格是给人看的分法。
> 对应关系写在 `src/lib/mail/domain-catalog.ts`，一条测试盯着数量：
>
> | 这里的格子 | `kind` | 为什么 |
> |---|---|---|
> | ① + ⑥ | `owned` | 站长自己的两个走**完全一样**的路径，不单独造类别 |
> | ② 待确认 | `blocked` | **先占位**。不导入的话它在系统里不存在，于是没有任何东西拦得住以后有人把它当公共域名加回来 |
> | ③ 封禁 | `blocked` | |
> | ④ / ⑤ | `reserved` / `temp` | |
>
> 所以库里是 `owned` 30 · `blocked` 34 · `reserved` 22 · `temp` 14。
> ②那 22 个定了归属之后，一条一条改成 `owned`。

> 8-13 改动：`马嘉祺` `华立` `teensintimes` `camhub` 四个从③挪到⑤（站长决定）。
> `babycam` 留在③等你一句话。
>
> 8-13 再改：`niuniu869.icu` **确定给 `niuniu.icu` 那位**（②里划走 1 个 → ①变 28）；
> `muran` / `jiangmuran` **不做单独类别，直接指给站长的账号** ——
> 这样 `owned` 那条路径站长自己天天在走，坏了当天就知道，
> 比任何测试都管用。

## ① 已有主（27）

`aetherstudio` `agenthing` `ai-talkshow` `cjlworks` `codetics` `corvusamuse`
`gptagent` `ificanfly` `iseeyouin` `jiubeixin` `jokerai` `kizunerwe` `layopc`
`lopleec` `lts4ai` `md5523` `meinianda` `neurolancer` `shipowner` `tech10`
`techcheng` `tianzzi` `tripfz-jmr` `vistago` `yigeren` `yintins-01` `zennon`

## ② 待确认归属（23）—— ⚠ 这一批不要动

名字的形状像是给具体的人准备的，我只能猜，不能替你分。分成两组：

**明显是某个已有主的第二个域名**（建议直接归同一个人）：

| 域名 | 看起来是 |
|---|---|
| `ashipowner.icu` | `shipowner.icu` 的变体 |
| `tripfzjmr.icu` | `tripfz-jmr.icu` 的去连字符版 |
| `yintins.icu` | `yintins-01.icu` 的干净版 |
| `niuniu869.icu` | **`niuniu.icu` 抢注失败的替代品** —— 那位现在的空头支票，落点应该就是这个 |
| `carleight.icu` `carleightwu.icu` | 同一个人的两个 |

**像是人名 / 个人 ID，但对不上现有的主**（14 个）：
`borancui` `ryanzhu` `sunyuchen` `daiyu1` `lay621` `cjl2726` `z091127`
`tatumisin` `cacinie` `techerng` `msadream` `qific` `awmcap` `m78ai`

**另外三个拿不准的**：`10kjs` `unknownuserfrommars` `cliproxyapi`。
最后那个要留意 —— **CLIProxyAPI 是一个真实存在的开源项目**，
如果不是那个项目作者本人要的，它该进第③类而不是公共池。

## ③ 封禁（12）—— 留着，但一个邮箱都不开

**商标近似（10）**

| 域名 | 撞的是 |
|---|---|
| **`githubusercontent.icu`** | ★ 最危险的一个。`raw.githubusercontent.com` 是所有开发者的肌肉记忆 |
| `huggingface.icu` | Hugging Face |
| `airtable.icu` | Airtable |
| `opencart.icu` | OpenCart（真实开源电商项目）|
| `openreview.icu` | OpenReview.net（真实学术平台）|
| `moonshot48.icu` | 月之暗面 Moonshot AI |
| `claudex.icu` | Anthropic Claude |
| `bilibill.icu` | bilibili 的形近拼写 —— 这正是仿冒域名的典型形态 |
| `dgxspark.icu` | NVIDIA DGX Spark |
| `adventurex.icu` | AdventureX（国内黑客松）|

**真人姓名（1）**：`余承东.icu`（华为高管）

**待你一句话（1）**：`babycam.icu` —— 同批的 `camhub` 你已经放进⑤了，
这个你没提，我先留在这。要挪过去说一声。

## ④ 靓号池（22）

| 档 | 年租 | 域名 |
|---|---|---|
| **S** | 400 | `tsuki` `seeus` `icubed` `spotme` `bluecat` |
| **A** | 150 | `opencode` `watchdog` `filedrop` `fastnote` `typeless` `canusee` `kandian` `quickview` `snapview` |
| **B** | 60 | `visionai` `deepvision` `quantwhale` `dailyplan` `takemyhand` `takemehand` `aicam` `aiclip` |

分档的依据只有两条：**短**，以及**是不是一个人会想拿来当自己主邮箱的词**。
`icubed.icu`（i-cubed）单独说一句 —— 它和 `.icu` 后缀本身押上了，这种巧合很少有。

`takemyhand` / `takemehand` 是一对近义词，放同一档，
免得有人拿到便宜那个之后觉得吃亏。

## ⑤ 一次性池（14）—— 跑一次性箱，同时也能申领

`pneumonoultramicroscopicsilicovolcanoconiosis` `我真的特别特别特别特别特别想你`
`云上耀斑` `rickroll` `dontgoto` `icudoctor` `orange-public` `trinity3`
`northwind` `agibar` **`马嘉祺` `华立` `teensintimes` `camhub`**（后四个 8-13 由站长挪进来）

挑选标准正是第二节那条 —— **进这个池的域名要能被牺牲掉**。
它们迟早会被某些网站列进临时邮箱黑名单，那时候被牺牲的是这十四个，
不是 `tsuki.icu`。

四点实现注意：

- **四个中文域名要存 punycode**。SMTP 的信封里只能是 A 标签：

  | | |
  |---|---|
  | `我真的特别…想你.icu` | `xn--6qqw1eaaaa206xh6b9z6dbabbb595logc.icu`（41 字符）|
  | `云上耀斑.icu` | `xn--fhqrmz20dmfy.icu` |
  | `马嘉祺.icu` | `xn--w4rs83f4sw.icu` |
  | `华立.icu` | `xn--xkrw23g.icu` |

  另外**很多网站的注册表单直接拒收 IDN 邮箱地址** ——
  所以这四个适合当补充，**不该是随机分配时的默认域名**。
- `pneumonoultra…icu` 一共 49 字符，加上前缀仍远低于 254 的地址上限，能用。
  它在一次性池里是加分项：**没有人会想长期用它，而它确实能收信**。
- ⚠ **`马嘉祺` `华立` `teensintimes` `camhub` 建议只发随机前缀，不开放自选**，
  也不进「随机分配」的默认轮换。理由：自选前缀 + 这四个域名，
  组合出来的地址才是有杀伤力的那种；随机的 12 位串没有这个问题。
  这是一行配置（域名上加一个 `allow_custom_local` 开关），不是新机制。
- `teensintimes` 和 `camhub` 我建议在**申领**那条路上也关掉
  （只跑一次性箱、不给长期占）—— 一个 24 小时就消失的地址，
  和一个署着你名字挂一年的地址，性质不一样。

## ⑥ 站长自己的 `owned`（2）

`muran.icu` `jiangmuran.icu` —— **指给站长本人的账号**，`kind = 'owned'`，
和另外 28 个有主域名走完全一样的路径。

站长的原话是「方便我测试逻辑」，而这正好是最好的安排：
**自有域名那条路径的第一个用户是你自己**。catch-all 没生效、
前缀发现没跑起来、DNS 灯是错的 —— 这些你当天就会撞上，
而不是等某个拿到域名的人来报。
