# 施工计划

按迭代排序。每完成一项都要**写测试并跑通**，不留「先跑起来回头再补」的账。

进度：✅ 完成 · 🔨 进行中 · ⬜ 未开始

---

## 已完成

| # | 内容 | 测试 |
|---|---|---|
| ✅ | 上游 API 客户端（类型按实测定义、按端点钳制 limit、重试与降级） | — |
| ✅ | 消息镜像 + FTS5 中文检索（CJK 逐字切分） | 6 |
| ✅ | 高质量消息判定（穷举校准，与上游 10/10 吻合） | 4 |
| ✅ | 三通道绑定登录 + 会话 + 限流 | — |
| ✅ | RBAC 底座：65 权限点 / 9 身份组 / `can()` 判定器 | — |
| ✅ | people 表：1803 人的显示名与头像 | — |
| ✅ | 工程基建：git / 备份 / 健康探测 / 审计入口 | — |
| ✅ | 应用外壳：移动 Tab Bar + 桌面侧边栏 + Large Title | 10 |
| ✅ | 首页 / 排行 / 我的 三个页面 | — |
| ✅ | 配色切换（自动 / 浅色 / 深色） | 4 |
| ✅ | 每日统计落库（数据库往返测试） | 6 |
| ✅ | **群可见性收口**：总榜公开，群列表与分群数据仅成员可见 | 16 |
| ✅ | **Passkey**：注册/登录/多设备/会话管理/登录历史 | 19 |
| ✅ | 论坛数据模型（12 张表，含编辑历史与可见性审计） | — |
| ✅ | **Markdown 管线**：marked + DOMPurify 允许清单 + shiki 高亮 | 32 |
| ✅ | **论坛可见性判定**：6 级 × 6 身份矩阵 + 三条硬约束 | 50 |
| ✅ | 论坛页面：版块 / 列表 / 详情 / 发帖 / 回帖 + Markdown 编辑器 | — |
| ✅ | 反应 / 收藏 / 订阅 / **通知聚合** + 未读角标 | 15 |
| ✅ | **积分流水**：唯一真值 / 只增不改 / 幂等 / 冲正 / 可对账 | 16 |
| ✅ | 问答：悬赏（发起即扣）+ 采纳（转移积分）| — |
| ✅ | 论坛全文检索 + **结果按可见性过滤** | 14 |
| ✅ | 版主工具 / 举报 / 申诉 / 标签归一化 | 12 |
| ✅ | 公开编辑历史 + 行级 diff 与折叠 | 9 |
| ✅ | 群聊转帖 + 同意流程（全员同意才可公开）| 9 |
| ✅ | 投票 / 打赏 / OG 分享卡片 | 20 |
| ✅ | HIG 交互：撤销 / 手势 / 骨架屏 / 快捷键 / 无障碍 | 16 |
| ✅ | **积分规则引擎**：打卡 / 连胜 / 等级 / 反作弊 | 26 |
| ✅ | 登录门禁中间件 + 开放重定向防护 | 7 |
| ✅ | **群消息检索** + 时间机器 + 上下文展开 | 22 |
| ✅ | 检索片段跨标签还原（修显示 bug）| 5 |
| ✅ | 后台外壳 + 仪表盘 + 审计日志 | 9 |
| ✅ | **部署脚本**：任何一步失败都不重启服务 | — |

共 350 个测试通过。

**迭代 2（论坛）、3（积分）、4（检索）已全部完成。**

---

## 迭代 1 — Passkey 与登录体系 ✅

- [x] WebAuthn 注册：绑定成功后在 /onboarding 引导设置
- [x] WebAuthn 登录：discoverable credential，不必先报身份
- [x] **多设备**：/me/登录与安全 里可绑定任意多把，逐个命名与移除
- [x] 会话管理：设备列表、单个下线、全部下线
- [x] 登录历史（含失败尝试），用户自己可见
- [x] 计数器克隆检测（存量为 0 时不误判，否则 iPhone 用户全被挡）
- [x] 登录限流（只算失败尝试，成功不消耗配额）
- [ ] 密码兜底（Argon2id）—— Passkey 覆盖不到的浏览器再补
- [ ] 管理员强制 Passkey 或 2FA（配置项已在库里，待后台接入）

---

## 迭代 2 — 论坛（核心）✅

**这是整个站最重要的一块**，不是附属功能。它承担群聊承担不了的事：沉淀。
完整规格见 `FORUM.md`，这里只列阶段。

| 阶段 | 内容 |
|---|---|
| 2.1 | ✅ 数据模型 · Markdown 管线 · 可见性判定 · 版块/列表/详情/发帖/回帖 · 编辑器 |
| 2.2 | **可见性分层**（6 级）+ 未登录浏览 + 三条硬约束 |
| 2.3 | ✅ 多维反应（乐观更新）/ 收藏 / 订阅 / 通知聚合 + 未读角标 |
| 2.4 | ✅ 问答（悬赏 + 采纳）· 全文搜索 · **积分流水**（迭代 3 的地基）｜标签体系待补 |
| 2.5 | ✅ 版主工具 · 举报 · **申诉** · **公开编辑历史（带 diff）** · 标签归一化 |
| 2.6 | ✅ **群聊一键成帖 + 全员同意才能公开** · 消息挑选器（拖选）|
| 2.7 | ✅ 投票（可撤票/投票后可见结果）· 打赏（走积分流水）· 动态分享卡片 |
| 2.8 | ✅ 撤销 Toast · 左滑手势 · 骨架屏 · 快捷键 · 跳到正文 · aria 标注 |

**权限分级**贯穿全程：版块级配置（谁能看/发/回、等级门槛、匿名开关）、
六级内容可见性、版主限定版块、楼主管理本帖、新人限制与频率限制。

**测试重点**：可见性判定必须做**矩阵测试** ——
6 个级别 × 5 类身份 = 30 种组合逐一断言。这是论坛最不能出错的地方。
另外要断言未登录访问私有帖返回 404 而非 403（403 会泄露「这个帖子存在」）。

---

## 迭代 3 — 积分 · 打卡 · 等级 ✅

- [x] 打卡（当日高质量 ≥ N 才可打，先有贡献再签到）
- [x] 积分规则引擎（纯函数，全部数值读 settings，零魔法数字）
- [x] 连胜（断签重置、奖励封顶）
- [x] 等级 L1–L10，按**累计获得**算 —— 花积分不掉级
- [x] `points_ledger` 冲正机制（绝不改原记录）
- [x] 反作弊：同分钟折叠、近似内容去重、异常进人工复核队列
- [x] 积分只由服务端发放，前端无任何加分入口
- [x] 登录门禁中间件（含开放重定向防护）
- [ ] 补签卡 —— 留到商店一起做（它是商品，不是打卡功能）

---

## 迭代 4 — 群消息检索 ✅

- [x] 搜索页：跨群 / 按人 / 按时间 / 只搜自己
- [x] **权限收口**：SQL 层就限定可见群，不是查出来再过滤
- [x] 结果高亮 + **就地展开前后文**（不跳走，保住滚动位置）
- [x] 时间机器：按天回看任意一天，日期导航常驻顶部
- [x] 我的存档：只搜自己说过的话

**测试**：22 项，含「换个人搜同一个词必须只剩他能看的」、
越权指定群返回空、上下文不串台、注入字符不炸。

---

## 迭代 5 — 后台管理（全量）⬜

**这是整个项目里体量最大的一块。** 表结构已经全部就位，
现在要把 11 个模块逐个做出来。每个模块都必须落盘到位 ——
验收标准见 `SCHEMA.md` 第十一节那张对照表。

### 5.1 总览仪表盘 ✅
- [x] DAU/WAU/MAU、新增绑定、消息量与高质量占比
- [x] 积分发放、打卡、论坛指标
- [x] **系统健康**：各组件状态、同步失败数、探测是否停摆
- [x] 磁盘水位、近 14 天消息趋势
- [x] **待办放最前**：举报/申诉/积分异常，每个数字都指向一个动作

### 5.2 用户管理 ✅
- [x] 搜索与多条件筛选（站内昵称/微信昵称/wxid/邮箱/账号 id 都能搜）
- [x] 用户详情：档案 / 所在群 / 积分流水 / 登录日志 / 设备与 Passkey /
      论坛行为 / 处罚记录 / 管理操作历史 / 生效权限（带来源）
- [x] 操作：改角色、调整积分（必填理由）、封禁解封、强制下线
- [x] 管理员备注（用户不可见）
- [ ] 导出、改等级、封禁期限、解绑重绑、合并账号、重置凭证
- [ ] **绑定审批队列**：好友申请列表 + 申请人活跃度，一键通过或拒绝

**护栏**（`src/lib/admin/rules.ts`，纯函数、可单测）：
每条写操作必填理由、不能改自己的状态、不能移除最后一个站长、
大额积分调整要额外权限、提权检查先于重复检查。
这些以前写在 server action 里，测试碰不到 —— 现在两边引同一份。

### 5.3 身份组与权限 ✅（矩阵只读）
- [x] **权限矩阵**：行=身份组 × 列=权限点，三态（允许/显式拒绝/未授予）
- [x] **权限反查**：选一个权限点，列出所有持有者及来源
- [x] 范围化授权与临时提权（到期自动失效，反查里不再计入）
- [ ] 矩阵在线编辑 + 保存前 diff 预览（「将获得 3 项、失去 1 项，影响 4 人」）
- [ ] **以某身份预览**：整站切成他的视角，常驻红色横幅（目前是占位面板）
- [ ] 自定义身份组（颜色/图标/优先级/名额上限/自动授予规则）
- [ ] 权限矩阵版本快照与一键回滚

**反查必须与 `can()` 同结论**，有测试锁死。三个来源里最容易漏的是
用户级显式拒绝 —— 只查「谁被授予」的话，被单独禁掉的人也会列在持有者里，
而那恰恰是最需要看清楚的一类。

### 5.4 内容审核（含论坛全套）✅
- [x] **举报队列**：按目标归组、严重度 + 先来先处理排序、超时标红、认领、结案
- [x] **申诉流程** —— 有处罚就必须有申诉，否则只会积累怨气
- [x] 处罚记录聚合到用户档案（5.2 已做）
- [x] **版块管理**：增删改、可见性三档配置、层级、排序、锁定
- [x] **标签管理**：合并、重命名、锁定、清理无用标签
- [x] **敏感词库与自动拦截规则**：三档处置 + 归一化匹配 + 预览器
- [x] **版主任免**（限定版块，默认带到期时间）
- [x] **可见性提升审核队列**（群聊转帖想让更多人看到时走这里）
- [x] **帖子/回复管理、批量操作**

**改版块配置也是对别人内容的操作。** 收紧可见性上限会让已经发出去的帖子
从别人眼前消失，而作者不会收到任何通知 —— 他只会发现自己的帖子别人看不到了。
所以受影响的**篇数和标题在保存前就摆出来**，事后通知等于没通知。
上限收紧后帖子的 visibility 会真的被降下来：只改版块配置不动帖子的话，
封顶就成了摆设，任何直接读 posts.visibility 的地方都会照旧放行。

标签合并要处理「两个标签都有的帖子」，不去重的话唯一索引直接报错、
整次合并回滚 —— 而那恰恰是最该合并的信号。

版块页上同时显示**真实帖子数与冗余计数**，不一致就标黄：
那个冗余列漂移过一次（「群聊沉淀」实际 2 篇却显示 0），修好一次
不代表以后不会再漂。

**敏感词的重点不是拦得住，是别误伤。** 子串匹配必然误伤 ——
一个两字的词能在无数正常表达里出现，而误伤是**静默**的：
没人会来报告「我发不出去帖子」，他们只会不再发帖。所以：
默认档位是送审不是拦截、词长有下限、命中特别多的规则标黄提示
「大概率是误伤而不是这条规则有用」、以及一个能拿真文本试的预览器 ——
词库是一堆字符串，光看列表想象不出它会命中什么。

匹配前先归一化（去空白标点、全角转半角、统一小写），否则加个空格
就绕过去了；但归一化会打乱下标，而替换和高亮都需要原文位置，
所以同步维护一张下标映射表。发帖、编辑、回复三条路都过这道闸 ——
只查发帖的话，先发一篇干净的再编辑把词加进去就绕过了。

**可见性提升是「群聊转帖锁定在原群」这条硬约束的唯一出口**，
所以它自己是最严的一段流程：上限「仅成员」永远到不了公开
（群里说的话不该出现在搜索引擎里 —— 那不是「更多人能看到」，
是「所有人永远都能搜到」）、需要原作者同意、需要另一个人审核。
通过之后扩散不可逆，所以所有前置条件都要在写库之前满足，
不能「先批了再补」；界面上通过与驳回不做主次色差，
把通过做成醒目主按钮等于在鼓励点它。

**版主默认带到期时间**（三个月）而不是默认永久。
不设到期的话，一年后没人记得当初为什么给了这个人权限，
也没人好意思去收 —— 到期自动回收把这件尴尬事变成了默认行为。
过期的仍然列出来只是灰掉：悄悄消失会让人以为系统弄丢了配置。

**队列按目标归组，不按举报条数列表。** 十个人举报同一条内容列成十行的话，
版主会把同一个帖子处理十遍，真正要看的其他事被挤到第二页。
归组后举报人数反过来成为升级信号：三个互不相识的人同时举报，基本不会同时看错。

**三条治理底线写在 `moderation/rules.ts` 里，不是配置项**：
不能处理自己举报的、不能处理针对自己的举报、
**不能复核自己下的处罚**。最后一条原先缺失 —— 由原处罚人判申诉，
等于让他给自己的判断打分，那这个入口只是让人多绕一圈再绝望一次。
界面上这三条在渲染时就说明原因并禁用按钮，不是点下去才被拒绝。

排序上有两处刻意反直觉：同严重度**先来先处理**（按最新排会让老举报永远沉底），
超时按**最早**那条算（按最新算的话，持续被举报的内容永远不超时）。

### 5.5 积分与等级配置 —— 经济看板已完成
- [x] **积分经济看板** `/admin/points`：通胀体检、发行/回收去向、
      分配集中度、每日上限的松紧、发行最多的人
- [x] 所有发行参数进 settings（每日上限、互动权重折算、连胜上限、手续费）
- [x] 手动发放/扣除（必填理由，大额需额外权限，见 5.2）
- [ ] 等级门槛与解锁权限的后台编辑
- [ ] 积分流水全站视图、风控队列

策略见 `ECONOMY.md`。支点是**每人每日发行上限、所有来源共享同一个预算** ——
各来源各自封顶意味着每加一个玩法就是给通胀开一个新口子。

### 5.5b 称号 ✅
- [x] 称号表 + 用户持有表 + 佩戴（`users.activeTitleId`）
- [x] 四种来源：授予 / 成就 / 购买 / 赛季；四档稀有度
- [x] 名额上限、租期与续费、到期与撤销分开
- [x] 个人页称号架（点一下佩戴，再点摘下，过期的变灰仍陈列）
- [x] 后台授予/收回（下拉里直接标出剩余名额）
- [x] `npm run grant-title` 批量授予，默认可 `--dry-run` 预演
- [ ] 成就型的自动授予任务
- [ ] 租用型的到期扣费任务

**佩戴的称号必须重新校验有效期**：`activeTitleId` 是冗余列，
租用称号到期后没有任何人会去清它 —— 不校验的话过期称号会一直挂在名字后面。

### 5.5c 旧的积分配置项
- [ ] 所有数值后台可改，改动进 `setting_history` 可回滚
- [ ] 等级门槛与解锁权限
- [ ] 手动发放/扣除（必填理由，大额走双人复核）
- [ ] 积分流水全站视图、风控队列
- [ ] 榜单重算 / 补发工具（先出 preview 再执行）

### 5.6 群与数据源
- [ ] 群列表、bound 状态、每群配置（quality_min 覆盖、是否计分、榜单是否公开）
- [ ] 手动触发同步、同步日志与失败重试
- [ ] API key 状态与配额

### 5.7 活动与模块
- [ ] 模块列表：启停开关、健康度、事件日志、熔断恢复
- [ ] 安装时展示 grants 权限清单要求确认
- [ ] 配置表单由 configSchema 自动生成
- [ ] 活动管理：创建、资格规则可视化编辑（实时显示符合人数）、
      申请审批、批量导出导入、名额流水

### 5.8 商店与订单
- [ ] 商品上下架、库存、兑换记录
- [ ] 实物订单发货状态流转（每次流转留痕）
- [ ] 退款与积分回滚

### 5.9 公告与推送
- [ ] 站内公告（横幅/弹窗/站内信，可定向到角色或群）
- [ ] **微信群发**：二次确认 + 预览快照 + 双人复核 + 频率上限 + 全量留痕
- [ ] 邮件群发（同样限流留痕）
- [ ] 每周精选推送的草稿审核界面

### 5.10 审计与系统
- [x] 审计日志检索（按动作/危险等级/时间）+ 变更前后快照，**无删除接口**
- [ ] 危险操作双人复核队列
- [ ] 系统设置与功能开关
- [ ] 长任务管理（存储裁剪、积分重算，先 preview 再执行）
- [ ] 后台路径可配 + 可选 IP 白名单

### 5.11 邀请体系
- [ ] 邀请码生成（限次/限时/绑定角色）
- [ ] 邀请树可视化，追溯上游与下游
- [ ] 邀请奖励与被封时的回滚

**测试**：每个写操作都要验证「三处齐全」（业务表 + 审计日志 + 通知）；
权限矩阵的先拒后允顺序；双人复核不能自批；审计日志无法删除。

已完成部分 **116 项**（5.1–5.4 举报与申诉）：权限反查与 `can()` 结论一致、过期授权不算数、
用户级拒绝能剔掉身份组给的权限、不能移除最后一个站长、不能改自己的状态、
大额调整门槛、后台整棵树未登录必须跳转、十人举报同一目标只占一行、
不能复核自己下的处罚、超时按最早那条算、标了 ready 的入口必须真的有页面。

---

## 迭代 6 — 可插拔活动系统 ⬜

- [ ] 模块运行时（manifest、SDK、故障隔离、熔断）
- [ ] 通用活动骨架 + 状态机 + 资格引擎
- [ ] 名额并发与超卖防护
- [ ] **域名注册活动**（第一个模块实例）
- [ ] 临时邮箱模块
- [ ] 每周精选回推模块

**测试**：并发抢名额不超卖、资格快照冻结、
状态机非法流转被拒、模块崩溃不影响主站。

---

## 迭代 7 — 打磨 ⬜

- [ ] 成员目录 + 技能标签
- [ ] 链接资源库
- [ ] 关键词雷达
- [ ] 通知中心
- [ ] 存储分层裁剪任务
- [ ] 异地备份
- [ ] 告警通道（隧道断了要有人知道）
- [ ] 无障碍走查、性能、移动端手势

---

## 贯穿始终的约束

1. **每个迭代结束必须跑通全部测试**，不留欠账
2. 代码里零魔法数字 —— 一切阈值进 `settings`
3. 每个后台写操作三处齐全：业务表 + `audit_logs` + 通知
4. 权限判定只走 `can()`，不写 `if (role === ...)`
5. 故障不能伪装成业务结果（「你不是社群成员」那次教训）
6. 上游返回的形态必须实测，不照文档推测
7. **群列表与群数据属于隐私**：访客一个群都看不到，成员只看自己所在的群。
   任何按群聚合的查询都必须先过 `visibleGroupIds()`；漏传的后果是空结果，
   不是全量结果 —— 这是靠必填参数在类型层面强制的
8. 同一段逻辑不写两遍。导航可见性、桶键构造这类判定都提取成共用函数，
   测试调用生产代码的那一份，不自己重写。
   **判定藏在拿不到的地方（server action、middleware）时，把它抽出来** ——
   `/admin` 漏进门禁那次，测试里抄了一份路径规则，所以照样全绿
