# 施工计划

按迭代排序。每完成一项都要**写测试并跑通**，不留「先跑起来回头再补」的账。

进度：✅ 完成 · 🔨 进行中 · ⬜ 未开始

---

## 已完成

| # | 内容 | 测试 |
|---|---|---|
| ✅ | 上游 API 客户端（类型按实测定义、按端点钳制 limit、重试与降级） | — |
| ✅ | 消息镜像 + FTS5 中文检索（CJK 逐字切分） | 6 |
| ✅ | 高质量消息判定（穷举校准，与上游 10/10 吻合） | 4 |
| ✅ | 三通道绑定登录 + 会话 + 限流 | — |
| ✅ | RBAC 底座：65 权限点 / 9 身份组 / `can()` 判定器 | — |
| ✅ | people 表：1803 人的显示名与头像 | — |
| ✅ | 工程基建：git / 备份 / 健康探测 / 审计入口 | — |
| ✅ | 应用外壳：移动 Tab Bar + 桌面侧边栏 + Large Title | 10 |
| ✅ | 首页 / 排行 / 我的 三个页面 | — |
| ✅ | 配色切换（自动 / 浅色 / 深色） | 4 |
| ✅ | 每日统计落库（数据库往返测试） | 6 |
| ✅ | **群可见性收口**：总榜公开，群列表与分群数据仅成员可见 | 16 |
| ✅ | **Passkey**：注册/登录/多设备/会话管理/登录历史 | 19 |
| ✅ | 论坛数据模型（12 张表，含编辑历史与可见性审计） | — |
| ✅ | **Markdown 管线**：marked + DOMPurify 允许清单 + shiki 高亮 | 32 |
| ✅ | **论坛可见性判定**：6 级 × 6 身份矩阵 + 三条硬约束 | 50 |
| ✅ | 论坛页面：版块 / 列表 / 详情 / 发帖 / 回帖 + Markdown 编辑器 | — |
| ✅ | 反应 / 收藏 / 订阅 / **通知聚合** + 未读角标 | 15 |
| ✅ | **积分流水**：唯一真值 / 只增不改 / 幂等 / 冲正 / 可对账 | 16 |
| ✅ | 问答：悬赏（发起即扣）+ 采纳（转移积分）| — |
| ✅ | 论坛全文检索 + **结果按可见性过滤** | 14 |
| ✅ | 版主工具 / 举报 / 申诉 / 标签归一化 | 12 |
| ✅ | 公开编辑历史 + 行级 diff 与折叠 | 9 |
| ✅ | 群聊转帖 + 同意流程（全员同意才可公开）| 9 |
| ✅ | 投票 / 打赏 / OG 分享卡片 | 20 |
| ✅ | HIG 交互：撤销 / 手势 / 骨架屏 / 快捷键 / 无障碍 | 16 |
| ✅ | **积分规则引擎**：打卡 / 连胜 / 等级 / 反作弊 | 26 |
| ✅ | 登录门禁中间件 + 开放重定向防护 | 7 |
| ✅ | **群消息检索** + 时间机器 + 上下文展开 | 22 |
| ✅ | 检索片段跨标签还原（修显示 bug）| 5 |
| ✅ | 后台外壳 + 仪表盘 + 审计日志 | 9 |
| ✅ | **部署脚本**：任何一步失败都不重启服务 | — |

共 350 个测试通过。

**迭代 2（论坛）、3（积分）、4（检索）已全部完成。**

---

## 迭代 1 — Passkey 与登录体系 ✅

- [x] WebAuthn 注册：绑定成功后在 /onboarding 引导设置
- [x] WebAuthn 登录：discoverable credential，不必先报身份
- [x] **多设备**：/me/登录与安全 里可绑定任意多把，逐个命名与移除
- [x] 会话管理：设备列表、单个下线、全部下线
- [x] 登录历史（含失败尝试），用户自己可见
- [x] 计数器克隆检测（存量为 0 时不误判，否则 iPhone 用户全被挡）
- [x] 登录限流（只算失败尝试，成功不消耗配额）
- [x] 密码兜底 —— 用 **scrypt**（Node 自带）而不是 Argon2id：后者要装原生依赖，
      而这条路本来就是「Passkey 用不了时的备用钥匙」，为它引入编译期依赖不值。
      参数在生产两核机上实测 117ms
- [x] **管理员强制 Passkey** —— 那个配置项在库里躺着、默认 true、设置页显示成开着的，
      而没有一行代码读它。现在真的读了：手里有任何 dangerLevel ≥ 2 权限的人
      （按权限判，不按角色名判）用密码登不进来。判定放在密码校验**之后**，
      否则「这个微信号是不是管理员」会变成一个免密查询接口。
      「开了会锁住谁」在设置页和 /api/health 上一直看得见

---

## 迭代 2 — 论坛（核心）✅

**这是整个站最重要的一块**，不是附属功能。它承担群聊承担不了的事：沉淀。
完整规格见 `FORUM.md`，这里只列阶段。

| 阶段 | 内容 |
|---|---|
| 2.1 | ✅ 数据模型 · Markdown 管线 · 可见性判定 · 版块/列表/详情/发帖/回帖 · 编辑器 |
| 2.2 | **可见性分层**（6 级）+ 未登录浏览 + 三条硬约束 |
| 2.3 | ✅ 多维反应（乐观更新）/ 收藏 / 订阅 / 通知聚合 + 未读角标 |
| 2.4 | ✅ 问答（悬赏 + 采纳）· 全文搜索 · **积分流水**（迭代 3 的地基）｜标签体系待补 |
| 2.5 | ✅ 版主工具 · 举报 · **申诉** · **公开编辑历史（带 diff）** · 标签归一化 |
| 2.6 | ✅ **群聊一键成帖 + 全员同意才能公开** · 消息挑选器（拖选）|
| 2.7 | ✅ 投票（可撤票/投票后可见结果）· 打赏（走积分流水）· 动态分享卡片 |
| 2.8 | ✅ 撤销 Toast · 左滑手势 · 骨架屏 · 快捷键 · 跳到正文 · aria 标注 |
| 2.9 | ✅ **帖子页行内管理**：统一操作菜单（编辑/加精/置顶/锁定/移动版块/删除/恢复，全走 `can()`，服务端复判）· 编辑帖子页 · 引用回复接线 · 只看楼主 · 跳回上次阅读楼层 |

### 2.9 补充说明与论坛待办

盘点时发现的**声明了但没人调用**（已接上）：`moderatePost`、`deleteMyPost` /
`restoreMyPost`（顺手补了 can() 判定、审计、摘索引、版块计数）、`editPost`
（原来连编辑页都没有）、`markReadFloor`、`createReply.quotedReplyId`。
接线用 `tests/forum-wiring.test.ts` 锁住 —— 这类断线类型系统查不出来。

还缺的（按影响排序，做的时候一次挑两三个）：

- [x] 编辑**回复**（schema 里 `replies.edit_count` 字段已在，action 和界面都没有）
- [x] 版主折叠低质回复的界面入口（`moderateReply` 支持 collapse，没接）+ 折叠展示
- [ ] 楼中楼 / 树形视图切换（版块可设默认）
- [x] 收藏夹分组（`bookmark_folders` 表已建，界面只有单一收藏）
- [x] 关注作者 / 版块（`subscriptions.target_type` 已预留）；标签那一路的扇出也做了，
      **但生产上还一个标签都没有**，所以没做关注标签的按钮 —— 那会是又一个死开关
- [x] 服务端草稿（换设备接着写；现在只有 IndexedDB/localStorage 本地草稿）
- [x] 定时发布（`scheduled_at` 字段已在）—— 挂在五分钟一轮的 health tick 上
- [x] 帖内跳转到未读第一条 + 阅读进度条
- [x] 楼主锁定自己的帖子（FORUM.md 4.3 允许，现在只有版主能锁）—— 楼主只解得开自己加的那把

**权限分级**贯穿全程：版块级配置（谁能看/发/回、等级门槛、匿名开关）、
六级内容可见性、版主限定版块、楼主管理本帖、新人限制与频率限制。

**测试重点**：可见性判定必须做**矩阵测试** ——
6 个级别 × 5 类身份 = 30 种组合逐一断言。这是论坛最不能出错的地方。
另外要断言未登录访问私有帖返回 404 而非 403（403 会泄露「这个帖子存在」）。

---

## 迭代 3 — 积分 · 打卡 · 等级 ✅

- [x] 打卡（当日高质量 ≥ N 才可打，先有贡献再签到）
- [x] 积分规则引擎（纯函数，全部数值读 settings，零魔法数字）
- [x] 连胜（断签重置、奖励封顶）
- [x] 等级 L1–L10，按**累计获得**算 —— 花积分不掉级
- [x] `points_ledger` 冲正机制（绝不改原记录）
- [x] 反作弊：同分钟折叠、近似内容去重、异常进人工复核队列
- [x] 积分只由服务端发放，前端无任何加分入口
- [x] 登录门禁中间件（含开放重定向防护）
- [ ] 补签卡 —— 留到商店一起做（它是商品，不是打卡功能）

---

## 迭代 4 — 群消息检索 ✅

- [x] 搜索页：跨群 / 按人 / 按时间 / 只搜自己
- [x] **权限收口**：SQL 层就限定可见群，不是查出来再过滤
- [x] 结果高亮 + **就地展开前后文**（不跳走，保住滚动位置）
- [x] 时间机器：按天回看任意一天，日期导航常驻顶部
- [x] 我的存档：只搜自己说过的话

**测试**：22 项，含「换个人搜同一个词必须只剩他能看的」、
越权指定群返回空、上下文不串台、注入字符不炸。

---

## 迭代 5 — 后台管理（全量）⬜

**这是整个项目里体量最大的一块。** 表结构已经全部就位，
现在要把 11 个模块逐个做出来。每个模块都必须落盘到位 ——
验收标准见 `SCHEMA.md` 第十一节那张对照表。

### 5.1 总览仪表盘 ✅
- [x] DAU/WAU/MAU、新增绑定、消息量与高质量占比
- [x] 积分发放、打卡、论坛指标
- [x] **系统健康**：各组件状态、同步失败数、探测是否停摆
- [x] 磁盘水位、近 14 天消息趋势
- [x] **待办放最前**：举报/申诉/积分异常，每个数字都指向一个动作

### 5.2 用户管理 ✅
- [x] 搜索与多条件筛选（站内昵称/微信昵称/wxid/邮箱/账号 id 都能搜）
- [x] 用户详情：档案 / 所在群 / 积分流水 / 登录日志 / 设备与 Passkey /
      论坛行为 / 处罚记录 / 管理操作历史 / 生效权限（带来源）
- [x] 操作：改角色、调整积分（必填理由）、封禁解封、强制下线
- [x] 管理员备注（用户不可见）
- [ ] 导出、改等级、封禁期限、解绑重绑、合并账号、重置凭证
- [x] **绑定审批队列**：好友申请列表 + 申请人活跃度，手动绑定或作废。
      审批的唯一依据是「他是不是真的在我们群里」—— 手动绑定**硬性要求**
      那个微信号在同步的群里，没有例外（留了例外，「只有群成员能登录」
      就退化成一个默认值）。「卡住的人」按 IP 聚合、门槛量出来定在
      取过 ≥2 次码且一直没成功

**护栏**（`src/lib/admin/rules.ts`，纯函数、可单测）：
每条写操作必填理由、不能改自己的状态、不能移除最后一个站长、
大额积分调整要额外权限、提权检查先于重复检查。
这些以前写在 server action 里，测试碰不到 —— 现在两边引同一份。

### 5.3 身份组与权限 ✅
- [x] **权限矩阵**：行=身份组 × 列=权限点，三态（允许/显式拒绝/未授予）
- [x] **权限反查**：选一个权限点，列出所有持有者及来源
- [x] 范围化授权与临时提权（到期自动失效，反查里不再计入）
- [x] **矩阵在线编辑 + 保存前 diff 预览**（「失去 1 项、获得 3 项，影响 4 人」）。
      影响面**按人算**不按身份组人数算 —— 预演的办法是在事务里真改一遍、
      调同一个 can.ts 量完、再回滚,所以预览和现实之间没有第二份实现。
      护栏:不能授予自己没有的权限（否则管理员编辑「管理员」组就能绕开
      ADMIN_DENIES）、不能动优先级比自己高的组、不能把 role.manage 摘光
      （那是把门从里面锁上再把钥匙扔了）、必填理由、认不出的权限点直接拒绝
- [x] **以某身份预览**：整站切成他的视角，常驻红色横幅。**权限只减不增**
      （生效的是「他的 ∩ 我的」，扣掉哪几项写在横幅里）、**全程只读**
      （判定照实回答所以按钮该出现就出现，真去点时拦下）、30 分钟过期、
      进出与掐断都进审计。只给站长，因为切成普通成员会看到他的私人视角。
- [ ] 自定义身份组（颜色/图标/优先级/名额上限/自动授予规则）
- [x] **权限矩阵版本快照与一键回滚**。快照拍的是**那次改动之前**的整张表,
      所以第一次编辑会顺手留下原始状态,不用另外种基线。回滚遍历
      **快照与当前的并集** —— 只看快照的话,快照之后新增的越权授权
      会留在原地,而回滚报告成功。回滚走同一套护栏（否则它就是绕开
      提权检查的近路）,并且自己也进历史。留最近 50 张

**反查必须与 `can()` 同结论**，有测试锁死。三个来源里最容易漏的是
用户级显式拒绝 —— 只查「谁被授予」的话，被单独禁掉的人也会列在持有者里，
而那恰恰是最需要看清楚的一类。

### 5.4 内容审核（含论坛全套）✅
- [x] **举报队列**：按目标归组、严重度 + 先来先处理排序、超时标红、认领、结案
- [x] **申诉流程** —— 有处罚就必须有申诉，否则只会积累怨气
- [x] 处罚记录聚合到用户档案（5.2 已做）
- [x] **版块管理**：增删改、可见性三档配置、层级、排序、锁定
- [x] **标签管理**：合并、重命名、锁定、清理无用标签
- [x] **敏感词库与自动拦截规则**：三档处置 + 归一化匹配 + 预览器
- [x] **版主任免**（限定版块，默认带到期时间）
- [x] **可见性提升审核队列**（群聊转帖想让更多人看到时走这里）
- [x] **帖子/回复管理、批量操作**

**改版块配置也是对别人内容的操作。** 收紧可见性上限会让已经发出去的帖子
从别人眼前消失，而作者不会收到任何通知 —— 他只会发现自己的帖子别人看不到了。
所以受影响的**篇数和标题在保存前就摆出来**，事后通知等于没通知。
上限收紧后帖子的 visibility 会真的被降下来：只改版块配置不动帖子的话，
封顶就成了摆设，任何直接读 posts.visibility 的地方都会照旧放行。

标签合并要处理「两个标签都有的帖子」，不去重的话唯一索引直接报错、
整次合并回滚 —— 而那恰恰是最该合并的信号。

版块页上同时显示**真实帖子数与冗余计数**，不一致就标黄：
那个冗余列漂移过一次（「群聊沉淀」实际 2 篇却显示 0），修好一次
不代表以后不会再漂。

**敏感词的重点不是拦得住，是别误伤。** 子串匹配必然误伤 ——
一个两字的词能在无数正常表达里出现，而误伤是**静默**的：
没人会来报告「我发不出去帖子」，他们只会不再发帖。所以：
默认档位是送审不是拦截、词长有下限、命中特别多的规则标黄提示
「大概率是误伤而不是这条规则有用」、以及一个能拿真文本试的预览器 ——
词库是一堆字符串，光看列表想象不出它会命中什么。

匹配前先归一化（去空白标点、全角转半角、统一小写），否则加个空格
就绕过去了；但归一化会打乱下标，而替换和高亮都需要原文位置，
所以同步维护一张下标映射表。发帖、编辑、回复三条路都过这道闸 ——
只查发帖的话，先发一篇干净的再编辑把词加进去就绕过了。

**可见性提升是「群聊转帖锁定在原群」这条硬约束的唯一出口**，
所以它自己是最严的一段流程：上限「仅成员」永远到不了公开
（群里说的话不该出现在搜索引擎里 —— 那不是「更多人能看到」，
是「所有人永远都能搜到」）、需要原作者同意、需要另一个人审核。
通过之后扩散不可逆，所以所有前置条件都要在写库之前满足，
不能「先批了再补」；界面上通过与驳回不做主次色差，
把通过做成醒目主按钮等于在鼓励点它。

**版主默认带到期时间**（三个月）而不是默认永久。
不设到期的话，一年后没人记得当初为什么给了这个人权限，
也没人好意思去收 —— 到期自动回收把这件尴尬事变成了默认行为。
过期的仍然列出来只是灰掉：悄悄消失会让人以为系统弄丢了配置。

**队列按目标归组，不按举报条数列表。** 十个人举报同一条内容列成十行的话，
版主会把同一个帖子处理十遍，真正要看的其他事被挤到第二页。
归组后举报人数反过来成为升级信号：三个互不相识的人同时举报，基本不会同时看错。

**三条治理底线写在 `moderation/rules.ts` 里，不是配置项**：
不能处理自己举报的、不能处理针对自己的举报、
**不能复核自己下的处罚**。最后一条原先缺失 —— 由原处罚人判申诉，
等于让他给自己的判断打分，那这个入口只是让人多绕一圈再绝望一次。
界面上这三条在渲染时就说明原因并禁用按钮，不是点下去才被拒绝。

排序上有两处刻意反直觉：同严重度**先来先处理**（按最新排会让老举报永远沉底），
超时按**最早**那条算（按最新算的话，持续被举报的内容永远不超时）。

### 5.5 积分与等级配置 —— 经济看板已完成
- [x] **积分经济看板** `/admin/points`：通胀体检、发行/回收去向、
      分配集中度、每日上限的松紧、发行最多的人
- [x] 所有发行参数进 settings（每日上限、互动权重折算、连胜上限、手续费）
- [x] 手动发放/扣除（必填理由，大额需额外权限，见 5.2）
- [ ] 等级门槛与解锁权限的后台编辑
- [ ] 积分流水全站视图、风控队列

策略见 `ECONOMY.md`。支点是**每人每日发行上限、所有来源共享同一个预算** ——
各来源各自封顶意味着每加一个玩法就是给通胀开一个新口子。

### 5.5b 称号 ✅
- [x] 称号表 + 用户持有表 + 佩戴（`users.activeTitleId`）
- [x] 四种来源：授予 / 成就 / 购买 / 赛季；四档稀有度
- [x] 名额上限、租期与续费、到期与撤销分开
- [x] 个人页称号架（点一下佩戴，再点摘下，过期的变灰仍陈列）
- [x] 后台授予/收回（下拉里直接标出剩余名额）
- [x] `npm run grant-title` 批量授予，默认可 `--dry-run` 预演
- [ ] 成就型的自动授予任务
- [ ] 租用型的到期扣费任务

**佩戴的称号必须重新校验有效期**：`activeTitleId` 是冗余列，
租用称号到期后没有任何人会去清它 —— 不校验的话过期称号会一直挂在名字后面。

### 5.5c 旧的积分配置项
- [ ] 所有数值后台可改，改动进 `setting_history` 可回滚
- [ ] 等级门槛与解锁权限
- [ ] 手动发放/扣除（必填理由，大额走双人复核）
- [ ] 积分流水全站视图、风控队列
- [ ] 榜单重算 / 补发工具（先出 preview 再执行）

### 5.6 群与数据源 ✅
- [x] 群列表、bound 状态、每群配置（quality_min 覆盖、是否计分、榜单是否公开、
      保留天数、排除同步）
- [x] 手动触发同步（排队 + 消费者）、同步日志与失败重试、一键重试全部
- [x] **每群同步健康判定**：相对于该群自己的节奏，而不是统一阈值
- [x] 增量游标视图
- [ ] API key 状态与配额（上游没有暴露配额接口，暂时无从查起）

**这一页存在的唯一理由是发现数据没进来。** 上游断掉的表现是消息数不再增长，
而那和「今天大家没说话」在数据上长得一模一样 —— 榜单照常显示、
首页照常显示 0，没有任何地方会红。

所以异常判定必须相对于**每个群自己的节奏**：一天两百条的群安静半天就该查，
一周三条的群安静三天很正常。用统一阈值的话，冷清的群天天报警，
然后报警就会被忽略，真出事时也被忽略。

同步任务的健康看两件事：失败率**和**距上次成功多久。
只看失败率会漏掉「任务根本没在跑」—— 定时器停了的话失败率是 0，看起来完美。

上游可达性报的是「不知道」而不是「正常」：这一页的全部意义就是发现上游断了，
它自己却谎报健康的话，就成了最坏的那种仪表盘。

### 5.7 活动与模块 ✅（模块管理界面待做）
- [x] **核心表**：activities / applications / events / quota_log
- [x] **资格引擎**：声明式规则 + 可解释的判定结果
- [x] **状态机**：活动与申请的合法流转、名额占用语义
- [x] **名额并发安全**：带条件的单条 UPDATE，永不超卖 + 可对账
- [x] **模块①域名发放**：表单、校验、RDAP 可用性查询
- [x] **活动管理界面**：创建、规则编辑器（实时预估人数）、审批、履约回填、
      待注册清单导出
- [x] **用户侧申请界面**：够不够格、差多少、已登记什么、到哪一步了
- [ ] 模块列表：启停开关、健康度、熔断恢复（模块目前是代码注册，够用）

**资格规则编辑器的核心是右边那个数字。** 规则调一格，
「现在有 N 人够格」立刻重算，并列出「差一点点」的人 ——
门槛从 50 降到 40 能多放进来几个，这个具体数字比任何讨论都有说服力。

**名额和资格规则一旦开放就不能改**：改了的话先报名的人和后报名的人
按不同标准，而先报名的往往正是最积极的那批。所以建出来是草稿，
开放是单独的一步。

用户侧不够格时**把差距写出来而不是把入口藏起来** ——
「还差 13 条高质量发言」是一个能去做的目标，
藏起来的话人只会觉得这个活动跟自己无关。

**60 个名额被 300 人同时抢时，「先查再改」必然出错**：
两个请求同时读到 quota_used = 59，都判断「还有」，然后都加一。
所以扣减是一条带条件的 UPDATE，条件与写入在同一条语句里，中间没有窗口。
quota_used 只是缓存列，真值是 activity_quota_log —— 名额算错在限量活动里
是致命事故，必须能事后重算比对。

**资格判定必须能解释。** 只回答「够格/不够格」是不够的：
限量活动里最容易吵的是「凭什么他能申请我不能」，
所以每条规则都给出「你 87 条，要求 200 条」这样的具体差距。
指标缺失判为不通过而不是当成 0 或放行 —— 一个拼错的指标名
如果导致所有人都够格，这件事在开放前的人数预估里完全看不出来。

申请时冻结**资格快照**：事后有人质疑，翻快照即可，无从争议。

### 5.8 商店与订单 ✅
- [x] 商品上下架、库存、限购、兑换记录
- [x] 实物订单发货状态流转（每次流转留痕并通知用户）
- [x] 退款与积分冲正（同时归还库存）
- [x] 虚拟商品自动交付：称号、补签卡
- [ ] 置顶位的实际生效逻辑（商品已就位，交付还是人工）

**商店是积分的主要回收口**，不是「多一个玩法」。只发不收的积分一年后
必然废掉：商店价格变成笑话、新人永远追不上老人、积分不再代表任何东西。
在它上线之前，`/admin/points` 上的回收覆盖率一直是 0。

积分不能凭空消失也不能凭空出现。顺序是**先扣分拿到流水 id 再下单** ——
反过来的话扣分失败会留下一张白拿的订单；这个顺序下，下单失败能靠流水 id
把分退回去。下单失败时**分和库存都要退**，少退一样就是凭空蒸发。

退款走冲正而不是凭空加分 —— 凭空加的话积分总量会悄悄多出来，
而通胀体检看到「有人白拿了分」却查不出源头。

虚拟商品交付失败（比如称号名额刚好满了）**留一张待人工处理的订单，
而不是回滚整笔兑换** —— 回滚的话用户会以为「没买成」然后再点一次，
而分其实已经扣过了。

### 5.9 公告与推送 ✅（微信群发）
- [x] 站内公告（横幅/弹窗/站内信）
- [x] **微信群发**：内容冻结 + 双人复核 + 频率上限 + 额度检查 +
      逐群留痕 + 逐条撤回
- [ ] 定向到角色或群（站内公告目前是全体）
- [ ] 邮件群发（同样限流留痕）
- [ ] 每周精选推送的草稿审核界面

**这是全站唯一做错之后没法挽回的功能。** 删错帖可以恢复、扣错分可以冲正、
封错人可以解封，但一条发到一千六百人手机上响过的消息，
撤回窗口只有两分钟且不保证成功。

所以流程比别处都长：起草 → **冻结内容** → 另一个人复核 → 排队 → 逐群发送。
内容哈希在提交复核时冻结，复核和发送前各校验一次 ——
不冻结的话，「先提一版温和的骗到批准，再改成别的」这条路是敞开的，
而这类操作从审计日志里看不出来：两条记录都合法。

频率上限（每天 3 次、间隔半小时）不是流程繁琐：机器人已经因为高频操作
被微信风控过一次（加好友那次），而群发比加好友显眼得多。
发太勤的另一个后果是大家开始屏蔽这个群，那比被风控更难挽回。

**查不到上游额度就不发。**「查不到就当没限制」是最危险的默认值 ——
那正是撞上风控的姿势。额度不够时也提前拒绝，而不是发到一半被上游拒：
发了一半是最糟的状态，一部分人收到、一部分没有，而重发会让前一部分人看到两遍。

### 5.10 审计与系统 ✅
- [x] 审计日志检索（按动作/危险等级/时间）+ 变更前后快照，**无删除接口**
- [x] **系统设置**：写入侧校验、变更历史、回滚、恢复默认、改过的项标出来
- [x] **危险操作双人复核队列**：注册表式动作 + 24 小时有效期 + 执行前重校验
- [ ] 功能开关的后台界面（feature_flags 表已就位）

**写入侧必须校验。** 读取侧遇到非法值会退回代码默认值，听起来很稳，
实际上制造了最难查的一类 bug：**后台显示的和实际生效的不是一回事**。
把上限填成 6O（字母 O）会保存成功、页面显示 6O，而系统一直在用 60，
没有任何地方报错，直到几个月后有人问「我明明改过为什么没生效」。

**双人复核只能用注册表，不能用「存下来待会执行」。**
把要执行的操作序列化存起来、批准后回放，等于在数据库里开了一个
延迟执行的远程调用入口 —— 谁能往表里写一行，谁就能让系统
以「已被批准」的身份执行任意操作。所以只有代码里登记过的动作能被提出，
表里存的是「哪个动作 + 什么参数」，不是「执行什么代码」。

判断一件事该不该进复核队列，标准不是「重不重要」，
是**「改错之后多久才会有人察觉」** —— 越久越危险。
把每日积分上限设成 0，全站都拿不到分，而大家只会以为「今天没发分」。
- [ ] 长任务管理（存储裁剪、积分重算，先 preview 再执行）
- [ ] 后台路径可配 + 可选 IP 白名单

### 5.11 邀请体系 ✅
- [x] 邀请码生成（限次/限时/绑定身份类型），码里不含形近字符
- [x] 邀请树：追溯上游与下游，成环时不会转死
- [x] **邀请奖励延迟发放 + 被封时回滚**
- [ ] 外部用户注册入口（现阶段只有群成员能登录，通道预留但未开）

**邀请体系最容易变成刷分工具** —— 拉一个僵尸号的成本几乎为零。
所以这套规则的重点不是「怎么邀请」，是「怎么让刷邀请不划算」：

奖励在被邀请人**完成首次打卡**时才发，不是注册时。
打卡本身要求群里发言或论坛活跃达标 —— 也就是说，
只有真的参与了社区的人才会让邀请人拿到奖励。这条门槛是复用现成的反作弊。

被邀请人被封时**冲正奖励**（走 revertPoints，流水只增不改）。
不回滚的话「刷号被抓也不亏」，那等于在鼓励刷。
封了再解封也不能重领 —— 回滚过的不再补发。

**只奖励直接邀请，没有多级。** 多级奖励是传销的结构，
它会把社区变成拉人游戏，而拉来的人不是冲着社区来的。

一个人只能被邀请一次，否则注销重注册就能反复给同一个邀请人送奖励。

**测试**：每个写操作都要验证「三处齐全」（业务表 + 审计日志 + 通知）；
权限矩阵的先拒后允顺序；双人复核不能自批；审计日志无法删除。

已完成部分 **116 项**（5.1–5.4 举报与申诉）：权限反查与 `can()` 结论一致、过期授权不算数、
用户级拒绝能剔掉身份组给的权限、不能移除最后一个站长、不能改自己的状态、
大额调整门槛、后台整棵树未登录必须跳转、十人举报同一目标只占一行、
不能复核自己下的处罚、超时按最早那条算、标了 ready 的入口必须真的有页面。

---

## 迭代 6 — 可插拔活动系统 ⬜

- [ ] 模块运行时（manifest、SDK、故障隔离、熔断）
- [ ] 通用活动骨架 + 状态机 + 资格引擎
- [ ] 名额并发与超卖防护
- [ ] **域名注册活动**（第一个模块实例）
- [ ] 临时邮箱模块
- [ ] 每周精选回推模块

**测试**：并发抢名额不超卖、资格快照冻结、
状态机非法流转被拒、模块崩溃不影响主站。

---

## 迭代 7 — 打磨 ⬜

- [x] 成员目录 + 技能标签
      - **只收录注册用户**：群成员表里有一千八百人，目录里只有二十几个。
        出现在群成员表里是微信群的事实，出现在可按技能检索的网页上
        是这个站替他做的公开 —— 他没同意过
      - **只显示和你同群的人**，且只说共同群的**数量**不说是哪个 ——
        说了等于把群名泄露给另一个群的人
      - 标签存 slug + label 两份：RAG/rag/ＲＡＧ/「大 模 型」归一成同一个。
        碎掉的标签目录等于没有目录 —— 每项一个人，谁也找不到谁
      - 显示用**最多人用的那个写法**，不是第一个人的写法
      - 只有一个人的标签不进筛选栏 —— 点进去看到一个人，是噪音
      - 隐身开关：隐身后**自己那一行还在**（标「仅自己可见」），
        否则用户没有任何办法确认开关生效，只能靠相信
      - 页面直说收录了几个、几个隐身、几个没填标签 ——
        一个说得出自己有多空的目录，比看起来很满的有用
      - wx_id 不进返回结构（只传配色下标）—— 它会被序列化进网页源码
      - 顺手修掉：`/members` `/shop` `/search` 在导航里标了 requiresAuth
        却不在中间件名单里，访客拿到的是 200 空壳。加了一条断言把
        「导航要求登录」和「中间件拦下」绑在一起
- [x] 链接资源库
      - 从聊天记录自动收录。**URL 抽取必须在第一个 CJK 字符处停下** ——
        真实数据里 `https://cloud.siliconflow.cn/i/Cn0CsuOt现在硅基流动注册…`
        用 `\S+` 抓到的是一个看起来完全正常、点进去 404 的地址
      - 去重键抹掉 www./协议/末尾斜杠/追踪参数（utm_*、spm、from、share_* 等），
        展示地址保留原样 —— 同一个东西出现五次是资源库最没用的样子
      - 说明取自**分享它的那条消息**，不去抓网页标题：
        「可以查实时台风情报」比 `<title>台风网</title>` 有用，
        而且不用发出站请求、不用防 SSRF。抓不到就留空，不编
      - 可见性同成员目录：只看得到自己群里分享过的，不显示来自哪个群，
        **连「被分享 N 次」都只数你看得到的那些次**（全站次数会泄露别处的热度）
      - 收藏前判一次可见性 —— 收藏不能成为绕过可见性的后门
      - 幂等：回填反复跑，计数从明细现算不做 +1；`--audit` 对账，`--retitle` 重算标题
      - 生产回填：42k 条消息 → 198 条链接、117 个域名；二次运行 0 新增、对账通过
- [x] 关键词雷达
      - **只在订阅者自己所在的群里匹配** —— 否则这不是雷达，
        是一个能监听任意群的工具。可见性判定放在匹配**之前**：
        反过来写也能得到同样结果，但那样漏一次过滤就是一次静默泄露
      - 中英文词边界分开处理：ASCII 卡词边界（订阅「AI」不被 said/chain 命中），
        CJK 走子串（否则中文一条都命中不了）。同一个规则套两种语言必有一种是坏的
      - 每个词每天最多提醒 5 次、十分钟内不重复；**跨天重置**
        （不重置的话一次热闹会让订阅永远失效）。被压掉的命中照样留痕并
        在列表上标「未通知」—— 少通知是有意的，瞒着不说不是
      - 订阅前先算「过去七天在你的群里响多少次」，太泛的默认拦下。
        阈值拿生产数据校准过：AI 343 次/7d 拦下，claude 67、Agent 91 放行
      - 「一天」走 `dateKey`（东八区），和签到连胜同一个边界 ——
        原本用服务器本地时区，在 PDT 的机器上会差整整一天
      - 幂等：同一条消息重扫不重复计数（唯一索引 + 累计数不 +1 两次）
- [x] 通知中心（补齐：偏好开关 + 筛选页签）
      - `notification_prefs` 这张表之前**一行代码都没读过** —— 表建好了、
        字段设计好了，而 notify() 根本不看它。现在真的接上了
      - 开关在**写入侧**生效：关掉的类型不产生那一行，而不是产生了再在
        读的时候过滤 —— 后者要让未读数、红点、聚合计数全都记住
        「这条对这个人不算数」，漏一处就是个点不掉的红点
      - **处罚与系统公告关不掉**：让人能把对自己不利的消息静音，
        等于让所有没收到的消息都不再说明什么。前端禁用 + 服务端再判一次
      - 筛选在 SQL 里做，不是取一页回来再过滤（否则「@ 我」会因为
        最近 50 条里没有 @ 而显示空）；页签上带条数
      - 邮件通道没接，所以界面上**不放**邮件开关 ——
        一个打开了却什么都不会发生的开关比没有它更糟
- [x] 存储分层裁剪任务
      - 三步按「删错了能不能救回来」排序：改层（零风险）→ 退索引（可逆，
        `reindexRange` 真的能恢复）→ 丢正文（**不可逆**）
      - **「本地是缓存、需要时回源」这个前提被验证了，结论是今天证明不了**：
        上游自己也只有约两个月历史。所以默认走归档（gzip NDJSON，按月分包），
        关掉归档时 `verifyUpstreamRetention()` 会抽样回查，验不过整步跳过
      - 自动触发只做可逆的两步 —— 永久删聊天记录该有个人按确认，
        而不是凌晨三点由 cron 悄悄完成；自动跑过也落 admin_tasks + 审计
      - 预览与执行之间隔一个 admin_tasks 行：确认的是**看到的那个数字**，
        不是执行时重新算出来的另一个
      - 按天回看/群聊沉淀会报出「这天有 N 条正文已归档」——
        裁剪过的一天和冷清的一天在结果里长得一模一样
- [x] 异地备份
      - 自己实现 SigV4（服务器上没有 aws-cli / rclone），正确性用 AWS 官方
        `get-vanilla` 测试向量锁住 —— 签名算错的表现是 403，
        而 403 很容易被读成「密钥填错了」
      - 兼容任何 S3 协议的对象存储：R2 / B2 / MinIO
      - **传完立刻读回来对哈希** —— PUT 返回 200 只证明请求没报错，
        对象被截断、写进别的桶、被静默丢弃都不会让上传这一步失败
      - **恢复演练**：从远端下载最新那份、解压、打开、数行。
        本地那份能打开完全不能说明传上去的能打开；
        「能打开但是空的」同样会通过完整性检查，所以还要看行数
      - 备份脚本备完就推异地 —— 拆成两个定时任务的话，
        第二个挂掉之后第一个还在每天报成功
      - **没配置报 degraded 而不是 ok**：不该因为「本来就没开」显示成正常。
        告警线定成 24 小时报一次、30 天重提醒 —— 这是「一直缺着」型的问题，
        按分钟级去报只会让人把整个通道静音
      - 归档文件也一起传：它是冷层正文的**唯一**副本，
        比数据库备份更不能只放在一块磁盘上
      - ⚠️ **生产上还没配对象存储**，见 STATUS.md；配好四个环境变量即可生效
- [x] 告警通道（隧道断了要有人知道）
      - 抑制优先：连续挂够久才报、一次故障只报一次、恢复要收尾
      - `frp_tunnel` + `upstream_api` 合成一个告警组件 —— 隧道一断，
        `upstream_api` 那一行会永远停在最后一次「正常」上
      - **上游挂了微信发不出去**（报信的和出事的是同一个）：
        告警照样落库，`notify_error` 如实记下发不出去的原因
      - 投递失败按 `retry_after` 重试，不用重提醒的节奏等 ——
        一次失败换来一小时沉默，而沉默和「一切正常」长得一样
      - 后台 `/admin/health`：先答「有人会知道吗」，再答「现在好不好」
- [x] 无障碍走查 + 触控区
      - **做成可执行的规则跟着 `npm test` 跑**，不是一次性走查 ——
        一次性走查会衰减：这周改干净了，下周新加一个图标按钮又没有
        aria-label，而没有人会再走查第二遍
      - 七条规则：图标按钮/链接缺名字、img 缺 alt、role=switch 缺
        aria-checked、输入框缺名字、_blank 缺 noopener、装饰图标缺
        aria-hidden、图标按钮触控区不足 44px
      - 扫描器的取舍是**宁可漏报不可误报**：会误报的检查第一次挡住
        正常提交时就会被加豁免注释，第二次就会被删掉。
        第一版报了 53 条，其中 47 条是误报（`{tab.label}` 这种运行时
        才确定的名字、`<Field label>` 包裹的输入框），收窄之后 6 条全是真的
      - `.tap-target` 用伪元素把可点范围撑到 44×44，**视觉尺寸不变** ——
        改 padding 会把排版顶开，于是这件事永远排不上优先级；
        指针设备上关掉，鼠标本来就点得准
      - 走查结果：全站 0 条未修复；焦点可见、减少动效、sr-only、
        跳到正文链接（`#main` 锚点真的存在）都有测试盯着
- [x] 性能守卫
      - **N+1 是唯一一种「不会被发现」的 bug**：不报错、不崩、测试全绿，
        二十条数据看不出来，两千条开始转圈，而没有哪一刻能指着说是这次
        改动带来的。所以给每个列表查询定查询条数预算，数据翻十倍再跑一遍，
        **条数不许涨**。故意造了一次 N+1 验证过这个守卫真的会红
      - `EXPLAIN QUERY PLAN` 断言 11 条热点查询不在大表上全表扫描。
        当场查出**排行榜在 daily_stats 上是 SCAN**（访问量最大的一页），
        补了 `(conv_id, date)` 索引，生产上确认 SCAN → SEARCH
      - deploy 加首屏 JS 预算，量的是**压缩后**的字节 ——
        同一批 chunk 未压缩 362 KB、压缩后 109 KB，
        按前者定预算等于按一个没有人经历过的数字做决定。当前 109/160 KB

---

### 已补齐的遗留项

- [x] 赛季积分
      - **赛季只重置排名，不重置积分**。表里没有任何积分列 ——
        有的话就是又一个会和 points_ledger 对不上的冗余计数器；
        赛季榜是按日期范围现算 daily_stats
      - 排行榜**默认就是本赛季**：不是默认那一屏的话，这件事不成立
      - 边界按东八区、按季度切，四个赛季首尾相接不留缝
      - 结算冻结名次 —— daily_stats 会被存储裁剪动到，
        而「2026 秋季赛冠军是谁」一旦发生就不该再变
      - 只发前三，且要过最低参与门槛：没人参与的赛季发称号只会让称号贬值
      - 榜上但没注册过的人：名次照样冻结，称号发不了（他确实是第一名，
        只是站上还没有能给他挂东西的地方）
      - 连任顺延到期日而不是插新记录；并列第一时两个人都是冠军
      - **两条测试直接读源码**，确认赛季规则与结算里不出现
        `grantPoints` / `pointsLedger`

- [x] 密码兜底登录
      - **它是兜底不是第二条正门**：只有已经登录的人才能设置密码。
        这个站的入口只有微信群里那条验证码 —— 密码要是能独立进门，
        「只有群成员能登录」当场就没了
      - 解决的是一个具体问题：Passkey 换设备就用不了，
        而验证码要靠群猫娘发得出来 —— 两条同时不通时，
        密码是唯一还能进来的钥匙
      - **不能创建账号，也不能激活 pending 账号**
      - 「没有这个人」「没设过密码」「密码不对」措辞完全一致，
        **而且耗时一致**（查不到人时也照样算一次 scrypt）——
        否则就送了一个查询群成员名单的接口
      - 账号状态检查放在密码校验**之后**：放前面等于告诉任何人
        某个微信号在这个社群里且被封过
      - 账号级锁定（5 次 / 15 分钟），IP 限流挡不住换 IP 爆破同一个人；
        锁定**有时限** —— 永久锁定等于谁都能把别人锁死
      - 用 Node 标准库的 scrypt 而不是第三方 argon2：
        认证是最不该依赖三方包的地方。参数随哈希一起存，
        登录成功时顺手升级老哈希

- [x] 置顶位实际生效逻辑
      - `deliver()` 里**没有 highlight 分支**，落到最后一行的
        「无需自动交付」；而 highlight 属于即时交付，订单会直接标成
        fulfilled、提示「已到账」—— 五百分扣了，帖子一动没动。
        （商品一直是下架状态，所以从没有人真的被扣过分）
      - `pinned_until` 这一列**从来没有人读**：排序只看 `pinned` 布尔，
        一次「置顶一天」等于置顶到天荒地老，而且看不出来
      - 现在排序、列表标记、后台清理统一走 `isEffectivelyPinned`
      - **付费置顶有名额**（每版块 1 个）：不限的话第一屏迟早全是买来的，
        那时候论坛卖掉的不是曝光，是别人被埋掉的机会
      - 目标校验全在**扣分之前**：帖子不存在 / 已删 / 不是自己的 /
        名额被占，一律当场拒绝，不收钱排队
      - 只能给自己的帖子买 —— 否则就是花钱把别人的帖子顶上去
      - 到期清理任务把标记清掉并通知买的人：花五百分买的东西
        悄无声息地消失，下次他不会再买，而且会怀疑上次到底有没有生效

- [x] 成就型称号自动授予 + 租用型称号到期结算
      - `meetsCondition` / `achievementStats` / `renewalExpiry` / `isTitleExpired`
        四个函数写好了、有测试、看起来很完整 —— 而**没有任何地方调用它们**。
        builtin 里那五个成就称号谁也拿不到，而称号架的空状态还写着
        「连续打卡、在论坛发帖和回复都会解锁」
      - 结算挂在健康探测那一轮里（5 分钟），不另开 timer ——
        又一个 timer 就是又一个可能悄悄挂掉的东西
      - **自动续费默认关，购买流程也不替人打开**：默认开着的自动续费
        会在某人早就不用它的时候每月悄悄扣三百分，而积分是这个站里
        唯一的硬通货
      - 分不够就让它过期，**不扣成负数** —— 那等于让人背上一笔
        没同意过的债，而下次签到的分会先去还债，他会以为签到没生效
      - 到期前 3 天提醒一次（只一次）；到期摘下但**不删记录**，
        「我曾经拿到过」也是履历
      - 生产上跑过：5 个发过帖的人自动拿到「开了个头」并收到通知，
        再跑一轮 0 新增

## 贯穿始终的约束

1. **每个迭代结束必须跑通全部测试**，不留欠账
2. 代码里零魔法数字 —— 一切阈值进 `settings`
3. 每个后台写操作三处齐全：业务表 + `audit_logs` + 通知
4. 权限判定只走 `can()`，不写 `if (role === ...)`
5. 故障不能伪装成业务结果（「你不是社群成员」那次教训）
6. 上游返回的形态必须实测，不照文档推测
7. **群列表与群数据属于隐私**：访客一个群都看不到，成员只看自己所在的群。
   任何按群聚合的查询都必须先过 `visibleGroupIds()`；漏传的后果是空结果，
   不是全量结果 —— 这是靠必填参数在类型层面强制的
8. 同一段逻辑不写两遍。导航可见性、桶键构造这类判定都提取成共用函数，
   测试调用生产代码的那一份，不自己重写。
   **判定藏在拿不到的地方（server action、middleware）时，把它抽出来** ——
   `/admin` 漏进门禁那次，测试里抄了一份路径规则，所以照样全绿

---

## 站长 2026-08-08 交办清单

一次性提的一大批，记在这里以免漏掉。**做完一条划掉一条，不要口头确认了事。**

### 已完成并上线
- [x] **后台分页** —— 统一分页组件（零客户端 JS）+ 纯逻辑分页器。普查出 16 处
      静默截断,其中 3 处是真 bug:商店副标题拿截断后的 `rows.length` 冒充订单总数、
      邀请页「已邀请 N 人」同样、滥用告警只统计前 50 条。所有排序补了 id 末位键
      成全序（并列多时半序翻页会丢行/重复行）
- [x] **域名活动批量注册与回填** —— 导出一行一个可直接粘进注册商；回填解析器
      宽容（全角逗号/URL 包裹/大小写/各种分隔符）但**三处不猜**：认不出的状态词
      报错而非默认成功、同一域名两行矛盾时两行都不执行、「系统里没这个申请」单独红色列出
- [x] **帖子页行内操作** —— 用户改自己的、版主/管理员快捷操作（加精/置顶/锁帖/
      移动版块/删除/恢复），统一操作菜单，移动端底部弹层。顺带接上了四处
      「写了但全站没人调用」的代码：`moderatePost`（318 行版主工具）、
      `deleteMyPost`/`restoreMyPost`、`editPost`（连编辑页都不存在）
- [x] **群消息的 @提及** —— 写时定人、读时定名（昵称会变，所以存 wx_id 不存昵称）。
      3,332 个提及认出 2,622（79%），歧义 15 标出候选、认不出 600 标 unknown，**绝不猜**
- [x] **移除管理端限速与强制复核** —— 好友申请额度改成纯提示、危险配置改成红色警告、
      复核队列降级为可选留痕。**没松的三条**：动作注册表（防任意代码执行）、
      24 小时过期（防执行旧 payload）、可见性提升要原作者同意（保护群里说话的人）
- [x] **账号可以没有密码** —— 新增「决定不设」状态，和「还没设」区分开
- [x] **登录态 5 分钟恢复** —— 断线回来接回原来的码；已匹配的码按匹配时间另开 5 分钟窗
- [x] **LLM 接入骨架** —— 对话（DeepSeek）与嵌入（自建 Qwen3-Embedding-0.6B）
      分两套配置，实测 DeepSeek 没有 embeddings 接口

### 正在做
- [x] **资源库 AI 整理** —— 200 条链接里 58 条标题就是域名。把链接连同上下文交给模型
      生成标题简介，**但不许编**：看不出来就留空
- [x] **语义聊天记录搜索** —— 按会话窗口嵌入而不是按单条消息（数过：一半的群消息
      不超过 8 个字，「哈哈」「好的」单独嵌入没有可检索性）。3,394 段全部嵌完，
      门槛 0.55 是量出来的：噪音最高 52%、信号最低 57%
- [x] **后台 LLM 设置项与可用性检测**（/admin/llm，每次打开真的各调一次）
- [x] **资源可以点赞** —— 和收藏分开（私人书签 vs 公开信号），计数从明细重算

### 还没开始
- [ ] **群消息的回复关系** —— ⚠ **卡在上游**：NekoBot 的 `/v1/messages` 不透传引用目标，
      探测过隐藏端点/扩展参数/admin API 都没有出口。列和解析器已就位，
      需要 NekoBot 侧透传 `<refermsg><svrid>` 后重跑回填即生效
- [x] **emoji 换成 SVG** —— 分清了三类：内容里的表情反应（不动）、正文里的 `→`（是标点）、
      当图标用的（换掉）。权限矩阵那三格原本就是 lucide，是我改成编辑器时换成 ✓✗− 的
- [x] **UI 布局重构** —— 逐页普查出 13 类割裂，按出现次数修。卡片配方手拼 36 处、
      Pill 行 11 处、提示横幅 15 处，全部收敛到基础构件。密集页面放宽到 78rem。
      顺带修掉 `var(--hairline)` 这个**从来没定义过**的变量（35 处引用）
- [x] **手机端够得着** —— 之前底部 tab 栏 5 格全是目的地，剩下 7 个板块
      （通知、资源库、活动、成员、雷达、商店、**整个后台**）在手机上
      **根本没有入口**。第 5 格改成「更多」，里面用减法列出所有不在 tab 栏的 ——
      新页面自动进得去，不需要有人记得改第二份清单。后台那 24 个入口
      在手机上收进一行选择器（原来整份目录堆在每个页面的正文上面）
- [x] **分享（两端都有）** —— 转发链接、分享文案、帖子转发图、聊天记录图。
      链接靠权限收口所以文案可以随便转；**图片跑出去就收不回来**，
      所以草稿/已删除/私密一律不生成，群聊图上**永远不出现群名**
      （路由根本不 import groups 表），并且生成即记审计。
      剪贴板在微信 webview 里可能不可用 —— 失败时摊开文本让人长按全选，
      不装作成功
- [x] **「整理成帖子」页加关键词搜索** —— 复用检索页那套 FTS（分词、转义都在
      `lib/db/fts.ts` 里踩过坑了）。原来只能选群 + 选日期，而人记得的是内容不是日期
- [x] **投票能建了** —— `createPoll` 原来一个调用点都没有。发帖时顺带建，
      和帖子在同一个事务里，不会留下「类型是投票但没有投票」的半成品
- [x] **bug 反馈** —— 新增 `feedback` 版块（公开可见、登录可发）。不做工单系统:
      版块比工单好的地方恰恰在于**公开** —— 别人报过的你看得见，不会再报一遍
- [x] **申请加入社群** —— `/join`，全站唯一未登录可写的入口。
      **对所有情况只回一句一模一样的话**:任何「你已经是成员了」「你已经申请过了」
      都在回答陌生人不该能问的问题，一个个试就能把成员名单摸出来。
      判断（在不在群里、有没有账号）全部留在管理员那一侧。按 IP 限流
- [x] **即时推送通知** —— 站内 SSE（服务端轮询数据库，因为通知不只有 Web 进程在写）
      + Web Push（RFC 8291 用 node:crypto 自实现，官方测试向量锁死）。
      断线补漏两级：连接被掐用 Last-Event-ID，整页被杀用 localStorage 游标
- [ ] ⚠ **等站长拍板**：群发公告仍是双人复核 + 每天 3 次 + 30 分钟间隔。
      它是全站唯一不可逆、直达 1600 人的动作，和后台那些「点错了能改回来」的
      操作不是一类 —— 要不要一并放开，你说了算
- [x] `/archive` 与 `/forum/convert` 的「翻天」合成一个 `DayNav`。
      顺带补上**直接跳日期** —— 原来两边都只能 ±1 天，回到上个月要点三十下

### 登录名与手机号（已完成）

- [x] 自定义登录名（`users.username`，唯一、存小写、保留词、挡抢微信 ID）
- [x] 手机号当登录名（`users.phone`，**未验证**：不能找回账号、不显示、不可搜）
- [x] 邮箱登录（`users.email` 早就在库里，一直没接）
- [x] 登录第一个框改收四种标识，撞车时微信 ID 优先
- [ ] 短信验证通道（有了才能写 `phone_verified_at`，也才谈得上手机号找回）
