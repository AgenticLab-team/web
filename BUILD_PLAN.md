# 施工计划

按迭代排序。每完成一项都要**写测试并跑通**，不留「先跑起来回头再补」的账。

进度：✅ 完成 · 🔨 进行中 · ⬜ 未开始

---

## 已完成

| # | 内容 | 测试 |
|---|---|---|
| ✅ | 上游 API 客户端（类型按实测定义、按端点钳制 limit、重试与降级） | — |
| ✅ | 消息镜像 + FTS5 中文检索（CJK 逐字切分） | 6 |
| ✅ | 高质量消息判定（穷举校准，与上游 10/10 吻合） | 4 |
| ✅ | 三通道绑定登录 + 会话 + 限流 | — |
| ✅ | RBAC 底座：65 权限点 / 9 身份组 / `can()` 判定器 | — |
| ✅ | people 表：1803 人的显示名与头像 | — |
| ✅ | 工程基建：git / 备份 / 健康探测 / 审计入口 | — |
| ✅ | 应用外壳：移动 Tab Bar + 桌面侧边栏 + Large Title | 10 |
| ✅ | 首页 / 排行 / 我的 三个页面 | — |
| ✅ | 配色切换（自动 / 浅色 / 深色） | 4 |
| ✅ | 每日统计落库（数据库往返测试） | 6 |

共 47 个测试通过。

---

## 迭代 1 — Passkey 与登录体系 🔨

现在每次登录都要回微信取验证码，这是当前最影响体验的缺口。

- [ ] WebAuthn 注册：绑定成功后引导设置 Passkey
- [ ] WebAuthn 登录：一键进入，支持多设备
- [ ] 密码兜底（Argon2id）+ 强度校验
- [ ] 设备与会话管理页：查看、命名、远程下线
- [ ] 登录历史（时间/IP/方式），用户自己可见
- [ ] 管理员强制 Passkey 或 2FA（配置项已在库里）

**测试**：凭证哈希与校验、计数器防克隆、会话过期与撤销、
限流窗口、封禁用户立即失效。

---

## 迭代 2 — 积分 · 打卡 · 等级 ⬜

- [ ] 打卡（当日高质量 ≥ N 才可打）
- [ ] 积分规则引擎（全部数值读 settings，代码里零魔法数字）
- [ ] 连胜与补签卡
- [ ] 等级与解锁权限
- [ ] `points_ledger` 冲正机制（绝不改原记录）
- [ ] 反作弊：同分钟折叠、近似内容去重、异常进风控队列
- [ ] 服务端定时结算，前端不可触发

**测试**：规则引擎各分支、封顶、连胜断签与补签、
冲正后余额一致、重算与流水求和比对。

---

## 迭代 3 — 群消息检索 ⬜

后端 FTS 已就绪，缺界面与权限收口。

- [ ] 搜索页：跨群 / 按人 / 按时间 / 按类型
- [ ] **权限收口**：只能搜自己所在的群，服务端强制校验
- [ ] 结果高亮与上下文跳转
- [ ] 时间机器：回看任意一天
- [ ] 我的存档：只搜自己说过的话

**测试**：权限隔离（换个用户搜同一个群必须为空）、
分词边界、翻页、注入字符不炸。

---

## 迭代 4 — 后台管理（全量）⬜

**这是整个项目里体量最大的一块。** 表结构已经全部就位，
现在要把 11 个模块逐个做出来。每个模块都必须落盘到位 ——
验收标准见 `SCHEMA.md` 第十一节那张对照表。

### 4.1 总览仪表盘
- [ ] DAU/WAU/MAU、新增绑定、消息量与高质量占比
- [ ] 积分发放与消耗、论坛与活动指标
- [ ] **系统健康**：frp 隧道状态、API 配额、同步成功率、失败重试队列
- [ ] 磁盘水位趋势图

### 4.2 用户管理
- [ ] 搜索与多条件筛选、导出
- [ ] 用户详情：档案 / 所在群 / 积分流水 / 登录日志 / 设备与 Passkey /
      论坛行为 / 举报记录 / 管理操作历史
- [ ] 操作：改角色、调整积分（必填理由）、改等级、封禁解封（可设期限）、
      强制下线、解绑重绑、合并账号、重置凭证
- [ ] **绑定审批队列**：好友申请列表 + 申请人活跃度，一键通过或拒绝
- [ ] 管理员备注（用户不可见）

### 4.3 身份组与权限
- [ ] **权限矩阵**：行=身份组 × 列=权限点，三态切换、框选批量
- [ ] 保存前 diff 预览（「将获得 3 项、失去 1 项，影响 4 人」）
- [ ] **以某身份预览**：整站切成他的视角，常驻红色横幅
- [ ] **权限反查**：选一个权限点，列出所有持有者及来源
- [ ] 自定义身份组（颜色/图标/优先级/名额上限/自动授予规则）
- [ ] 范围化授权与临时提权（到期自动回收）
- [ ] 权限矩阵版本快照与一键回滚

### 4.4 内容审核
- [ ] 举报队列（按严重度排序）、分派、处理
- [ ] 帖子/回复管理、批量操作
- [ ] 敏感词库与自动拦截规则
- [ ] 处罚记录聚合到用户档案
- [ ] **申诉流程** —— 有处罚就必须有申诉，否则只会积累怨气

### 4.5 积分与等级配置
- [ ] 所有数值后台可改，改动进 `setting_history` 可回滚
- [ ] 等级门槛与解锁权限
- [ ] 手动发放/扣除（必填理由，大额走双人复核）
- [ ] 积分流水全站视图、风控队列
- [ ] 榜单重算 / 补发工具（先出 preview 再执行）

### 4.6 群与数据源
- [ ] 群列表、bound 状态、每群配置（quality_min 覆盖、是否计分、榜单是否公开）
- [ ] 手动触发同步、同步日志与失败重试
- [ ] API key 状态与配额

### 4.7 活动与模块
- [ ] 模块列表：启停开关、健康度、事件日志、熔断恢复
- [ ] 安装时展示 grants 权限清单要求确认
- [ ] 配置表单由 configSchema 自动生成
- [ ] 活动管理：创建、资格规则可视化编辑（实时显示符合人数）、
      申请审批、批量导出导入、名额流水

### 4.8 商店与订单
- [ ] 商品上下架、库存、兑换记录
- [ ] 实物订单发货状态流转（每次流转留痕）
- [ ] 退款与积分回滚

### 4.9 公告与推送
- [ ] 站内公告（横幅/弹窗/站内信，可定向到角色或群）
- [ ] **微信群发**：二次确认 + 预览快照 + 双人复核 + 频率上限 + 全量留痕
- [ ] 邮件群发（同样限流留痕）
- [ ] 每周精选推送的草稿审核界面

### 4.10 审计与系统
- [ ] 审计日志检索（按人/类型/时间），**无删除接口**
- [ ] 危险操作双人复核队列
- [ ] 系统设置与功能开关
- [ ] 长任务管理（存储裁剪、积分重算，先 preview 再执行）
- [ ] 后台路径可配 + 可选 IP 白名单

### 4.11 邀请体系
- [ ] 邀请码生成（限次/限时/绑定角色）
- [ ] 邀请树可视化，追溯上游与下游
- [ ] 邀请奖励与被封时的回滚

**测试**：每个写操作都要验证「三处齐全」（业务表 + 审计日志 + 通知）；
权限矩阵的先拒后允顺序；双人复核不能自批；审计日志无法删除。

---

## 迭代 5 — 论坛 ⬜

- [ ] 版块、发帖、回复、点赞、收藏、@提及
- [ ] Markdown + 代码高亮 + 图片上传
- [ ] 加精/置顶/锁定/移动
- [ ] **群聊一键成帖** + 原作者同意流程
- [ ] **可见性分层**：未登录可看 public，群聊转帖锁死在原群范围
- [ ] 举报入口

**测试**：可见性判定矩阵（6 个级别 × 4 类身份）、
群聊转帖不可越权提升、未登录访问私有帖返回 404 而非 403（不泄露存在性）。

---

## 迭代 6 — 可插拔活动系统 ⬜

- [ ] 模块运行时（manifest、SDK、故障隔离、熔断）
- [ ] 通用活动骨架 + 状态机 + 资格引擎
- [ ] 名额并发与超卖防护
- [ ] **域名注册活动**（第一个模块实例）
- [ ] 临时邮箱模块
- [ ] 每周精选回推模块

**测试**：并发抢名额不超卖、资格快照冻结、
状态机非法流转被拒、模块崩溃不影响主站。

---

## 迭代 7 — 打磨 ⬜

- [ ] 成员目录 + 技能标签
- [ ] 链接资源库
- [ ] 关键词雷达
- [ ] 通知中心
- [ ] 存储分层裁剪任务
- [ ] 异地备份
- [ ] 告警通道（隧道断了要有人知道）
- [ ] 无障碍走查、性能、移动端手势

---

## 贯穿始终的约束

1. **每个迭代结束必须跑通全部测试**，不留欠账
2. 代码里零魔法数字 —— 一切阈值进 `settings`
3. 每个后台写操作三处齐全：业务表 + `audit_logs` + 通知
4. 权限判定只走 `can()`，不写 `if (role === ...)`
5. 故障不能伪装成业务结果（「你不是社群成员」那次教训）
6. 上游返回的形态必须实测，不照文档推测
