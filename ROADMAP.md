# 还没做的

> **读者**：站长和执行者
> **性质**：**打算**
> **最后核对**：2026-08-10，六组并行对着代码逐条核过
>
> **全站只有这一份和 `DONE.md` 可以打勾。** 别处一律只写「为什么这么设计」，
> 不写做没做 —— 同一件事在两个地方各打一次勾，是上一版最主要的错误来源
> （核出 13 条标着「待办」其实早就上线了，3 处同一件事两个位置状态相反）。

里程碑只留一套。原来的「迭代 1–7」和 PLAN 里的「M0–M8」是两套互不对应的
编号，谁也说不清 M5 和迭代 5 是不是一回事 —— 现在按**主题**分，做完一条划一条。

---

## 一、内容与互动

- [ ] **群消息的回复关系** —— ⚠ **卡在上游**。NekoBot 的 `/v1/messages`
      不透传引用目标，探测过隐藏端点 / 扩展参数 / admin API 都没有出口。
      列和解析器已就位，NekoBot 侧透传 `<refermsg><svrid>` 后重跑回填即生效。
      在那之前「被回复」恒为 0，这是如实的，不是 bug
- [x] **个人主页**：他发过的帖（GitHub 联动那半早就做了）——
      按作者筛的列表**永远排除匿名帖**，这一条写在查询层且没有例外
- [x] **标签体系接上了**：发帖能打标签、帖子上显示、点得动、
      `?tag=` 不再被静默丢弃、`require_tags` 终于有人读
- [x] **匿名发帖与回复接上了**：发帖框和回复框有开关（只在允许的版块出现）、
      后台能给版块打开、`createReply` 补上了和发帖一致的校验、
      顺带堵上「匿名帖的头像配色仍然是作者 id 的哈希」这个漏点
- [x] **短链接上了**：`/p/<code>` → 帖子。`share_code` 一直在生成而没人读，
      分享出去的是 26 位 ULID；顺带修掉那个会产出短码的生成方式，
      以及群聊转帖那条路根本不生成码
- [ ] **全站置顶**（`pinned_globally` 零引用）—— 值得先想清楚要不要做：
      站内公告（定向、可关闭、会过期）在「所有人都得看见」这件事上
      比一条置顶帖做得好，这一列可能该删而不是接

## 二、信息架构（站长连提四条，是同一件事）

- [ ] 侧边栏电脑端收拢，不常用的进「更多」
- [ ] 「我关注的」合并进「我的」
- [ ] 社区除成员之外的部分整合
- [ ] 成员模块不能只是一个列表

## 三、账号与权限

- [ ] 用户管理还缺：**导出、改等级、解绑重绑、合并账号、重置凭证**
      （封禁期限已完成，别再跟着这一行一起算作待办）
- [ ] **短信验证通道** —— 有了才能写 `phone_verified_at`，
      也才谈得上手机号找回
- [ ] **外部用户注册入口** —— 现阶段只有群成员能登录，通道预留但未开
- [ ] 后台路径可配 + 可选 IP 白名单
- [x] **权限表里 18 个零调用点的权限点**：两个接上了（功能开关页和存储页 ——
      管理员原来根本打不开开关页），其余如实标成 `planned` 并在权限矩阵上
      显示「未生效」。一条测试盯着这个标记不许变成谎话
- [ ] 那 16 个 `planned` 里，**8 个是「功能做了但由更粗的权限点在管」** ——
      也就是细粒度授权办不到。要不要逐个收细，看需要

## 四、运营工具

- [ ] **邮件群发**（同样限流留痕）
- [ ] 公告**定向到群**（定向到角色已完成）
- [ ] ⚠ **等站长拍板**：群发公告仍是双人复核 + 每天 3 次 + 30 分钟间隔。
      它是全站唯一不可逆、直达 1600 人的动作，和后台那些「点错了能改回来」
      的操作不是一类 —— 要不要一并放开，你说了算
- [ ] 管理员看到的是完整榜单，但**界面上还没把「别人看不到的那几行」标出来** ——
      不标的话他会照着一个只有自己看得到的名次去发公告、发奖

## 五、经济

- [ ] 价格用「≈ 几天的日常收入」表述，让价格随发行速度自动可比

## 六、模块

- [ ] **临时邮箱模块** —— 唯一一个真没做的模块（开关是占位的）
- [ ] **媒体缓存分层** —— PLAN 里承诺过，从未实现，
      留下 `storage.media_cache_max_bytes`、`storage.thumb_max_edge`
      两个零读取设置项和两个恒为 0 的列。要么做，要么删 ——
      不能继续挂在设置页上显示成「开着」

## 七、工程与运维

- [ ] **蓝绿部署还没装到线上**（脚本和测试都就位了，安装脚本没跑成）。
      在那之前每次部署仍有两段 502
- [ ] **一条静态检查：schema 列 / 设置项 / 权限点若零读取点，测试就红**。
      这是这个项目的地方病，现在全靠人肉发现
- [ ] ⚠ **等站长处理**：git 历史里有旧公网 IP（从根 commit 就在，
      约 122 个快照）。现在还没有任何 remote，是清理成本最低的时刻
- [ ] ⚠ **等站长定 LICENSE**。建议 AGPL-3.0 —— 这是一个自己在运营的
      托管服务，别人拿去改完做成闭源服务应该要回馈代码

## 八、文档（本轮正在做）

- [x] 补上 `ARCHITECTURE.md`（最大的缺口：没有任何一份讲代码怎么组织）
- [x] 补上 `LESSONS.md`（踩坑记录 + 死开关的识别方法）
- [x] `BUILD_PLAN.md` 拆成 `ROADMAP.md` + `DONE.md`，状态只在一处打勾
- [x] `PLAN.md` 归档到 `docs/archive/`（它是开工前的设计稿，今天没有人该读它了解现状）
- [x] `STATUS.md` 删（时间戳冻在 8-08）—— 第七节那张踩坑表已抢救进 `LESSONS.md`
- [ ] `SCHEMA.md` 的逐表字段清单删掉 —— 那是 schema 源码的活，
      抄一份必然烂（现在 `invites` 已经和真实表名撞车）
- [ ] `MODULES.md` 第二节（module.json / SDK / 五张 module_* 表）
      标注「**未采纳**：实际走 `src/lib/modules/registry.ts` 的写死清单」
- [ ] `PERMISSIONS.md` 三处要改：判定链顺序和代码写反了、
      指向死表 `role_snapshots`、缺 `visibility_requests` 审批链路
- [ ] `DEPLOY.md` 加一节 RUNBOOK：怎么回滚、迁移毁数据怎么从备份恢复、
      告警响了看哪

## 九、GitHub 生态深接入（站长 8-10：「我希望咱们生态可以高度接入」）

已经有的那一层是**账号级**的：绑定、主页展示自己的项目、
有新 repo / 新 merged PR 时提示「要不要发帖分享」（见 `DONE.md`）。
它到此为止 —— GitHub 的东西只出现在个人主页那一角，
**社区正文里贴出来的 GitHub 链接仍然只是一条蓝字**。

下面这几条是把它从「个人资料的一栏」变成「社区的一层」。
按价值排，不按难度排。

### 先做：让贴出来的链接自己会说话

- [x] **资源库里的 GitHub 链接直接问 GitHub，不再问模型**。这是整条线上
      成本最低、见效最快的一步：资源库原来把链接和群里的上下文丢给模型
      让它猜「这是什么」，而 GitHub 的接口**直接就会回答**，
      而且答案是权威的（仓库叫什么、一句话简介、什么语言、多少 star、
      归没归档；issue/PR 还带开着还是已合并）。省一次调用，答案还更准。
      落在 `fact_*` 那几列而不是 `ai_*` —— 界面上标的是「来自 GitHub」，
      不是「AI 整理」：两种都标成机器写的话，那句提示就废了
- [x] **帖子底下有了「这篇提到的」**（repo / issue / PR）。
      正文里那条链接一个字不动，卡片在**读的时候**从缓存拼出来 ——
      烤进正文 HTML 的话，「★ 1.2k」会永远停在发帖那天，
      而且看不出它是旧的。**渲染这条路上绝不联网**：缓存里没有就
      整块不出现，正文里那条链接原样还在，读者什么都没少
- [ ] **commit 与代码行永久链接**还没展开。前者要显示 commit message，
      后者要把那几行代码取回来 —— 都比 repo/issue 多一次取内容，
      而且代码块还要过一遍高亮。等 `GITHUB_API_TOKEN` 配好再说
      这是本节里唯一一条**每个人每天都会碰到**的：这个社区互相甩的
      就是 repo 和 PR 链接（`IDEAS.md` 第 2 条早就点破了「最有价值的
      就是互相甩的链接」）。一条裸链接要点进去才知道是什么，
      而**大多数人不会点** —— 展开成卡片等于把「这是什么」提前一步。
      要走服务端抓 + 缓存（客户端直连会把每个读者的 IP 送给 GitHub），
      并且**只认公开仓库**：私有仓抓不到是对的，抓得到才是事故
- [ ] **`owner/repo#123` 这种简写自动成链**。社区里本来就这么写
- [ ] **代码行永久链接展开成代码块**（`/blob/<sha>/file#L10-L20`）。
      只认带 sha 的永久链接 —— 认 branch 的话内容会在帖子底下悄悄变，
      而讨论还停在旧代码上

### 再做：把项目变成站内的一等公民

- [ ] **发帖可以关联一个 repo**，帖子列表上带项目标记，
      项目页能反查「站里聊过它的帖子」。
      现在同一个项目的讨论散在几十篇帖子里，谁也串不起来
- [ ] **项目页 / 项目目录** —— 社区成员的公开项目汇总成一页，
      可按语言、活跃度筛。这是这个社区**最适合对外展示的一张脸**
- [ ] **「谁在用 / 谁在做」** —— 一个项目下面显示站内有哪些人绑过它、
      提过 PR。⚠ 隐私口径要先定：绑定关系是公开的（本来就在 GitHub 上），
      但**不能因此推断出「这个微信号是这个 GitHub 账号」给不该看的人**

### 可以做，但要先想清楚

- [ ] **release / 新 PR 的 webhook 通知**。⚠ 硬约束：
      **到站内提示为止，绝不代发**。现有 `githubSharePrompts` 的形状
      就是对的 —— 生成一条「要不要分享」，人点了才发。
      自动发帖会把这里变成一个机器人刷屏的地方，
      而且违反「网站不能带用户发消息」的口径
- [ ] **贡献者徽章 / 等级** —— 和站内经济系统接。
      先想清楚它奖励的是什么：star 数会奖励营销，
      merged PR 数会奖励拆分 commit
- [ ] ~~用 GitHub 登录~~ —— **不做**。现阶段「只有群成员能登录」，
      GitHub OAuth **必须永远只能绑到一个已有账号上，不能创建账号**。
      现在的实现就是这样，这一条写在这里是防止后来的人「顺手」加上

## 十、个人主页的「这个人是什么样的」（站长 8-10 追加）

原话：「个人主页分析加一个口头禅 和谁对话最多 之类的 还有称号」。

现在的成员主页只有四个数字（发言数、被 @ 数、被回复数、共同群）
和一列最近被提及 —— **它说得清这个人有多活跃，说不清这个人是谁**。

- [x] **「常挂在嘴边」上线了**（不叫「口头禅」—— 见 `DONE.md`：
      算出来的结果一半是真口头禅、一半是他常聊的东西，
      两者算法上分不开，**改标题比改算法诚实**）
- [x] **最常用的表情**单独一条 —— `[旺柴]` 是他点的，不是他说的
- [x] **和谁 @ 得最多**（不叫「聊得最多」：回复关系卡在上游）
- [x] **称号摆上主页**，佩戴中的排第一
- [ ] 还可以再加的：最活跃的时段、第一次说话是哪天、
      和某个人的「相遇」时间线。都走同一套边界（共同群 + 隐私开关），
      加起来的边际成本很低

## 「声明了、没人读」这件事本身

反复出现的那个病，现在有了静态检查：

- **配置项** —— `tests/dead-settings.test.ts`。65 个里 10 个从来没被读过。
  出路只有三条：接上、标 `planned`、退役（`RETIRED_SETTINGS`，seed 会
  把行从库里删掉 —— 只从清单里删不够，后台列的是库里的行）。
- **权限点** —— `tests/permission-wiring.test.ts`（早先做的）。

- **数据库列** —— `tests/dead-columns.test.ts`。1058 列逐列检查，
  每一列要么被读、要么在 `src/lib/db/dead-columns.ts` 上写明实情。
  22 列在名单上，分五类（`duplicate` / `decided-against` /
  `superseded` / `planned` / `gap`）。

名单上最该动手的几个：

| 列 | 实情 |
|---|---|
| `user_privacy.hide_from_directory` | **重复的开关** —— 真正在用的是 `users.directory_hidden` |
| `user_privacy.hide_activity_hours` | 当初想清楚后**主动没做**（目录只给「本周/本月」粗档，没有逐小时直方图可藏） |
| `user_identities.*`（4 列） | **整张表没人用** —— GitHub 绑定走的是 `github_connections` |
| `bind_codes.attempts` | 设计变了：绑定码是用户发给机器人的，站内没有「输码」这一步，也就没有可爆破的地方 |
| `forum_posts.pinned_globally` | 站内公告（定向、可关闭、会过期）在「所有人都得看见」这件事上做得更好，这一列大概该删 |
| `invites.grant_role_id` | 邀请码创建界面根本不让选角色 —— 值得确认是不是漏了个功能 |
| ~~`api_usage.*`~~ | ~~整张表建了 763 天、0 行~~ —— **已接上**：每一次 HTTP 尝试记一行，健康页有了「最近 24 小时」那一块 |

删列要走迁移（SQLite 得重建整张表），比删一行配置重得多，
所以名单只逼「说得出实情」，不逼删。

顺手修掉的：`storage_snapshots.disk_total/used` 其实一直在写，
只是存储页只显示百分比 —— 现在把绝对值也显示出来了
（「还能撑多久」「清一次能腾出多少」，百分比答不了）。

## 会话与设备

- [x] **微信被显示成了 Chrome** —— 线上 65 个来自微信的会话里 47 个中招。
      内嵌浏览器的 UA 里全都带 `Chrome/` 或 `Safari/`，必须排在前面判；
      企业微信还要再往前（它同时带 `wxwork` 和 `MicroMessenger`）
- [x] **同时登录的设备有上限**（默认 10）—— 线上有人三天攒了 25 个活会话，
      「登录设备」那一页因此认不出哪台是自己
- [x] **过期会话真的删掉** —— 在此之前没有任何地方删过。每一行都带着
      IP 和 UA，留着不看等于白留一份可泄露的东西
- [ ] 用户管理剩下的：导出 / 解绑重绑 / 合并账号。
      线上 117 人全都绑好、没有重号 —— **现在做是在造还不存在的问题**，
      等真的出现再动

## 公告

- [x] **定向到群** —— 以前只能按身份组定向。「A 群周六线下」发给全站，
      对另外十一个群的人是纯噪音，而且顺带告诉了他们 A 群的存在和活动安排，
      **而群的事情属于群里的人**
- [x] 两个条件同时填 = **取交集**。并集听起来覆盖更广，但它的失败方向是
      「发多了」—— 而这两个维度存在的理由恰恰是发准一点
- [x] 后台显示的受众数 = 真的看得到的人数（有测试直接钉这条不变式）

## 权限矩阵的诚实化

- [x] **`group.sync.trigger` 接上了** —— 手动触发同步从 `group.manage` 里拆出来。
      改群配置是改状态，触发同步只是排一个队；合在一起的后果是
      「想让人在同步卡住时踢一脚，就得连带给他改群配置的权限」
- [x] **`group.stats.read` 接上了** —— 群页现在两个权限点任一即可进，
      没有 `group.manage` 的人看到只读视图（并且明确说是权限不够，不是页面坏了）
- [x] **`forum.view` / `forum.react` 退役** —— 已经有别的机制在管同一件事，
      多留一个勾就是第三套判断的入口
- [ ] 剩下的 planned：8 个是「功能没做」（user.delete/merge/export、
      permission.override、module.install/config、broadcast.email、badge.manage），
      4 个是「被更粗的权限管着」（moderation.action、activity.view/apply、digest.manage）
- **媒体缓存卡在上游**：`/v1/messages` 不返回任何媒体地址，
  库里 2998 条图片消息的正文就是「[图片]」三个字 —— 没有可缓存的东西

## 部署（原始需求：「想一个尽可能减少服务器瘫痪时间的部署方法」）

- [x] **蓝绿已上线并实测**。三次全程探针（部署 / 回滚 / 再部署），
      共 435 次公网探测，**全部 200，没有一次 502**
- [x] 回滚路径也实测过 —— 未经测试的回滚等于没有回滚
- [x] 进程租约（两个实例同时活着时，推送只由一个进程发）
- [x] 优雅停止（143 不算失败、SSE 主动断开、20 秒兜底）

- [x] **502 页真的能被用户看到了** —— 它一直被 Cloudflare 在边缘换掉。
      改成 503 之后原样透传（临时加一个指向死端口的 location 验证的，零停机）

## 找消息 → 引用（原话：「我想找消息，我不能快速滑动某手机端去引用消息」）

- [x] **关键词检索结果能直接引用** —— 以前要跳到按天回看、
      在几千条里重新找到它、再点那儿的引用。三步做一件事，
      而搜索本来就是为了少走路
- [x] **语义检索结果有出口了** —— 它以前**一条路都没有**：
      看到一段有用的对话，只能自己记住群名和时间再去翻
- [x] 补齐 `.hairline-t`（这一组本来只有 b 和 r）

## 权限：「被更粗的权限管着」这一类清完了

这一类一共 8 个。逐个查完之后：

- **接上 2 个** —— `group.stats.read`、`group.sync.trigger`。
  它们是真的分权：看群统计 ≠ 改群配置，触发同步 ≠ 改群配置
- **退役 6 个** —— `forum.view`、`forum.react`、`moderation.action`、
  `digest.manage`、`activity.apply`、`activity.view`。
  每一个都已经有更具体的机制在管同一件事

现在 planned 里只剩 8 个「功能真没做」的
（user.delete/merge/export、permission.override、
module.install/config、broadcast.email、badge.manage），
并且有一条测试把这个状态钉住 —— 新增 planned 要动那一行。

- [x] **本机备份的恢复演练** —— 在此之前没有一份备份被证明过能用
      （现有的演练只对异地跑，而异地还没配）。现在每次备份都演一次，
      结果显示在后台「恢复演练」那一栏
- [ ] 异地备份仍未配置：需要一个 S3 协议的对象存储
      （R2 / B2 / MinIO 都行，每天几 MB 落在免费额度里）—— **等凭证**

## 隐私开关归到一处

- [x] 「隐身」原来单独摆在个人资料页，榜单和检索那两个在隐私页 ——
      三个问的是同一件事「谁看得见我」，却分在两页。
      **分开的后果不是多点一次，是有人设了其中一个就以为设完了**
- [x] 三个开关走同一个模型、同一条写路。它们存在不同的表里
      （隐身在 `users.directory_hidden`），由 `source` 字段分流 ——
      界面不知道有两张表
- [x] 删掉重复的写路 `setDirectoryHidden`
- [x] 个人资料页只显示当前状态 + 一条去改的路

## 第四个注册表：功能开关

- [x] **`status` 必须说实话** —— 加了「wired 的必须真的被判过 /
      planned 的必须真的没被判」这条静态检查。另外三个注册表
      （权限点、配置项、数据库列）都有，开关这一份一直没有
- [x] **功能开关也有退役机制了** —— 和配置项、权限点同一套办法
      （seed 启动时从 `feature_flags` 表里清掉）
- [x] **周报归进模块登记表** —— 它以前有两个各说各话的开关：
      配置项叫「启用每周精选回推」、功能开关叫「每周精选回推微信群」，
      两个名字都在暗示它会自己发出去，而代码从头到尾只备草稿

## 没写完的草稿会被提醒了

线上 56 篇帖子对着 **8 份草稿**，其中两份上千字（3353 字、1169 字）——
有人认真写了一半，而**没有任何地方提醒过他**。草稿页藏在「我的」
下面，离开那个编辑器之后它就消失了。

- [x] 首页「你不在的时候」那张卡加上草稿，**排在最前面**
- [x] 只有一份时报标题：「《人脑相当于多大的 AI 模型？》还没写完」
- [x] 不设时间窗 —— 放了三天的草稿更需要有人提一句

## 群聊整理帖：同意齐了之后没人知道

线上 56 篇帖子里只有 3 篇是转帖来的，而其中**一篇三位原作者全同意了、
可见性还停在 group** —— 提升可见范围的按钮只有版主看得到，
而没有任何地方告诉版主「这一篇已经齐了」。

- [x] 举报队列那一页加上「等着提升可见范围」的队列（同一类工作：
      有人在等一个治理决定；多开一页的结果是那一页没人每天看）
- [x] 另列「还在等原作者表态」的数目 —— 这一步**不该催**
- [x] 作者那一侧说清楚在等谁，以及为什么不由他自己按
- [x] `forum_post_sources.converted_at` 从死列名单上划掉 —— 队列真的读它了

## 资源库：213 条链接，只有 2 条被赞过

那一页的注释写着「两百条里值得看的就那么十几条，而只有点赞数
能把它们浮上来」—— 而点赞数是 2，于是「最有用」那一档
**实际上是在按时间排**。

- [x] 加「多人分享」一档，并设为默认 —— 这一页是资源库不是时间线
- [x] 用 `visibleShares`（你看得到的群里的次数），**不是全站次数** ——
      拿全站次数排序会让顺序本身泄露别的群的热度

## 称号解锁不再是「关不掉」的

线上 27 条 `system` 通知里 **26 条是「解锁称号」**，
而 `system` 在 ALWAYS_ON 里 —— 关不掉。它和
「你在群里的发言被整理成了帖子」共用同一个静不了音的开关。

- [x] 称号解锁拆出自己的类型（可关），`system` 留给真正该关不掉的事
- [x] 历史通知一次性改签 —— 不改的话它们永远躺在「系统公告」那一档下面
- [x] **`NotificationType` 原来有两份手写清单** —— 顺手合并成一份

## 榜单上有从没注册过这个站的人

榜单按 `daily_stats` 的 wx_id 聚合 —— 群里的每一个人都在榜上，
**包括从没打开过这个站的人**（线上第 4 活跃的那位就没有账号）。

而退出榜单的开关需要一个账号才拨得动：暴露对所有人成立，
退出只对加入过的人开放。

- [x] 访客看到的榜单里，没注册过的人**不具名**（名次和条数照旧真实）
- [x] 登录成员看得到全名 —— 那些昵称他们每天在微信里都看见

- [x] **管理员看到的完整榜单现在标出了「别人看不到的那几行」** ——
      代码里那个 ⚠ 注释说的正是这件事：不标的话他会照着一个只有自己
      看得见的名次去发公告、发奖
- [x] 「谁藏了自己」**绝不给普通成员** —— 给了等于把开关废掉，
      藏起来的人反而更显眼
