<!--
  **读者**：第一次看到这个仓库的人
  **性质**：**现状**
  **最后核对**：2026-08-13

  这三行写在注释里而不是正文里 —— README 是仓库的门面，
  把内部元信息摆在最上面很难看。但它仍然要有：
  一份不知道什么时候核过的现状描述，读者只能当它是对的。
  tests/docs.test.ts 里为 README 单独放行了这种注释写法。
-->

<div align="center">

# Agentic Lab

**一个微信群社区的站内门户** —— 把群聊存档下来、可检索，
并在它上面长出论坛、积分、称号、活动、资源库、开放 API。

[![License: AGPL v3 + Commons Clause](https://img.shields.io/badge/License-AGPL%20v3%20%2B%20Commons%20Clause-blue)](./LICENSE)
[![Next.js 16](https://img.shields.io/badge/Next.js-16-black)](https://nextjs.org)
[![React 19](https://img.shields.io/badge/React-19-61dafb)](https://react.dev)
[![SQLite](https://img.shields.io/badge/SQLite-FTS5-003b57)](https://sqlite.org)
[![tests](https://img.shields.io/badge/tests-6900%2B-brightgreen)](./tests)

</div>

---

## 它不是一个通用的论坛程序

整个站建立在一条硬约束上：

> **只有微信群成员能登录。**

账号是靠在群里向机器人发一条验证码建立的，**没有公开注册入口**。
所有可见性判定（谁能看哪条消息、谁的主页对谁可见）都以
「你们有没有共同的群」为准。

如果你想拿它当一个开放注册的社区来用，要改的不是一两个开关，
**而是这条前提本身** —— 它渗在权限、可见性、同步、榜单每一层里。

## 有什么

| | |
|---|---|
| **群聊存档** | 全文检索（FTS5 + 中文分词）、按人/按群/按时间的活跃度画像 |
| **论坛** | 版块、可见性收口、匿名帖、投票、悬赏、精华、草稿、定时发布 |
| **积分与赛季** | 打卡、榜单、商店、称号、补签卡 |
| **开放 API** | 令牌 + scope，读帖发帖、读群消息、逐群授权的代发（强制署名） |
| **GitHub 生态** | 账号绑定、项目目录、帖子里的链接自动展开成卡片 |
| **管理后台** | 30 个页面：权限矩阵、审计、封禁申诉、备份演练、健康检查 |
| **PWA** | 可安装、Web Push、离线兜底 |

---

## 跑起来

需要 Node 22+（开发用的是 24）。数据库是本地 SQLite 文件，不需要额外的数据库服务。

```bash
npm install
cp .env.example .env.local     # 然后把里面的值填上，见下一节
npm run bootstrap              # 建表、灌种子（幂等，可反复跑）
npm run dev
```

打开 http://localhost:3000 。

第一次进去是没有账号的 —— 登录要走微信绑定，而那需要上游 API（见下）。
只想看看界面的话，`npm run bootstrap` 之后库里已经有版块、权限点和角色，
可以自己往 `users` 表里插一行再造一个会话。

### 常用脚本

| 命令 | 做什么 |
| --- | --- |
| `npm run bootstrap` | 迁移 + 种子 + 拉群列表。幂等 |
| `npm run sync` | 从上游同步消息与成员 |
| `npm run health` | 定时任务的一轮：探活、备份、各种结算、告警投递 |
| `npm run backup` | 本地备份（`npm run offsite` 推异地） |
| `npm run db:generate` | 改完 `src/lib/db/schema/` 之后生成迁移 |
| `npm test` | 跑全部测试（tsx 的 test runner，不做类型检查） |

单跑一个测试文件：

```bash
npx tsx --conditions=react-server --test tests/github-oauth.test.ts
```

---

## 依赖哪些外部服务

只有第一项是必须的，其余不配就是对应功能整体不出现 —— **不会报错，也不会半死不活**。
这是这个仓库里反复出现的一条原则：半套配置比没配置更糟，
它会让功能真的跑起来、真的失败，然后失败被当成偶发问题忽略掉。

| 服务 | 必须？ | 不配会怎样 |
| --- | --- | --- |
| **NekoBot API**（微信侧的消息与机器人接口） | 是 | 站起不来 —— 登录、同步、群列表全靠它 |
| Web Push（VAPID，自己生成） | 否 | 只有站内通知，没有系统推送 |
| **GitHub OAuth App** | 否 | GitHub 绑定整个功能不出现：入口不渲染、路由 404 |
| S3 协议的对象存储（R2 / B2 / MinIO） | 否 | 没有异地备份副本，磁盘挂了备份跟着一起没 |
| 一个 LLM API | 否 | 链接摘要、语义检索这些降级成没有 |

各自的环境变量、怎么申请、注意事项，都写在 `.env.example` 的注释里。

### GitHub 绑定申请的权限

**scope 是空的。** 也就是说拿到的 token 只能读任何匿名访客也能读到的东西 ——
公开资料、公开仓库、公开动态。它碰不到任何私有仓库，也发不了任何东西。

抓数据走的是 `/users/{login}/repos` 和 `/users/{login}/events/public`，
这两个接口**按定义只返回公开数据**，不是靠我们过滤。

绑定是「一个已经登录的人给自己的账号加一个绑定」，
**不是一种登录方式** —— 见 `src/lib/github/link.ts` 顶上那段说明，
以及 `tests/github-oauth.test.ts` 里逐条钉死它的那些用例。

---

## 从哪读起

14 份文档核对下来最大的收获是：**没有人知道每份文档是「打算」还是「现状」**。
所以现在每一份顶上都写着读者是谁、是打算还是现状、最后核对是哪天。

| 想知道什么 | 读哪份 |
|---|---|
| 代码怎么组织、三套开关怎么选、定时任务在跑什么 | **`ARCHITECTURE.md`**（先读这份） |
| 这个项目踩过哪些坑，尤其是「看起来在跑、其实什么都没拦住」那一类 | **`LESSONS.md`** |
| 还剩什么没做 / 已经有什么 | `ROADMAP.md` / `DONE.md`（**全站只有这两份打勾**） |
| 线上长什么样、出事怎么办 | `DEPLOY.md` |
| 积分、赛季、商店为什么这么设计 | `ECONOMY.md`（讲「为什么」的范本） |
| 论坛的可见性与管理动作 | `FORUM.md` |
| 权限判定链 | `PERMISSIONS.md` |
| 活动与模块 | `MODULES.md` |
| 数据建模的决定 | `SCHEMA.md` |
| 还没做的想法 | `IDEAS.md` |

`AGENTS.md` / `CLAUDE.md` 是**给 AI 读的**，由 `next dev` 自动写回 ——
别往里塞给人看的内容。`docs/archive/` 里是冻结的历史设计稿，
它记录「当时为什么这么决定」，不描述现状。

---

## 代码里的一些规矩

在这个仓库里改东西之前，值得知道的几条（详细的在 `ARCHITECTURE.md`）：

- **Next.js 16 App Router + Turbopack、React 19、Tailwind v4、SQLite（better-sqlite3 + Drizzle + FTS5）**
- 注释用中文，而且写**为什么**，不写「这里做了什么」。
  这个仓库里长注释很多，它们大多是在记「为什么不是那个更显然的做法」——
  那些是最容易被下一个人顺手改回去的东西。
- **纯规则放 `*-rules.ts`**，不许 import `server-only` / `@/lib/db` / `drizzle-orm`。
  这样它们能被直接测，而不需要一个数据库。
- 加数据库列要写迁移，放 `drizzle/`，照编号往后排。
- 手机端和电脑端都要能用 —— 相当一部分访问来自微信内置浏览器。
- 测试里的 `describe` / `it` 说的是**这条规则为什么存在**，不是「测试某函数」。
  几个大的结构性检查（无障碍、审计覆盖、预览态拦截、UI 一致性）会扫全仓，
  加了新页面要跟着跑一遍。

---

## 参与进来

修 bug、补测试、改文档、提可用性问题 —— **随时欢迎，不用先问**。

加功能之前先开个 issue 聊一下：这个项目是**一个具体的微信群在用的站**，
不是通用产品，所以门槛不是「这个功能好不好」，而是「那个群需不需要」。

提交前三条都要干净（`npm test` 用 tsx 的 runner，**它不做类型检查**，
所以 `tsc` 那条不能省）：

```bash
npx tsc --noEmit && npx eslint . && npm test
```

几条会让 PR 更容易被合的规矩：

- **注释用中文，写「为什么」**。仓库里长注释很多，记的是「为什么不是那个
  更显然的做法」—— 要改带长注释的代码，先读那段注释，它多半在解释
  一个你还没踩到的坑
- **纯规则放 `*-rules.ts`**，不 import `server-only` / 数据库 —— 这样不用起库就能测
- 测试的 `it` 标题说**这条规则为什么存在**，不是「测试 xxx 函数」。
  一条测试挂了的时候第一个问题永远是「它在保护什么」，
  标题答不上来的话，它会被顺手改成通过

完整版见 **[CONTRIBUTING.md](./CONTRIBUTING.md)**，
里面还讲了两条测试跑法为什么必须分开、以及绝对不能提交进来的东西。

## 许可证

**GNU AGPL v3 + Commons Clause** —— 见 [`LICENSE`](./LICENSE)。

- **不能闭源。** 改完之后源码必须同样公开，**包括只通过网络提供服务的情况**
  （AGPL 相对 GPL 多出来的正是这一条，也是对一个网站项目唯一有意义的那条）
- **不能商用。** 不能靠它收费，也不能靠它提供收费的托管/咨询/技术支持

自用、学习、研究、非营利社区部署都不受限制。想商用请联系作者单独授权。

> ⚠️ 加了 Commons Clause 之后，本项目在 OSI 的定义下**不算「开源软件」**
> （开源的定义不允许限制使用领域），GitHub 会标成 "Other"。
> 这是满足「不能商用」必须付的代价 —— 任何 OSI 认可的许可证都不能禁止商用。

---

## 这个仓库里没有什么

为了能公开，下面这些东西一律不在仓库里，也请不要提交进来：

- 任何真实的密钥、token、密码（`.env*` 全部忽略）
- **源站的 IP 或主机地址**。站点在 Cloudflare 后面，源站只对 CF 网段开放，
  而这套防护唯一的前提就是没人知道它。部署地址放在被忽略的 `.deploy-host` 里。
- 数据库文件、备份、归档（`/data/`、`*.db`）
- 真实的群聊内容与成员信息
