# 踩过的坑

> **读者**：在这个仓库里写代码的人和 AI
> **性质**：**现状**（这些事真的发生过）
> **最后核对**：2026-08-10

一条贯穿始终的主线：**这个项目最常见的失败不是「功能坏了」，
是「看起来在工作、其实什么都没发生」。** 下面每一条都是这个形状。

---

## 一、检查看起来在跑，实际什么都没拦住

- **`if ... | grep -q` 配上 `set -o pipefail`**：tsc 发现错误会返回非零，
  整条管道跟着非零，于是判定为假，**反而跳过了报错分支**。检查形同虚设。
  改成先把输出落到文件再判断。
- **过滤按错误文本而不是按路径**：写成过滤 `"not assignable"`，
  把真实的类型错误一起滤掉了。同样是形同虚设。
- **`grep -qE '[0-9]+ error'`**：eslint 在只有 warning 时会打印「0 errors」，
  于是每次都误判失败。要写 `[1-9][0-9]*`。
- **只扫 `src` 的 lint**：看起来在跑，实际放过了三分之一的代码
  （加上全仓扫描的当天就查出两处真问题）。
- **`npm test` 用 tsx 跑，不做类型检查**。类型错误只有 `tsc --noEmit` 抓得到 ——
  测试全绿不代表能构建。
- **断言的范围划错，红的是对的代码**：切源码时一路切到文件末尾，
  把另一个函数里那次**合法**的调用也算了进来。

## 二、死开关

**缺的功能人看得出来，一个显示成「开」的开关会让人不再去想这件事。**

已经查出并修过的：`bookmark_folders` 整套、`forum_drafts` 整张表、
`feature_flags` 整套机制（`isFeatureEnabled` 零调用点）、
`user_privacy` 整张表（四个隐私开关零读零写，而建表注释写着
「群聊可检索这件事需要它来平衡」）、`activeAnnouncements()` 零调用点
（后台发的站内公告**没有任何用户看得到**，而界面回一句「已发布」）、
`auth.require_passkey_for_admin`（后台显示成开着，没有一行代码读它）。

**识别方法**：schema 列 / 设置项 / 权限点，在定义之外 grep 不到读取点，就是死的。

**还欠一条静态检查**：像 `audit-coverage` 那样，零读取点就让测试红。
在那之前，每加一列都要问一句「谁读它」。

## 三、按 id 连表，不按 rowid

`messages_fts` 是独立的 fts5 虚表，它的 rowid 和 `messages` 的 rowid
**没有任何关系**。按 rowid 连出来的是一批完全无关的消息。

而这个错误极难从结果上看出来：命中数看着很正常（搜「台风」返回 22 条），
只有真的去读那 22 条内容才会发现里面一条台风都没有。
是上线之后对着生产数据抽查内容才抓到的。

## 四、排序要有次级键

只按 `ts` 排，同一秒有多条时 SQLite 不保证顺序 ——
**同一条消息可能在第 1 页和第 2 页各出现一次，另一条一次都不出现**。
而列表看起来一切正常。

## 五、两份状态迟早分叉

- **通知重复弹出**：服务端有 `readAt`，客户端有一个活不过刷新的 `Set`，
  而回放查询完全不看 `readAt`。两者相乘 = 刷几次弹几次。
- **隐私开关存 localStorage**：换个设备、清一次缓存，关掉的全回来了。

**真值只能有一份。**

## 六、只返回一个组件状态，另一个就永远停在上一次

健康探测失败时写 `frp_tunnel: down`，成功时写 `upstream_api: ok` ——
没有任何路径把 `frp_tunnel` 写回 ok。隧道断过一次就永远卡在 down。

后果不是「显示不准」，是**健康状态变成一个学会撒谎的仪表盘**：
看过两次「明明好了还说坏」，真出事那次也不会有人信。

## 七、Next.js / React 这一版的坑

- **`"use server"` 文件只能导出 async 函数**。导出一个同步函数 → 构建挂掉，
  而部署脚本会正确地拒绝重启（线上保持旧版本）。
- **函数不能跨 RSC 边界**。Server Component 往 Client Component 传
  render-prop / 回调 = 线上 500。
- **React Compiler**：render 期间不许读 `Date.now()`、不许在 effect body 里
  setState、不许写 ref。时钟读取要推到查询层。
- **`RouteContext<'/你的/路径'>` 不能用在新路由上**：它从构建产物生成的
  路由表上取，而新路由还不在那张表里 —— `tsc` 报错，
  而流水线里 tsc 跑在构建之前。手写 `{ params }: { params: Promise<{...}> }`。
- **DOMPurify 的 `ALLOWED_TAGS` 很窄**，`div` 不在里面。
- **JSX 里的 `**粗体**` 会渲染成字面星号**。要用 `<strong>`。

## 八、浏览器与触摸

- **触摸指针在 `pointerdown` 那一刻被隐式捕获到起始元素**，
  兄弟元素的 `pointerenter` 永远不触发 —— 靠 enter 做的拖选
  **在手机上从来没生效过**，而鼠标上测是好的。
- **`group-hover:` 需要父元素上真的有 `group`**。没有的话那个元素
  在任何设备上都是全透明的。
- **不 `preventDefault` 掉 `dragover`**，浏览器会用拖进来的文件
  替换掉整个页面 —— 一篇没保存的正文就没了。
- **`prefers-reduced-motion` 会把动画压成 0.01ms**：只靠闪烁做的高亮，
  那些人什么都看不到。要有一层常驻的。

## 九、外部世界不按文档来

- **上游的错误响应体可能不是 JSON**（前面那层 CDN 挡下来时是 HTML），
  直接 `.json()` 会抛一个对排查毫无帮助的异常。
- **`PUT` 返回 200 只证明请求没报错**，不证明存对了 —— 传完要读回来核对。
- **嵌入接口可能忽略 `model` 字段**：填一个不存在的模型名照样返回正常向量。
  所以探测要发两句毫不相干的话去比对。
- **只依赖别人愿意告诉我们的事实，就会漏掉所有他没说的**：
  靠上游的改名事件去认老昵称，而上游根本没报过那一次。

## 十、安全上反直觉的几条

- **`in` 会走原型链**：`?scope=__proto__` 能过 `value in OBJ` 的校验。用 `Object.hasOwn`。
- **CSV 里以 `= + - @` 开头的格子会被 Excel 当公式执行**，
  而**引号转义挡不住** —— `"=1+1"` 照样求值。
- **`X-Forwarded-For` 是客户端可以伪造的**：nginx 传
  `$proxy_add_x_forwarded_for` 会把客户端自己发来的那串保留下来，
  而应用取第一个 → 按 IP 限流当场失效、审计日志可以随便伪造。
- **`endsWith` 式的域名判断骗得过**：`files.example.com.evil.net`。逐字比对主机名。
- **`//example.com` 不是相对路径** —— 它以 `/` 开头，但会去请求别人的服务器。
- **`img` 的 `src` 没有白名单时，任何人都能在帖子里放一个追踪像素**：
  一张 1×1 的透明图就够把「谁在什么时候读了这篇」连 IP 送出去。
- **`NOT IN` 遇到 NULL 会把整行判掉**。

## 十一、运维

- **在线上直接 `npm run build` 会把正在跑的进程脚下的 `.next` 换掉** ——
  502 里长的那一段是构建，不是重启。
- **`rsync --exclude .next` 挡不住 `.next-blue`**：`--delete` 会把
  此刻正在服务的那份产物删掉，而且不报任何错。
- **systemd 的 `ReadWritePaths` 列了不存在的路径会让单元起不来**，
  症状是一个和代码无关的 `226/NAMESPACE`。要用 `-` 前缀。
- **`ufw enable` 之前必须先放行 22**，否则那条命令就是最后一条能跑的命令。
- **curl 失败时 `-w '%{http_code}'` 会打印 `000` 且退出非零**：
  写 `|| echo 000` 会得到 `000000`，而它 `!= "000"`。
- **迁移在切流量之前跑**，所以有一小会儿是老代码跑新库 ——
  加列加表无害，删列要拆成两次部署。

## 十二、开工头一个月踩的（从已废弃的 STATUS.md 抢救出来）

| 坑 | 现象 | 修法 |
|---|---|---|
| 高质量消息的定义靠猜 | 每人少算 3–35 条 | 穷举反推出 `{text, quote}`，`npm run calibrate` 可复验 |
| `/users/{id}/groups` 返回对象不是数组 | 成员判定全失败，**所有人都「不是社群成员」** | 修类型；**判定失败改为 unknown 而不是 false** |
| SQL `max(sender_name)` 取名字 | 站长在榜上显示成 wxid（字典序 w > j） | 建 `people` 表，用窗口函数取最近一条 |
| `limit=500` 超上限 | 头像同步**静默**拿到 0 个 | 按端点钳制 limit + 失败不再静默 |
| trigram 分词器 | 「鉴权」「部署」这类 2 字词一个都查不到 | CJK 逐字切分 + unicode61 + 短语查询 |

两条共同教训，和这份文档开头那条是同一件事：

- **上游返回的形态必须实测，不能照文档推测。**
- **故障不能伪装成业务结果。** 那个「你不是社群成员」的假象就是这么来的 ——
  接口挂了，判定拿到 false，界面如实地把 false 说成了「你不是成员」。

## Route Handler 里的 `request.url` 是内网地址

短链 `/p/<code>` 本地怎么点都对，部署上去第一次点就跳到了
`https://localhost:3000/forum/p/...` —— **每一条分享出去的链接
都把人送回他自己的机器**。

站点在 nginx 后面，Node 收到的请求行写的就是 `http://localhost:3000/...`。
`new URL(path, request.url)` 忠实地照着拼。

坑在于**本地测不出来**：本地它恰好是对的。也在于
`middleware.ts` 里同样的写法当时**是对的** —— Edge 运行时会按请求头
重建公网地址（线上实测跳的是正确域名）。

**这条豁免后来没了**：Next 16 把 middleware 改名成 `proxy.ts`，
运行时固定成 nodejs。同一行代码，换个运行时就开始把人送去 localhost。

- 跳站内 → 写相对 `Location`，这台机器不需要知道自己叫什么
- 非要绝对地址（OAuth redirect_uri、邮件链接、OG 图）→ `env.site.url`
- 读 `new URL(request.url).searchParams` 没问题，错的只有 host

`tests/absolute-urls.test.ts` 扫所有 `route.ts` 盯这一条 ——
现在没有例外了，proxy 也一起管。

## 两层的跳转地址要求正好相反

同一件事「往哪跳」，在 proxy 和 Route Handler 里的正确写法是反的：

| | 相对 Location | 绝对地址 |
|---|---|---|
| **Route Handler** | ✅ 正确 | 只能来自 `env.site.url` |
| **proxy.ts** | ❌ `ERR_INVALID_URL` | ✅ 必须，且只能来自 `env.site.url` |

proxy 那一层的 Location 会被 Next 自己 `new URL()` 一遍，
给相对地址会抛 `ERR_INVALID_URL` —— 而这不是那一条路径 500，
是**整条 matcher 覆盖的路径一起 500**：登录、后台、论坛同时挂掉。

这个是本地跑真构建才发现的。`npm test` 和 `tsc` 全绿，
因为它是运行时行为，既不是类型问题也不是结构问题。

**教训不是「记住这张表」，是「碰 proxy 就得本地起一次真构建再点几下」。**
它是唯一一处「写错一行、整站白屏」的代码。

## Next 16：middleware 已废弃，改名 proxy

`mv middleware.ts proxy.ts`，导出的函数从 `middleware` 改成 `proxy`，
`skipMiddlewareUrlNormalize` 之类的配置项同步改名。
**运行时固定 nodejs，不可配置** —— 这条是有用的：
proxy 从此查得了数据库，站点级配置（不是身份）可以在这一层判。

身份仍然不能在这一层判：光凭 cookie 自称就放行等于让客户端自证身份。

## redirect() 在有 loading.tsx 的路由下不是 HTTP 跳转

论坛那道门第一版写在 `layout.tsx` 里。线上实测：访客拿到的是
**200 + 一个空壳**，跳转指令躺在 HTML 流里等客户端执行。

浏览器会跳，所以点着是对的 —— 而 `curl`、爬虫、监控、
微信的预览抓取拿到的都是 200。内容确实没渲染，但状态码在骗人。

门要开在 proxy 里，在渲染开始之前。页面里那一道留着当兜底，
两处读的是**同一个**判定函数 —— 一个决定，两个执行点，不是两套逻辑。

## 朴素的「去掉行注释」会吃掉半行代码

结构测试里到处都是这一行：

```ts
s.replace(/\/\*[\s\S]*?\*\//g, "").replace(/\/\/.*/g, "")
```

第二个 replace 是错的。一个以「反斜杠、斜杠」结尾的正则字面量，
后面紧跟着字面量本身的收尾斜杠 —— 两个斜杠连在一起，
于是这一行从那儿往后全被当成注释吃掉。

坏在**它不报错**：一条断言去比对一个根本不存在的字符串，
结果是「找不到」。运气好的时候测试红得莫名其妙（代码明明是对的），
运气不好的时候是一条永远为真的否定断言。

现在统一走 `tests/_source.ts` 的 `stripComments`：
只认**行首**的 `//`。少剥掉几个行尾注释，
比悄悄吃掉半行代码安全得多。

## UA 里的内嵌浏览器必须排在 Chrome / Safari 前面

安卓微信的 UA 是
`… Chrome/132 … Mobile Safari/537.36 … MicroMessenger/8.0.32`。

把 `MicroMessenger` 放在 `Chrome/` 后面判，它永远轮不到 ——
线上 65 个来自微信的会话里 **47 个被显示成了 Chrome**，
而微信是这个社群绝大多数人进站的方式。

企业微信还要再往前：它的 UA 里**同时**带 `wxwork` 和 `MicroMessenger`。

规律是「越具体的越靠前」，而这个顺序在代码里看不出重要性 ——
所以配了一条结构断言盯着，调换一下就会红。

## `next start` 收到 SIGTERM 以 143 退出，systemd 当它失败

蓝绿上线后第一次真部署，一切正常 —— 而被停掉的那一边留下一个
`failed` 单元。部署脚本没报错，公网也一直是 200。

两个不同的原因叠在一起：

**① 干净停止 = 退出码 143。** 128+15，Node 的惯例。
systemd 默认把非零退出码当失败。线上实测：零流量停止耗时 0 秒，
结果却是 `failed / exit-code 143`。修法是 `SuccessExitStatus=143`。

**② 有 SSE 连接时 = 90 秒超时后被 SIGKILL。**
SSE 是一条永远不结束的响应，而 `next start` 会等手上的请求跑完再退 ——
它永远等不到。修法是应用侧在 SIGTERM 时主动断开所有 SSE
（用户无感，`EventSource` 本来就会自动重连，而且此刻流量已经切走），
外加 `TimeoutStopSec=20` 兜底。

**那个 failed 不影响服务，但它是误导性状态** —— 以后真出事时，
看 `systemctl list-units` 的人会先被这个假失败带走十分钟。

## 蓝绿部署要先解决「两个实例同时活着」

切换之后老的那一边不立刻停（为了能瞬间回切），中间有一段两个进程
连着同一个库。轮询只读没关系，但**推送派发的状态全在内存里** ——
两个进程各攒各的，同一条通知会推两遍。

解法是进程租约：抢到的跑，抢不到的歇着，持有者死了自动接手。
不要「让待机那边别跑」—— 那要让进程知道自己是不是待机的，
而那个答案在 nginx 的配置文件里。
