# 参与进来

> **读者**：想提 PR 的人
> **性质**：**现状**
> **最后核对**：2026-08-10

先说清楚这个项目的性质：它是**一个具体的微信群在用的站**，不是一个通用产品。
所以「加一个功能」的门槛不是「这个功能好不好」，而是「那个群需不需要」。
提功能之前先开个 issue 聊一下，比直接来一个大 PR 有效得多。

修 bug、补测试、改文档、提可用性问题 —— 这些随时欢迎，不用先问。

## 本地跑起来

见 [README](./README.md)。只有 NekoBot API 那一项是必须的，
其余外部服务不配就是对应功能整体不出现。

## 提交之前

```bash
npx tsc --noEmit          # 类型
npx eslint .              # lint
npm test                  # 全部测试
```

三条都得干净。`npm test` 用 tsx 的 test runner 跑，**它不做类型检查** ——
所以 `tsc` 那一条不能省。

`npm test` 里面是**两条跑法**，一条都不能少：

| | 跑什么 | 为什么分开 |
|---|---|---|
| `test:server` | `tests/*.test.ts` | 带 `--conditions=react-server`，服务端组件和 `server-only` 模块靠它才 import 得进来 |
| `test:ui` | `tests/ui/*.test.ts` | **不带**那个条件。带上的话 `react-dom/server` 和 `lucide-react` 都 import 不进来 —— 前者直接报「不支持」，后者拿不到 `createContext` |

所以**要把组件真的渲染出来看**的测试，放 `tests/ui/`。
这个仓库里大量组件测试只在读源码字符串，不是不想渲染，
是主跑法下渲染不了 —— 而读字符串守得住「这一行别被删」，
守不住它到底渲染成了什么。

`tests/test-lanes.test.ts` 盯着这件事：两条都要被 `npm test` 跑到、
`tests/ui/` 不许是空的、放进去的必须真的渲染了东西。

## 写代码时

- **注释用中文，写「为什么」。** 这个仓库里长注释很多，
  它们记的大多是「为什么不是那个更显然的做法」。
  那些正是最容易被下一个人顺手改回去的东西 ——
  如果你要改掉一段带着长注释的代码，先读那段注释，
  它多半在解释一个你还没踩到的坑。
- **纯规则放 `*-rules.ts`**，不许 import `server-only` / `@/lib/db` / `drizzle-orm`。
  这样它们不需要数据库就能测。
- 加数据库列要写迁移，放 `drizzle/`，照编号往后排，别改已经存在的迁移文件。
- 手机端和电脑端都要能用。相当一部分访问来自微信内置浏览器，
  它比你想的更旧、更容易杀后台。

## 写测试

`describe` / `it` 里说的是**这条规则为什么存在**，不是「测试 xxx 函数」。
理由是：一条测试挂了的时候，第一个问题永远是「这条到底在保护什么」——
如果标题回答不了，它会被顺手改成通过。

有几个结构性检查会扫全仓（无障碍、审计覆盖、预览态写入拦截、UI 一致性、
加载骨架），加了新页面或新组件之后要跟着跑一遍。
写这类静态检查时记得**先去掉注释再上正则**，
否则会被自己的说明文字骗过去 —— 越是把原因写清楚的地方越容易误报。

## 绝对不要提交进来的东西

- 任何真实的密钥、token、密码
- **源站的 IP 或主机地址**。站点在 Cloudflare 后面、源站只对 CF 网段开放，
  而这套防护唯一的前提就是没人知道它。地址放在被忽略的 `.deploy-host` 里。
- 数据库文件、备份、归档
- 真实的群聊内容、真实微信 ID、真实姓名和联系方式 —— 包括**测试夹具里的**

前两类一旦推上去，删掉也没用，历史里还在。发现了就当作已经泄露处理：
换密钥、换 IP。
