# 需求对账 — 你提过的每一条现在是什么状态

截至 2026-08-08 23:00。**设计层面没有漏项，实现层面才做到大约 15%。**

图例：✅ 已上线 · 🟡 部分 · 📐 只有设计 · ❌ 未开始

---

## 一、已经在线上跑的

| 需求 | 状态 | 说明 |
|---|---|---|
| 站点上线 + HTTPS | ✅ | https://agenticlab.sh，证书自动续期 |
| 读取群消息、统计发言 | ✅ | 10 个 bound 群、36,668 条消息，每 2 分钟增量同步 |
| 高质量消息统计 | ✅ | 规则反推校准，与上游榜单 10/10 吻合 |
| 以 bound 判定纳入范围 | ✅ | 群 bound 变化下一轮自动生效，无需人工开关 |
| 验证身份 / 登录 | ✅ | 三通道：好友申请理由 / 私聊 / 群内「登录 xxxxxx」 |
| 只有群成员能登录 | ✅ | 三级判定：成员表 → 消息镜像 → 回源上游 |
| 验证码点击复制 | ✅ | 验证码与群内指令都可轻点复制 |
| 同步群友昵称 | ✅ | people 表，每轮同步刷新，昵称改了会跟上 |
| 完善的管理员体系（底座） | ✅ | 65 权限点、9 身份组、190 条映射已入库 |
| jmr 设为站长 | ✅ | owner 身份组已授予 |
| 排行榜 | 🟡 | 首页有本周贡献榜，多维榜单未做 |
| 头像同步 | ✅ | 上游补了字段后覆盖 **1361/1391**，每轮同步刷新 |

## 二、只有设计、还没写代码

| 需求 | 文档 | 优先级 |
|---|---|---|
| Passkey / 密码登录 | PLAN.md §3.2 | **高** —— 现在每次登录都要发验证码 |
| 手机端 Tab Bar + 桌面端侧边栏 | PLAN.md §6.1 | **高** —— 现在是单栏，你要的 App 化布局还没有 |
| 打卡 · 积分 · 连胜 · 等级 | PLAN.md §4.4 | 高 |
| 群消息检索页面 | IDEAS #1 | 高（后端 FTS 已就绪，缺界面） |
| 后台管理界面 | SCHEMA.md 第五部分 | 高（表结构全建好了，零界面） |
| 论坛 + 群聊一键成帖 | IDEAS #11 | 中 |
| 未登录可看论坛 / 可见性分层 | PERMISSIONS.md | 中 |
| **可插拔活动系统** | MODULES.md | **见下方时间风险** |
| **域名注册活动** | MODULES.md 第四节 | **见下方时间风险** |
| 临时邮箱 | MODULES.md 第五节 | 中 |
| 每周精选回推微信群 | MODULES.md 第六节 | 中 |
| 邀请体系 | SCHEMA.md 第三节 | 中 |
| 积分商店 / 兑换 | SCHEMA.md 第七节 | 低 |
| 成员目录 + 技能标签 | IDEAS #13 | 低 |
| 存储分层裁剪 | PLAN.md §7.3 | 低（实测一年才 150MB，不急） |
| 外部用户 | 开关已建，默认关 | 低（你说先只开群成员） |

---

## 三、⚠️ 时间风险：域名注册活动

你说过「**明天**开放那个半天的域名注册，60 个名额」。今天是 8-08，那就是 **8-09**。

**活动系统现在一行代码都没有。** 完整做完（模块运行时 + 活动骨架 + 资格引擎 +
域名校验 + RDAP 查询 + 导出导入）不是一晚上的量。

三个选择，需要你定：

1. **只做域名登记页**（不做通用模块系统）—— 硬编码一个活动页面：
   资格判定 + 输域名 + RDAP 查可用性 + 占名额 + 后台导出 CSV。
   估计几小时，明天能上。代价是以后要重构进模块框架。
2. **推迟活动**，先把通用模块系统搭好再上第一个活动。
3. **告诉我这只是举例**，没有真的定在明天。

我倾向 1 —— 60 个名额的活动错过窗口就没意义了，而且登记逻辑本来就要写，
重构成模块的成本远小于错过活动。**但这个决定该你做。**

---

## 四、头像（已解决）

上游在 `members` / `leaderboard` / `users/{wx_id}` / `users/search` 四个接口
都补上了 `avatar` 与 `avatar_full`，`members` 还多了 `name` 字段。

覆盖率从 **22/1391 提升到 1361/1391**。主来源改为群成员名册（每轮同步刷新，
换头像会跟上），好友申请退化为补漏。剩下的 30 人是发过言但已退出所有同步群的。

处理过的两个坑：
- **协议不统一**：部分头像返回 `http://`，而站点是 HTTPS，
  浏览器会当混合内容直接拦掉 —— 表现是头像整个不显示且页面上毫无提示。
  存库时统一升级为 https（`src/lib/avatar.ts`）。
- **域名有多个**：`wx.qlogo.cn` 与 `mmhead.hk.wechat.com`。
  只放行已知的微信头像域名，避免这个字段变成任意图片注入点。

仍未解决的小问题：`/users/{wx_id}` 对部分账号的 `name` 依旧返回 wx_id 本身
（站长账号就是），所以显示名仍以群成员名册为准，不用这个字段。

---

## 五、工程基建对账（这一轮补的）

功能好不好做是一回事，**工程流程有没有立起来**是另一回事。
之前有一批东西是「表建好了但没有代码」或者干脆没有：

| 项目 | 之前 | 现在 |
|---|---|---|
| 版本控制 | **完全没有 git** | ✅ 已初始化，3 次提交，.env 与 data/ 确认未入库 |
| 备份 | **完全没有** | ✅ 每日 04:00，SQLite 在线备份 API + 完整性校验 + 7日/4周轮转 |
| 权限判定 | 65 个权限点躺在库里，**没有任何代码读它们** | ✅ `can()` 判定器，先拒后允、兜底拒绝、支持 scope 与到期 |
| 审计日志 | 只有 2 处零散写入 | 🟡 统一入口 `audit()` 已建，但还没有拦截层强制所有写操作走它 |
| 健康探测 | 表建了没人写 | ✅ 每 5 分钟探 frp 隧道/库/磁盘，`/api/health` 供外部监控 |
| 测试 | **一个都没有** | ✅ 16 个，覆盖高质量判定、中文检索、时区边界 |
| 接口限流 | 配置项写了但没实施 | ✅ 绑定接口 5次/10分钟 + 30次/天 |

**测试立刻就赚回成本**：第一次跑就抓到 FTS 查询构造的顺序 bug ——
先按空格切分再剔除语法字符，会把 `鉴权"OR"1` 拼成单个短语 `"鉴 权  OR 1"`，
永远匹配不到任何东西。这种 bug 靠人工点页面几乎不可能发现。

**限流也当场暴露一个设计错误**：原本设成 5 次/天/IP，
但国内运营商大量用 NAT，一个学校出口后面几十个群友共用一个 IP，
前 5 个人登录后其余全被锁死。已改成短窗口防爆刷 + 宽松日限。

---

## 六、还缺的（按重要性）

### 需要你决定的

**首页排行榜现在对未登录公开。** 任何人打开 https://agenticlab.sh 都能看到
群友昵称和发言数排名，不需要登录。这是个隐私决定，不是技术问题 —— 要不要挡在登录后？

### 我接着做的

| 缺口 | 风险 |
|---|---|
| **备份只存在同一块磁盘上** | 磁盘挂了备份跟着一起没。需要异地：对象存储或同步到你本地 |
| **健康数据没有告警通道** | 隧道断了库里有记录，但没人会知道。需要接微信/邮件推送 |
| **审计日志没有强制拦截层** | 现在靠调用方自觉，SCHEMA.md 里写明了「靠自觉一定会漏」 |
| **权限判定器还没有页面在用** | 写好了但所有页面仍是裸的，等做后台和论坛时接上 |
| 迁移前不自动备份 | schema 改错会直接毁数据 |
| 没有 CI | 提交不跑测试与 lint |
| 没有预发环境 | 直接改生产 |
| X-Forwarded-For 未校验可信代理 | 理论上可伪造 IP 绕限流；实际前面只有自己的 nginx，风险低 |

---

## 七、实测踩到的坑（都已修，记下来免得重犯）

| 坑 | 现象 | 修法 |
|---|---|---|
| 高质量消息定义 | 每人少算 3–35 条 | 穷举反推出 `{text, quote}`，`npm run calibrate` 可复验 |
| `/users/{id}/groups` 返回对象不是数组 | 成员判定全失败，所有人「不是社群成员」 | 修类型；**判定失败改为 unknown 而非 false** |
| SQL `max(sender_name)` | 站长在榜上显示成 wxid（字典序 w > j） | 建 people 表，用窗口函数取最近一条 |
| `limit=500` 超上限 | 头像同步静默拿到 0 个 | 客户端按端点钳制 limit + 失败不再静默 |
| trigram 分词器 | 「鉴权」「部署」等 2 字词查不到 | CJK 逐字切分 + unicode61 + 短语查询 |

共同教训：**上游返回的形态必须实测，不能照文档推测**；
以及**故障不能伪装成业务结果** —— 那个「你不是社群成员」的假象就是这么来的。
