# 这个仓库是怎么组织的

> **读者**：第一次接手这个项目的人，或者第一次读它的 AI
> **性质**：**现状**（不是打算）
> **最后核对**：2026-08-10，对着代码逐条核过

一份文档核对下来发现的最大缺口就是这一份：`src/lib` 底下五十来个目录，
文件名有明确的分工，但**没有任何一份文档讲过这套分工**。
于是每个新来的人都要自己从代码里反推一遍。

---

## 一、四类文件，各管一件事

一个功能通常摊成四个文件。这个分层不是风格偏好，是被两件事逼出来的：
**纯逻辑要能密集地测**，以及**权限判定必须只有一处**。

| 文件 | 干什么 | 硬规矩 |
|---|---|---|
| `*-rules.ts` | 纯函数。判定、计算、文案 | **不许 import `server-only` / `@/lib/db` / `drizzle-orm`**。有测试逐个盯着 |
| `queries.ts` / `*.ts` | 读数据库。带 `import "server-only"` | 时钟在这一层读完传下去 —— 渲染期读 `Date.now()` 过不了 React Compiler |
| `*-actions.ts` | Server Action | **文件里只能导出 async 函数**。导出一个同步函数会让整个构建挂掉 |
| `app/**/page.tsx` | 页面 | Server Component 往 Client Component **不能传函数**，传了就是线上 500 |

为什么规则层要禁 db：它是登录、权限、限流这些路径上的判定，
测试必须能一秒跑几百次。一旦引了数据库，测试就要起库、造数据，
于是没人会为一个边界情况多写一条 —— 而边界情况正是这些判定的全部内容。

---

## 二、三套开关，别用错

这是这个仓库最容易搞混的地方，也是**最容易造出「死开关」的地方**。

| | 在哪 | 管什么 | 谁改 |
|---|---|---|---|
| **系统设置** | `src/lib/settings/defaults.ts` + `settings` 表 | **数值与阈值**：每日积分上限、新人多少天内发外链被降权、限流参数 | 后台设置页。每次改写 `setting_history`，可回滚 |
| **功能开关** | `src/lib/flags/registry.ts` + `feature_flags` 表 | **整块功能的开关**：关掉之后导航入口消失、页面 404 | 后台开关页 |
| **模块** | `src/lib/modules/registry.ts` | **可插拔的玩法**（活动、雷达、每周精选）：关掉之后连后台任务都不跑 | 后台模块页 |

选用规则：**改一个数** → 设置；**整块藏起来** → 开关；
**一个能独立存在、有自己后台任务的玩法** → 模块。

### 死开关是这个项目的地方病

`flags/registry.ts` 顶上那段注释是全仓对这个病写得最好的一段，值得先读。

已经查出并修过的：`bookmark_folders` 整套、`subscriptions.target_type` 四个值里的三个、
`forum_drafts` 整张表、`scheduled_at`、`replies.parent_id`、`boards.view_mode`、
`feature_flags` 整套机制、`points_ledger` 全站视图、`points.recount` 权限、
`roles` 的四个字段、`moderation_actions.duration_seconds`、
`user_privacy` 整张表（四个隐私开关零读零写）、`activeAnnouncements()` 零调用点。

**识别方法**：一列 / 一个设置项 / 一个权限点，如果在 schema 或注册表之外
grep 不到任何读取点，它就是死的。而死开关比缺功能糟 ——
**缺的功能人看得出来，一个显示成「开」的开关会让人不再去想这件事**。

---

## 三、定时任务

四个 systemd timer，都在服务器上。**一轮里每一步都是隔开的**
（`src/lib/ops/tick.ts` 的 `runSteps`）—— 一步失败不影响其它步，
失败会被汇总报出来。要加一步就往对应脚本的步骤表里加一项。

| timer | 频率 | 一轮跑什么 |
|---|---|---|
| `agenticlab-sync` | 2 分钟 | 后台队列 · 群发投递 · 刷新群列表 · 同步消息 · 群成员名册 · 人员名录 |
| `agenticlab-health` | 5 分钟 | 存储快照 · 自动裁剪 · 置顶到期 · 定时发布 · 赛季结算 · 到期解封 · 身份组结算 · 称号结算 · **告警投递** |
| `agenticlab-backup` | 每日 04:00 | 本机快照（失败即退出）+ 异地推送与恢复演练（非致命） |
| `agenticlab-digest` | 每周一 09:00 | 每周精选 —— **只生成草稿，不发送** |

**告警投递必须排在 health 那一轮的最后一步**，而且它读的是**上一轮**
写下的健康状态：这一轮自己的结果它不可能知道。
把它排在前面的话，一次失败要等到下一轮才报，而下一轮它又在最前面。

---

## 四、后台写操作的必备三件套

一个改数据的后台操作，缺一不可：

1. `requireWritableAdmin("权限点")` —— **不是** `requireAdmin`。
   后者服务于页面读取，不挡预览态；用错的后果是**管理员以别人的身份写了数据，
   而审计日志记在被预览的人头上**
2. `audit(...)` —— 每一个后台写操作都要留痕，没有例外
3. 权限点要在 `src/lib/rbac/permissions.ts` 里注册，且**按危险等级**分级

有六组结构性测试盯着这些，写漏了会红：
`tests/preview-coverage.test.ts`（调了 requireAdmin 又写数据的一个都不许漏）、
`tests/audit-coverage.test.ts`、`tests/permission-*.test.ts` 等。

**取当前用户一律用 `getRealUser()`，不是 `getCurrentUser()`** ——
后者在预览态下返回**被预览的那个人**。这个坑已经踩到过三次
（GitHub 绑定、数据导出、图床上传），第四次就该是条件反射了。

---

## 五、几条不能破的线

这些不是配置项，是写死在代码里的约束。改它们之前先想清楚为什么它们在这儿。

- **群列表属于隐私**：未登录访客看不到任何群的信息，登录用户**只能看到自己所在的群**。
  一切群相关查询走 `src/lib/queries/visibility.ts` 收口
- **只有群成员能登录**。任何「用 X 就能注册进来」的路径都会把这条打穿 ——
  GitHub 绑定为此写了三层防线
- **网站绝不代用户发微信消息**。只有系统 / 管理员广播
- **微信 ID 不出现在任何对外界面上**。榜单对未登录访客公开，
  兜底显示名绝不能退化成 wx_id
- **搜索是最容易绕过权限的入口**：只要能搜到只言片语，私密内容就已经泄露了。
  所以可见性在 **SQL 层**就切掉，不是查出来再过滤
- **管理端不做限流、不做双人复核**（站长的原话：「我有数」），
  但**用户侧的限流一律保留**

---

## 六、术语

| 词 | 意思 |
|---|---|
| `users` vs `people` | `users` 是**站内账号**（能登录的）；`people` 是**微信那一侧的人**（群里的所有人，多数没有账号）。靠 `wx_id` 搭桥 |
| bound / 绑定 | 一个 `people` 认领了一个 `users` 账号 |
| member / external / guest | 账号的 kind。目前只有 member 这条路开着 |
| 高质量消息 | 榜单主排序用的口径。按总条数排会让复读机上榜 |
| 赛季 | 榜单的周期。总榜跑久了会冻住 —— 最早那批人永远在前面 |
| tier / 分层 | 消息的存储层级（热 / 冷 / 已裁剪），见存储裁剪 |

---

## 七、从哪读起

1. 这一份
2. `ECONOMY.md` —— **这批文档里质量最高的一份**，讲「为什么」的范本
3. `DEPLOY.md` —— 线上长什么样、出事怎么办
4. `LESSONS.md` —— 这个项目踩过的坑，尤其是「检查看起来在跑、其实什么都没拦住」那一类
5. 要动某一块时再读对应的 `FORUM.md` / `PERMISSIONS.md` / `MODULES.md`
6. `ROADMAP.md` 看还剩什么，`DONE.md` 看已经有什么

`AGENTS.md` / `CLAUDE.md` 是**给 AI 读的**，由 `next dev` 自动写回，
别往里塞给人看的内容。
