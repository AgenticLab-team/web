# 已经做完的

> **读者**：站长（验收）、执行者（别重复造）
> **性质**：**现状**台账，只进不出
> **最后核对**：2026-08-10，六组并行对着代码逐条核过
>
> 从 `BUILD_PLAN.md` 拆出来的。拆的理由：状态在同一份文档里有两个落点时，
> 必然分叉 —— 上一版核出 3 处同一件事两个位置状态相反，
> 以及 13 条标着「待办」其实早就上线了。
>
> **这里每一条都核过接线**（调用点 / 界面入口 / 定时任务挂载点），
> 而不是「代码里有个函数」。核不过的已经退回 `ROADMAP.md`，
> 其中最要紧的一条是**补签卡：买得到、用不掉**。

---

## 迭代 1 — Passkey 与登录体系
- WebAuthn 注册：绑定成功后在 /onboarding 引导设置
- WebAuthn 登录：discoverable credential，不必先报身份
- **多设备**：/me/登录与安全 里可绑定任意多把，逐个命名与移除
- 会话管理：设备列表、单个下线、全部下线
- 登录历史（含失败尝试），用户自己可见
- 计数器克隆检测（存量为 0 时不误判，否则 iPhone 用户全被挡）
- 登录限流（只算失败尝试，成功不消耗配额）
- 密码兜底 —— 用 **scrypt**（Node 自带）而不是 Argon2id：后者要装原生依赖，
  而这条路本来就是「Passkey 用不了时的备用钥匙」，为它引入编译期依赖不值。
  参数在生产两核机上实测 117ms
- **管理员强制 Passkey** —— 那个配置项在库里躺着、默认 true、设置页显示成开着的，
  而没有一行代码读它。现在真的读了：手里有任何 dangerLevel ≥ 2 权限的人
  （按权限判，不按角色名判）用密码登不进来。判定放在密码校验**之后**，
  否则「这个微信号是不是管理员」会变成一个免密查询接口。
  「开了会锁住谁」在设置页和 /api/health 上一直看得见

### 2.9 补充说明与论坛待办
- 编辑**回复**（schema 里 `replies.edit_count` 字段已在，action 和界面都没有）
- 版主折叠低质回复的界面入口（`moderateReply` 支持 collapse，没接）+ 折叠展示
- 楼中楼 / 树形视图切换（版块可设默认）—— 先把「回复某一楼」变得看得见，树才有东西可长
- 收藏夹分组（`bookmark_folders` 表已建，界面只有单一收藏）
- 关注作者 / 版块（`subscriptions.target_type` 已预留）；标签那一路的扇出也做了，
  **但生产上还一个标签都没有**，所以没做关注标签的按钮 —— 那会是又一个死开关
- 服务端草稿（换设备接着写；现在只有 IndexedDB/localStorage 本地草稿）
- 定时发布（`scheduled_at` 字段已在）—— 挂在五分钟一轮的 health tick 上
- 帖内跳转到未读第一条 + 阅读进度条
- 楼主锁定自己的帖子（FORUM.md 4.3 允许，现在只有版主能锁）—— 楼主只解得开自己加的那把

## 迭代 3 — 积分 · 打卡 · 等级
- 打卡（当日高质量 ≥ N 才可打，先有贡献再签到）
- 积分规则引擎（纯函数，全部数值读 settings，零魔法数字）
- 连胜（断签重置、奖励封顶）
- 等级 L1–L10，按**累计获得**算 —— 花积分不掉级
- `points_ledger` 冲正机制（绝不改原记录）
- 反作弊：同分钟折叠、近似内容去重、异常进人工复核队列
- 积分只由服务端发放，前端无任何加分入口
- 登录门禁中间件（含开放重定向防护）

## 迭代 4 — 群消息检索
- 搜索页：跨群 / 按人 / 按时间 / 只搜自己
- **权限收口**：SQL 层就限定可见群，不是查出来再过滤
- 结果高亮 + **就地展开前后文**（不跳走，保住滚动位置）
- 时间机器：按天回看任意一天，日期导航常驻顶部
- 我的存档：只搜自己说过的话

### 5.1 总览仪表盘
- DAU/WAU/MAU、新增绑定、消息量与高质量占比
- 积分发放、打卡、论坛指标
- **系统健康**：各组件状态、同步失败数、探测是否停摆
- 磁盘水位、近 14 天消息趋势
- **待办放最前**：举报/申诉/积分异常，每个数字都指向一个动作

### 5.2 用户管理
- 搜索与多条件筛选（站内昵称/微信昵称/wxid/邮箱/账号 id 都能搜）
- 用户详情：档案 / 所在群 / 积分流水 / 登录日志 / 设备与 Passkey /
  论坛行为 / 处罚记录 / 管理操作历史 / 生效权限（带来源）
- 操作：改角色、调整积分（必填理由）、封禁解封、强制下线
- 管理员备注（用户不可见）
- **绑定审批队列**：好友申请列表 + 申请人活跃度，手动绑定或作废。
  审批的唯一依据是「他是不是真的在我们群里」—— 手动绑定**硬性要求**
  那个微信号在同步的群里，没有例外（留了例外，「只有群成员能登录」
  就退化成一个默认值）。「卡住的人」按 IP 聚合、门槛量出来定在
  取过 ≥2 次码且一直没成功

### 5.3 身份组与权限
- **权限矩阵**：行=身份组 × 列=权限点，三态（允许/显式拒绝/未授予）
- **权限反查**：选一个权限点，列出所有持有者及来源
- 范围化授权与临时提权（到期自动失效，反查里不再计入）
- **矩阵在线编辑 + 保存前 diff 预览**（「失去 1 项、获得 3 项，影响 4 人」）。
  影响面**按人算**不按身份组人数算 —— 预演的办法是在事务里真改一遍、
  调同一个 can.ts 量完、再回滚,所以预览和现实之间没有第二份实现。
  护栏:不能授予自己没有的权限（否则管理员编辑「管理员」组就能绕开
  ADMIN_DENIES）、不能动优先级比自己高的组、不能把 role.manage 摘光
  （那是把门从里面锁上再把钥匙扔了）、必填理由、认不出的权限点直接拒绝
- **以某身份预览**：整站切成他的视角，常驻红色横幅。**权限只减不增**
  （生效的是「他的 ∩ 我的」，扣掉哪几项写在横幅里）、**全程只读**
  （判定照实回答所以按钮该出现就出现，真去点时拦下）、30 分钟过期、
  进出与掐断都进审计。只给站长，因为切成普通成员会看到他的私人视角。
- 自定义身份组（颜色/图标/优先级/名额上限/自动授予规则）—— 带危险权限的组**不许**配自动授予
- **权限矩阵版本快照与一键回滚**。快照拍的是**那次改动之前**的整张表,
  所以第一次编辑会顺手留下原始状态,不用另外种基线。回滚遍历
  **快照与当前的并集** —— 只看快照的话,快照之后新增的越权授权
  会留在原地,而回滚报告成功。回滚走同一套护栏（否则它就是绕开
  提权检查的近路）,并且自己也进历史。留最近 50 张

### 5.4 内容审核（含论坛全套）
- **举报队列**：按目标归组、严重度 + 先来先处理排序、超时标红、认领、结案
- **申诉流程** —— 有处罚就必须有申诉，否则只会积累怨气
- 处罚记录聚合到用户档案（5.2 已做）
- **版块管理**：增删改、可见性三档配置、层级、排序、锁定
- **标签管理**：合并、重命名、锁定、清理无用标签
- **敏感词库与自动拦截规则**：三档处置 + 归一化匹配 + 预览器
- **版主任免**（限定版块，默认带到期时间）
- **可见性提升审核队列**（群聊转帖想让更多人看到时走这里）
- **帖子/回复管理、批量操作**

### 5.5 积分与等级配置 —— 经济看板已完成
- **积分经济看板** `/admin/points`：通胀体检、发行/回收去向、
  分配集中度、每日上限的松紧、发行最多的人
- 所有发行参数进 settings（每日上限、互动权重折算、连胜上限、手续费）
- 手动发放/扣除（必填理由，大额需额外权限，见 5.2）
- 等级门槛与解锁权限的后台编辑
- 积分流水全站视图、风控队列

### 5.5b 称号
- 称号表 + 用户持有表 + 佩戴（`users.activeTitleId`）
- 四种来源：授予 / 成就 / 购买 / 赛季；四档稀有度
- 名额上限、租期与续费、到期与撤销分开
- 个人页称号架（点一下佩戴，再点摘下，过期的变灰仍陈列）
- 后台授予/收回（下拉里直接标出剩余名额）
- `npm run grant-title` 批量授予，默认可 `--dry-run` 预演

### 5.5c 旧的积分配置项
- 等级门槛与解锁权限
- 积分流水全站视图、风控队列
- **积分**重算（对账修复，先 preview 再执行）—— 榜单是实时算的，没有可重算的物化表

### 5.6 群与数据源
- 群列表、bound 状态、每群配置（quality_min 覆盖、是否计分、榜单是否公开、
  保留天数、排除同步）
- 手动触发同步（排队 + 消费者）、同步日志与失败重试、一键重试全部
- **每群同步健康判定**：相对于该群自己的节奏，而不是统一阈值
- 增量游标视图

### 5.7 活动与模块 ✅（模块管理界面待做）
- **核心表**：activities / applications / events / quota_log
- **资格引擎**：声明式规则 + 可解释的判定结果
- **状态机**：活动与申请的合法流转、名额占用语义
- **名额并发安全**：带条件的单条 UPDATE，永不超卖 + 可对账
- **模块①域名发放**：表单、校验、RDAP 可用性查询
- **活动管理界面**：创建、规则编辑器（实时预估人数）、审批、履约回填、
  待注册清单导出
- **用户侧申请界面**：够不够格、差多少、已登记什么、到哪一步了

### 5.8 商店与订单
- 商品上下架、库存、限购、兑换记录
- 实物订单发货状态流转（每次流转留痕并通知用户）
- 退款与积分冲正（同时归还库存）
- 虚拟商品自动交付：称号、补签卡

### 5.9 公告与推送 ✅（微信群发）
- 站内公告（横幅/弹窗/站内信）
- **微信群发**：内容冻结 + 双人复核 + 频率上限 + 额度检查 +
  逐群留痕 + 逐条撤回

### 5.10 审计与系统
- 审计日志检索（按动作/危险等级/时间）+ 变更前后快照，**无删除接口**
- **系统设置**：写入侧校验、变更历史、回滚、恢复默认、改过的项标出来
- **危险操作双人复核队列**：注册表式动作 + 24 小时有效期 + 执行前重校验
- 功能开关的后台界面（feature_flags 表已就位）—— 开关此前**零调用点**，接线才是重点
- 长任务管理（存储裁剪、积分重算，先 preview 再执行）—— 都走同一张 admin_tasks

### 5.11 邀请体系
- 邀请码生成（限次/限时/绑定身份类型），码里不含形近字符
- 邀请树：追溯上游与下游，成环时不会转死
- **邀请奖励延迟发放 + 被封时回滚**

## 迭代 7 — 打磨
- 成员目录 + 技能标签
  - **只收录注册用户**：群成员表里有一千八百人，目录里只有二十几个。
  出现在群成员表里是微信群的事实，出现在可按技能检索的网页上
  是这个站替他做的公开 —— 他没同意过
  - **只显示和你同群的人**，且只说共同群的**数量**不说是哪个 ——
  说了等于把群名泄露给另一个群的人
  - 标签存 slug + label 两份：RAG/rag/ＲＡＧ/「大 模 型」归一成同一个。
  碎掉的标签目录等于没有目录 —— 每项一个人，谁也找不到谁
  - 显示用**最多人用的那个写法**，不是第一个人的写法
  - 只有一个人的标签不进筛选栏 —— 点进去看到一个人，是噪音
  - 隐身开关：隐身后**自己那一行还在**（标「仅自己可见」），
  否则用户没有任何办法确认开关生效，只能靠相信
  - 页面直说收录了几个、几个隐身、几个没填标签 ——
  一个说得出自己有多空的目录，比看起来很满的有用
  - wx_id 不进返回结构（只传配色下标）—— 它会被序列化进网页源码
  - 顺手修掉：`/members` `/shop` `/search` 在导航里标了 requiresAuth
  却不在中间件名单里，访客拿到的是 200 空壳。加了一条断言把
  「导航要求登录」和「中间件拦下」绑在一起
- 链接资源库
  - 从聊天记录自动收录。**URL 抽取必须在第一个 CJK 字符处停下** ——
  真实数据里 `https://cloud.siliconflow.cn/i/Ex4mpl3Ab现在硅基流动注册…`
  用 `\S+` 抓到的是一个看起来完全正常、点进去 404 的地址
  - 去重键抹掉 www./协议/末尾斜杠/追踪参数（utm_*、spm、from、share_* 等），
  展示地址保留原样 —— 同一个东西出现五次是资源库最没用的样子
  - 说明取自**分享它的那条消息**，不去抓网页标题：
  「可以查实时台风情报」比 `<title>台风网</title>` 有用，
  而且不用发出站请求、不用防 SSRF。抓不到就留空，不编
  - 可见性同成员目录：只看得到自己群里分享过的，不显示来自哪个群，
  **连「被分享 N 次」都只数你看得到的那些次**（全站次数会泄露别处的热度）
  - 收藏前判一次可见性 —— 收藏不能成为绕过可见性的后门
  - 幂等：回填反复跑，计数从明细现算不做 +1；`--audit` 对账，`--retitle` 重算标题
  - 生产回填：42k 条消息 → 198 条链接、117 个域名；二次运行 0 新增、对账通过
- 关键词雷达
  - **只在订阅者自己所在的群里匹配** —— 否则这不是雷达，
  是一个能监听任意群的工具。可见性判定放在匹配**之前**：
  反过来写也能得到同样结果，但那样漏一次过滤就是一次静默泄露
  - 中英文词边界分开处理：ASCII 卡词边界（订阅「AI」不被 said/chain 命中），
  CJK 走子串（否则中文一条都命中不了）。同一个规则套两种语言必有一种是坏的
  - 每个词每天最多提醒 5 次、十分钟内不重复；**跨天重置**
  （不重置的话一次热闹会让订阅永远失效）。被压掉的命中照样留痕并
  在列表上标「未通知」—— 少通知是有意的，瞒着不说不是
  - 订阅前先算「过去七天在你的群里响多少次」，太泛的默认拦下。
  阈值拿生产数据校准过：AI 343 次/7d 拦下，claude 67、Agent 91 放行
  - 「一天」走 `dateKey`（东八区），和签到连胜同一个边界 ——
  原本用服务器本地时区，在 PDT 的机器上会差整整一天
  - 幂等：同一条消息重扫不重复计数（唯一索引 + 累计数不 +1 两次）
- 通知中心（补齐：偏好开关 + 筛选页签）
  - `notification_prefs` 这张表之前**一行代码都没读过** —— 表建好了、
  字段设计好了，而 notify() 根本不看它。现在真的接上了
  - 开关在**写入侧**生效：关掉的类型不产生那一行，而不是产生了再在
  读的时候过滤 —— 后者要让未读数、红点、聚合计数全都记住
  「这条对这个人不算数」，漏一处就是个点不掉的红点
  - **处罚与系统公告关不掉**：让人能把对自己不利的消息静音，
  等于让所有没收到的消息都不再说明什么。前端禁用 + 服务端再判一次
  - 筛选在 SQL 里做，不是取一页回来再过滤（否则「@ 我」会因为
  最近 50 条里没有 @ 而显示空）；页签上带条数
  - 邮件通道没接，所以界面上**不放**邮件开关 ——
  一个打开了却什么都不会发生的开关比没有它更糟
- 存储分层裁剪任务
  - 三步按「删错了能不能救回来」排序：改层（零风险）→ 退索引（可逆，
  `reindexRange` 真的能恢复）→ 丢正文（**不可逆**）
  - **「本地是缓存、需要时回源」这个前提被验证了，结论是今天证明不了**：
  上游自己也只有约两个月历史。所以默认走归档（gzip NDJSON，按月分包），
  关掉归档时 `verifyUpstreamRetention()` 会抽样回查，验不过整步跳过
  - 自动触发只做可逆的两步 —— 永久删聊天记录该有个人按确认，
  而不是凌晨三点由 cron 悄悄完成；自动跑过也落 admin_tasks + 审计
  - 预览与执行之间隔一个 admin_tasks 行：确认的是**看到的那个数字**，
  不是执行时重新算出来的另一个
  - 按天回看/群聊沉淀会报出「这天有 N 条正文已归档」——
  裁剪过的一天和冷清的一天在结果里长得一模一样
- 异地备份
  - 自己实现 SigV4（服务器上没有 aws-cli / rclone），正确性用 AWS 官方
  `get-vanilla` 测试向量锁住 —— 签名算错的表现是 403，
  而 403 很容易被读成「密钥填错了」
  - 兼容任何 S3 协议的对象存储：R2 / B2 / MinIO
  - **传完立刻读回来对哈希** —— PUT 返回 200 只证明请求没报错，
  对象被截断、写进别的桶、被静默丢弃都不会让上传这一步失败
  - **恢复演练**：从远端下载最新那份、解压、打开、数行。
  本地那份能打开完全不能说明传上去的能打开；
  「能打开但是空的」同样会通过完整性检查，所以还要看行数
  - 备份脚本备完就推异地 —— 拆成两个定时任务的话，
  第二个挂掉之后第一个还在每天报成功
  - **没配置报 degraded 而不是 ok**：不该因为「本来就没开」显示成正常。
  告警线定成 24 小时报一次、30 天重提醒 —— 这是「一直缺着」型的问题，
  按分钟级去报只会让人把整个通道静音
  - 归档文件也一起传：它是冷层正文的**唯一**副本，
  比数据库备份更不能只放在一块磁盘上
  - ⚠️ **生产上还没配对象存储**，配好四个环境变量即可生效
- 告警通道（隧道断了要有人知道）
  - 抑制优先：连续挂够久才报、一次故障只报一次、恢复要收尾
  - `frp_tunnel` + `upstream_api` 合成一个告警组件 —— 隧道一断，
  `upstream_api` 那一行会永远停在最后一次「正常」上
  - **上游挂了微信发不出去**（报信的和出事的是同一个）：
  告警照样落库，`notify_error` 如实记下发不出去的原因
  - 投递失败按 `retry_after` 重试，不用重提醒的节奏等 ——
  一次失败换来一小时沉默，而沉默和「一切正常」长得一样
  - 后台 `/admin/health`：先答「有人会知道吗」，再答「现在好不好」
- 无障碍走查 + 触控区
  - **做成可执行的规则跟着 `npm test` 跑**，不是一次性走查 ——
  一次性走查会衰减：这周改干净了，下周新加一个图标按钮又没有
  aria-label，而没有人会再走查第二遍
  - 七条规则：图标按钮/链接缺名字、img 缺 alt、role=switch 缺
  aria-checked、输入框缺名字、_blank 缺 noopener、装饰图标缺
  aria-hidden、图标按钮触控区不足 44px
  - 扫描器的取舍是**宁可漏报不可误报**：会误报的检查第一次挡住
  正常提交时就会被加豁免注释，第二次就会被删掉。
  第一版报了 53 条，其中 47 条是误报（`{tab.label}` 这种运行时
  才确定的名字、`<Field label>` 包裹的输入框），收窄之后 6 条全是真的
  - `.tap-target` 用伪元素把可点范围撑到 44×44，**视觉尺寸不变** ——
  改 padding 会把排版顶开，于是这件事永远排不上优先级；
  指针设备上关掉，鼠标本来就点得准
  - 走查结果：全站 0 条未修复；焦点可见、减少动效、sr-only、
  跳到正文链接（`#main` 锚点真的存在）都有测试盯着
- 性能守卫
  - **N+1 是唯一一种「不会被发现」的 bug**：不报错、不崩、测试全绿，
  二十条数据看不出来，两千条开始转圈，而没有哪一刻能指着说是这次
  改动带来的。所以给每个列表查询定查询条数预算，数据翻十倍再跑一遍，
  **条数不许涨**。故意造了一次 N+1 验证过这个守卫真的会红
  - `EXPLAIN QUERY PLAN` 断言 11 条热点查询不在大表上全表扫描。
  当场查出**排行榜在 daily_stats 上是 SCAN**（访问量最大的一页），
  补了 `(conv_id, date)` 索引，生产上确认 SCAN → SEARCH
  - deploy 加首屏 JS 预算，量的是**压缩后**的字节 ——
  同一批 chunk 未压缩 362 KB、压缩后 109 KB，
  按前者定预算等于按一个没有人经历过的数字做决定。当前 109/160 KB

### 已补齐的遗留项
- 赛季积分
  - **赛季只重置排名，不重置积分**。表里没有任何积分列 ——
  有的话就是又一个会和 points_ledger 对不上的冗余计数器；
  赛季榜是按日期范围现算 daily_stats
  - 排行榜**默认就是本赛季**：不是默认那一屏的话，这件事不成立
  - 边界按东八区、按季度切，四个赛季首尾相接不留缝
  - 结算冻结名次 —— daily_stats 会被存储裁剪动到，
  而「2026 秋季赛冠军是谁」一旦发生就不该再变
  - 只发前三，且要过最低参与门槛：没人参与的赛季发称号只会让称号贬值
  - 榜上但没注册过的人：名次照样冻结，称号发不了（他确实是第一名，
  只是站上还没有能给他挂东西的地方）
  - 连任顺延到期日而不是插新记录；并列第一时两个人都是冠军
  - **两条测试直接读源码**，确认赛季规则与结算里不出现
  `grantPoints` / `pointsLedger`
- 密码兜底登录
  - **它是兜底不是第二条正门**：只有已经登录的人才能设置密码。
  这个站的入口只有微信群里那条验证码 —— 密码要是能独立进门，
  「只有群成员能登录」当场就没了
  - 解决的是一个具体问题：Passkey 换设备就用不了，
  而验证码要靠群猫娘发得出来 —— 两条同时不通时，
  密码是唯一还能进来的钥匙
  - **不能创建账号，也不能激活 pending 账号**
  - 「没有这个人」「没设过密码」「密码不对」措辞完全一致，
  **而且耗时一致**（查不到人时也照样算一次 scrypt）——
  否则就送了一个查询群成员名单的接口
  - 账号状态检查放在密码校验**之后**：放前面等于告诉任何人
  某个微信号在这个社群里且被封过
  - 账号级锁定（5 次 / 15 分钟），IP 限流挡不住换 IP 爆破同一个人；
  锁定**有时限** —— 永久锁定等于谁都能把别人锁死
  - 用 Node 标准库的 scrypt 而不是第三方 argon2：
  认证是最不该依赖三方包的地方。参数随哈希一起存，
  登录成功时顺手升级老哈希
- 置顶位实际生效逻辑
  - `deliver()` 里**没有 highlight 分支**，落到最后一行的
  「无需自动交付」；而 highlight 属于即时交付，订单会直接标成
  fulfilled、提示「已到账」—— 五百分扣了，帖子一动没动。
  （商品一直是下架状态，所以从没有人真的被扣过分）
  - `pinned_until` 这一列**从来没有人读**：排序只看 `pinned` 布尔，
  一次「置顶一天」等于置顶到天荒地老，而且看不出来
  - 现在排序、列表标记、后台清理统一走 `isEffectivelyPinned`
  - **付费置顶有名额**（每版块 1 个）：不限的话第一屏迟早全是买来的，
  那时候论坛卖掉的不是曝光，是别人被埋掉的机会
  - 目标校验全在**扣分之前**：帖子不存在 / 已删 / 不是自己的 /
  名额被占，一律当场拒绝，不收钱排队
  - 只能给自己的帖子买 —— 否则就是花钱把别人的帖子顶上去
  - 到期清理任务把标记清掉并通知买的人：花五百分买的东西
  悄无声息地消失，下次他不会再买，而且会怀疑上次到底有没有生效
- 成就型称号自动授予 + 租用型称号到期结算
  - `meetsCondition` / `achievementStats` / `renewalExpiry` / `isTitleExpired`
  四个函数写好了、有测试、看起来很完整 —— 而**没有任何地方调用它们**。
  builtin 里那五个成就称号谁也拿不到，而称号架的空状态还写着
  「连续打卡、在论坛发帖和回复都会解锁」
  - 结算挂在健康探测那一轮里（5 分钟），不另开 timer ——
  又一个 timer 就是又一个可能悄悄挂掉的东西
  - **自动续费默认关，购买流程也不替人打开**：默认开着的自动续费
  会在某人早就不用它的时候每月悄悄扣三百分，而积分是这个站里
  唯一的硬通货
  - 分不够就让它过期，**不扣成负数** —— 那等于让人背上一笔
  没同意过的债，而下次签到的分会先去还债，他会以为签到没生效
  - 到期前 3 天提醒一次（只一次）；到期摘下但**不删记录**，
  「我曾经拿到过」也是履历
  - 生产上跑过：5 个发过帖的人自动拿到「开了个头」并收到通知，
  再跑一轮 0 新增

### 已完成并上线
- **后台分页** —— 统一分页组件（零客户端 JS）+ 纯逻辑分页器。普查出 16 处
  静默截断,其中 3 处是真 bug:商店副标题拿截断后的 `rows.length` 冒充订单总数、
  邀请页「已邀请 N 人」同样、滥用告警只统计前 50 条。所有排序补了 id 末位键
  成全序（并列多时半序翻页会丢行/重复行）
- **域名活动批量注册与回填** —— 导出一行一个可直接粘进注册商；回填解析器
  宽容（全角逗号/URL 包裹/大小写/各种分隔符）但**三处不猜**：认不出的状态词
  报错而非默认成功、同一域名两行矛盾时两行都不执行、「系统里没这个申请」单独红色列出
- **帖子页行内操作** —— 用户改自己的、版主/管理员快捷操作（加精/置顶/锁帖/
  移动版块/删除/恢复），统一操作菜单，移动端底部弹层。顺带接上了四处
  「写了但全站没人调用」的代码：`moderatePost`（318 行版主工具）、
  `deleteMyPost`/`restoreMyPost`、`editPost`（连编辑页都不存在）
- **群消息的 @提及** —— 写时定人、读时定名（昵称会变，所以存 wx_id 不存昵称）。
  3,332 个提及认出 2,622（79%），歧义 15 标出候选、认不出 600 标 unknown，**绝不猜**
- **移除管理端限速与强制复核** —— 好友申请额度改成纯提示、危险配置改成红色警告、
  复核队列降级为可选留痕。**没松的三条**：动作注册表（防任意代码执行）、
  24 小时过期（防执行旧 payload）、可见性提升要原作者同意（保护群里说话的人）
- **账号可以没有密码** —— 新增「决定不设」状态，和「还没设」区分开
- **登录态 5 分钟恢复** —— 断线回来接回原来的码；已匹配的码按匹配时间另开 5 分钟窗
- **LLM 接入骨架** —— 对话（DeepSeek）与嵌入（自建 Qwen3-Embedding-0.6B）
  分两套配置，实测 DeepSeek 没有 embeddings 接口

### 正在做
- **资源库 AI 整理** —— 200 条链接里 58 条标题就是域名。把链接连同上下文交给模型
  生成标题简介，**但不许编**：看不出来就留空
- **语义聊天记录搜索** —— 按会话窗口嵌入而不是按单条消息（数过：一半的群消息
  不超过 8 个字，「哈哈」「好的」单独嵌入没有可检索性）。3,394 段全部嵌完，
  门槛 0.55 是量出来的：噪音最高 52%、信号最低 57%
- **后台 LLM 设置项与可用性检测**（/admin/llm，每次打开真的各调一次）
- **资源可以点赞** —— 和收藏分开（私人书签 vs 公开信号），计数从明细重算

### 还没开始
- **emoji 换成 SVG** —— 分清了三类：内容里的表情反应（不动）、正文里的 `→`（是标点）、
  当图标用的（换掉）。权限矩阵那三格原本就是 lucide，是我改成编辑器时换成 ✓✗− 的
- **UI 布局重构** —— 逐页普查出 13 类割裂，按出现次数修。卡片配方手拼 36 处、
  Pill 行 11 处、提示横幅 15 处，全部收敛到基础构件。密集页面放宽到 78rem。
  顺带修掉 `var(--hairline)` 这个**从来没定义过**的变量（35 处引用）
- **手机端够得着** —— 之前底部 tab 栏 5 格全是目的地，剩下 7 个板块
  （通知、资源库、活动、成员、雷达、商店、**整个后台**）在手机上
  **根本没有入口**。第 5 格改成「更多」，里面用减法列出所有不在 tab 栏的 ——
  新页面自动进得去，不需要有人记得改第二份清单。后台那 24 个入口
  在手机上收进一行选择器（原来整份目录堆在每个页面的正文上面）
- **分享（两端都有）** —— 转发链接、分享文案、帖子转发图、聊天记录图。
  链接靠权限收口所以文案可以随便转；**图片跑出去就收不回来**，
  所以草稿/已删除/私密一律不生成，群聊图上**永远不出现群名**
  （路由根本不 import groups 表），并且生成即记审计。
  剪贴板在微信 webview 里可能不可用 —— 失败时摊开文本让人长按全选，
  不装作成功
- **「整理成帖子」页加关键词搜索** —— 复用检索页那套 FTS（分词、转义都在
  `lib/db/fts.ts` 里踩过坑了）。原来只能选群 + 选日期，而人记得的是内容不是日期
- **投票能建了** —— `createPoll` 原来一个调用点都没有。发帖时顺带建，
  和帖子在同一个事务里，不会留下「类型是投票但没有投票」的半成品
- **bug 反馈** —— 新增 `feedback` 版块（公开可见、登录可发）。不做工单系统:
  版块比工单好的地方恰恰在于**公开** —— 别人报过的你看得见，不会再报一遍
- **申请加入社群** —— `/join`，全站唯一未登录可写的入口。
  **对所有情况只回一句一模一样的话**:任何「你已经是成员了」「你已经申请过了」
  都在回答陌生人不该能问的问题，一个个试就能把成员名单摸出来。
  判断（在不在群里、有没有账号）全部留在管理员那一侧。按 IP 限流
- **即时推送通知** —— 站内 SSE（服务端轮询数据库，因为通知不只有 Web 进程在写）
  + Web Push（RFC 8291 用 node:crypto 自实现，官方测试向量锁死）。
  断线补漏两级：连接被掐用 Last-Event-ID，整页被杀用 localStorage 游标
- `/archive` 与 `/forum/convert` 的「翻天」合成一个 `DayNav`。
  顺带补上**直接跳日期** —— 原来两边都只能 ±1 天，回到上个月要点三十下

### 登录名与手机号（已完成）
- 自定义登录名（`users.username`，唯一、存小写、保留词、挡抢微信 ID）
- 手机号当登录名（`users.phone`，**未验证**：不能找回账号、不显示、不可搜）
- 邮箱登录（`users.email` 早就在库里，一直没接）
- 登录第一个框改收四种标识，撞车时微信 ID 优先

### 正在被人踩的 bug
- ⚠ **通知重复弹出** —— 比「两条补漏路径不认账」更基础：
  **底部那张吐司卡片是个光秃秃的 `<Link>`，点它根本不标已读**；
  而补漏回放的去重靠的是一个活不过刷新的内存 Set，
  服务端 `listSince` 又完全不看 `readAt`。两者相乘 = 刷几次弹几次。
  真值只能有一份，就是服务端的 `readAt`
- ⚠ **转发图有 bug** —— 对话超过 12 条一律 500。satori 的规矩是
  `<div>` 子节点多于一个又没写 display 就抛，而那行「…前面还有 N 条」
  正好三个子节点，且**只有裁剪时才走到** —— 所以短对话一直是好的，
  越长越值得转的对话越转不出去
- ⚠ **「值得看的发言」有问题** —— 那条「昨天群里有 N 条值得看的发言」
  的链接指向 `/messages`，而这个站根本没有这个路由。首页最上面
  唯一「让人明天还会回来」的内容，第一个数字点开是 404
- ⚠ **历史消息那一整块「特别弱智」**，四条全做完了：
- 消息列被挤成三行 —— 一条消息占三行，一屏看不到几条
- 应该按时间**倒序**
- 手机端没法快速滑动去引用某条消息 —— 查下去是**三个 bug 叠一起**，
  其中两个在电脑上测永远是好的
- 谁 @ 了我，点进去只跳到那一页，还要自己在一页里找 ——
  要能**直接定位到那一条**
- ⚠ **502 时间太长、太频繁** —— 蓝绿两份构建目录 + 两个端口，
  建的永远是没在服务的那一边，建好起好自检过了才改 nginx upstream 并
  reload。**长的那段本来就不是重启**：在线上直接 build 会把正在跑的
  进程脚下的 `.next` 换掉，那一段一分钟以上。回滚不用重新构建。
  ⚠ 安装脚本还没跑成 —— 服务器当时正好第二次掉线

### 新做的
- **导出自己的全部数据为 zip**。「自己的」划成五条：自己发的话全是自己的
  （含已退群的）；上下文只给他此刻在站内本来就读得到的（**导出不新增
  任何可见性**）；上下文里的人假名化、正文原样（身份标识符是拿去
  连接别的数据集的钥匙，正文只是文本）；群名只给看得见的群；
  **没有任何「导出某某人」的参数**。用 `getRealUser()` —— 预览态下
  `getCurrentUser()` 返回被预览的人，管理员点一下就把别人的记录带走了
- **导出域名清单**（站长 8-09 追加）。原来那个「导出」只有裸域名、
  只能复制到剪贴板，答不上「这个域名是谁的」。现在是能存下来、
  能用表格打开、能对账的 CSV。**昵称是用户自己填的而 Excel 会把它
  当公式执行**，所以要防注入；**没有 BOM 中文会变乱码**；
  **文件里不放微信 ID** —— CSV 落到本地之后就不受这套权限管了
- **绑定 GitHub**，并且把 GitHub 生态接进来：
- 用户主页展示自己的 GitHub 项目（带展示开关和缓存）
- 个人主页提示「有新项目 / 新 PR，要不要发帖分享」——
  「提示过了」不是一个状态位，**是那一行的存在本身**，
  靠状态位的话任何一次漏改都会让提示复活
- 开源准备（README / CONTRIBUTING / .env.example / 全仓敏感信息扫描）

## 站长 2026-08-10 追加
- **图床**（`files.mrusercontent.com`）。编辑器里三条路：按钮、
  **粘贴**（截图后 ⌘V）、拖入。做在共用的 `Editor` 上，
  发帖和回复一起有了。线上要配 `UPLOAD_API_KEY` ——
  不配走访客通道，而那是**按 IP** 限流的，我们从服务器代传，
  全站共用一个出口 IP
- **顺带堵上：帖子里的图可以当追踪像素用**。`img` 早是放行标签而
  `src` 没有白名单 —— 任何人都能放一张指向自己服务器的图，
  每个读者的浏览器都会去请求它一次。一张 1×1 的透明图就够把
  「谁在什么时候读了这篇」连 IP 送过去。现在只放行自己的图床和头像，
  别的降级成链接（不是删掉 —— 把「要不要去访问」还给读者）
- **关键词雷达漏接了隐私开关**（对抗性审计查出来的）。
  开关接线时认的是「四个检索出口」，而雷达是第五个。
  一个关掉开关的人一开口，昵称和高亮片段会被**主动推送**给
  同群订阅者 —— 比被搜到更进一步
- **源站只对 Cloudflare 开门**（站长换了公网 IP 之后）。
  顺带堵上 `*:3000`（Next 直接对公网可达，绕过 nginx 和 CF），
  并修好「客户端可以伪造 X-Forwarded-For」——
  那让按 IP 限流当场失效、审计日志可以随便伪造

## 站长 2026-08-10 追加（第二批）

> 原话：「我希望安装桌面端 app 和激活通知这两件事情用和 passkey 一样的
> 形式出现在首页 不过你要想想怎么不让用户反感 以及一次显示 3 个可能会
> 挡住正经内容」／「把设备推送的开启按钮放到通知设置的第一个」／
> 「你现在一行两个按钮一大一小看着好丑」

- **首页三件提示并成一个位子，一次只出一个**（`lib/nudges/rules.ts`）。
  站长自己点出了那个坑：三张卡摞在首页上，头一屏全是「你还没做这个」——
  而人打开首页是来看社区发生了什么的。更麻烦的是它们**互相稀释**：
  三条同时在，每条都变成背景噪音，结果三件事一件都不会做
- **顺序按依赖排，不按重要性排**。iOS 上只有加到主屏的网站才收得到推送，
  所以那种设备上「装到主屏」必须排在「开推送」前面 —— 反过来的话，
  人点「开启推送」会撞上一个**在这台设备上做不到**的按钮，
  而那一次失败会让他再也不试第二回。Passkey 排第一，
  它是唯一一件关系到「还进不进得来」的事
- **表过态之后整块安静三天**。点掉一张下一张立刻顶上来是打地鼠，
  是让人学会一见到这块区域就划过去最快的办法。
  安静期**覆盖全部三种**，不只是刚点掉的那一种
- **同一件事不在两个地方各提一次** —— 通知页上那张单独的 `PushNudge`
  删了，并进首页这一个位子。同一个提醒重复出现是让人厌烦最快的办法
- **设备推送开关挪到通知设置第一个**（站长直接要求）。
  下面每一行「推送到设备」都得先有这台设备订阅了才谈得上 ——
  把它排在一堆开关后面，等于让人先拨十个不生效的开关
- **按钮不再一大一小**（`components/home/NudgeCard.tsx`）。
  原来是「一个实心大按钮 + 一个 32px 的 ×」，两个问题：
  一大一小**在用尺寸替人做决定**，而这三件事全是可选的；
  32px 低于 44px 触摸下限，拇指按下去有一半概率落空，
  **而落空的那一半会点到旁边那个实心按钮上** ——
  想关掉它的人反而被推着往前走了一步。
  现在并排等高（`min-h-11`）、同字号，主次**只靠颜色**区分

## GitHub 生态接入（站长 8-10：「我希望咱们生态可以高度接入」）

- **资源库里的 GitHub 链接直接问 GitHub，不再问模型**。资源库原来把
  链接和群里的上下文丢给模型让它猜「这是什么」；而 GitHub 的接口
  **直接就会回答**，并且是权威的。省一次调用、一次往返，
  最要紧的是 —— **模型会编**，而一个语气笃定却说错了的简介比空着更坏，
  因为没有人会去核对。线上 36 条 GitHub 链接一轮问出 31 条
- **落在 `fact_*` 而不是 `ai_*`**，界面上标「来自 GitHub」而不是「AI 整理」。
  两种都标成机器写的话那句提示就废了：人看见它出现在一条明显准确的
  条目上，下次真正机器写的那条出现时也不会再当回事
- **解析层是安全边界，而且只有它一处**（`lib/github/link-refs.ts`）。
  认出来之后我们会给这条链接**盖上自己的章**，读者信的是那张卡片、
  不是那条链接 —— 所以 `github.com.evil.com` / `evil.com/github.com/…` /
  `github.com@evil.com` 一律不认，域名走**全等**比对。
  代码链接只认带 40 位 sha 的：分支名指向的内容会在帖子底下悄悄变，
  而讨论还停在旧代码上
- **顺带查出两个「显示了一个错的东西」**：
  · `/issues/1e3` —— `Number("1e3")` 是 1000，会被认成 1000 号
  · `open-city-ai/haidian#1061` 截成 `#10…` —— 同一份列表里就摆着
    `#840`，所以 `#10…` 看上去完全正常，而那个仓库里真有 10 号。
    两个都不是「显示得不全」，是**指向了另一件东西**
- **失败分两类记**（`lib/github/link-lookup.ts`）。404/451 是结论，
  记下时间别再问；网络错误、限流、超时是故障，**不能记** ——
  记了的话一次抖动会让这批链接永远不再被问，而且没有任何地方看得出来。
  403 尤其不能当成「没有」：它几乎总是配额用完，而配额是一次影响一大批的
- **每小时一个 timer 自愈**（`ops/agenticlab-github.*`）。不跟着同步跑
  （每 2 分钟）是因为一次批量入库能把 60 次/小时的配额瞬间烧干
- ⚠ **`GITHUB_API_TOKEN` 还没配**。不配也能跑，但按服务器 IP 每小时
  只有 60 次，而我们全站共用一个出口 IP —— 已经撞到过。
  建 token 时**一个 scope 都不要勾**：勾了 `repo` 的话私有仓库会开始
  有响应，于是别人贴一条私有链接，我们会替他展开给所有人看

## 帖子底下的「这篇提到的」

- **卡片在读的时候拼，不烤进正文 HTML**。帖子的 HTML 是发表那一刻
  渲染好存下来的 —— 把「★ 1.2k」写进去的话那个数字永远停在发帖那天，
  而且**看不出它是旧的**。一个停住的数字比没有数字更坏
- **渲染这条路上绝不联网**。缓存里没有就整块不出现（不显示占位骨架、
  不显示「加载失败」），正文里那条链接原样还在，读者什么都没少。
  反过来会把一个第三方接口接进每一次打开帖子的路径上：
  GitHub 慢一秒我们的帖子就慢一秒 —— 换来的只是一张卡片早几分钟出现
- **按 ref 存不按帖子存**（`github_facts`）。同一个仓库会被十个人在
  十篇帖子里贴到，按帖子存等于同一份事实抄十遍，刷新时要么漏掉几份、
  要么问十遍，而配额是按小时算的
- **顺序跟着正文** —— 作者先说的排前面。按缓存表的顺序返回的话，
  同一篇帖子每次刷新都可能换个排列，那是最容易让人觉得页面坏了的表现
- **一轮扫描有上限，且从最新往回扫**。一次全表扫描会翻出几百个 ref、
  一轮烧干配额，之后**新发的帖子反而排在最后面** —— 而新帖子
  恰恰是有人正在读的那些

## 第二条测试跑法：终于能把组件真的渲染出来看了

主测试跑在 `--conditions=react-server` 下，而那个条件下
**`react-dom/server` 和 `lucide-react` 都 import 不进来**。
这就是为什么这个仓库里所有组件测试一直只在读源码字符串 ——
不是不想渲染，是渲染不了。而读字符串守得住「这一行别被删」，
守不住**它到底渲染成了什么**：条件写反、属性挂错元素、
图标和文案对不上，源码里那几个字符串一个不少，页面上却是错的。

- `npm test` 现在跑两条：`test:server`（带条件）+ `test:ui`（不带）
- 渲染测试放 `tests/ui/`，第一批是那几张卡片 ——
  真的渲染出 HTML 再断言图标、`rel`、`aria-hidden`、空数组返回 null
- **`tests/test-lanes.test.ts` 盯着这件事不许被改回去**：
  两条都要被 `npm test` 跑到、`tests/ui/` 不许是空的、
  放进去的必须真的渲染了东西。一条没人跑的测试比没有测试更糟 ——
  它让人以为那一块有人守着

## 成员主页：从「他有多活跃」到「他是什么样的」

原话：「个人主页分析加一个口头禅 和谁对话最多 之类的 还有称号」。

- **「常挂在嘴边」**（`lib/members/catchphrase.ts`）。难的不是数词频 ——
  只按词频排的话每个人的口头禅都是「我们」「这个」，那不是口头禅，
  那是中文。要的是**他的使用率 ÷ 同群其他人的使用率**。
  没有中文分词就不分词：直接数 2–4 字的连续片段，
  因为**口头禅本来就常常不是词**（「无所谓啦」「我不到啊」）
- **不叫「口头禅」，叫「常挂在嘴边」**。拿线上四万条消息跑出来，
  一半是真口头禅（「哈哈哈」「了么」「有没有」「牛逼」），
  一半是他常聊的东西（「域名」「公益站」）—— 两者算法上分不开。
  硬按「口头禅」发布的话，一半的人会看到一个明显不像的词，
  然后这块区域就不可信了。**改标题比改算法诚实**
- **最常用的表情单独一条**。第一版 112 个人里有四个的「口头禅」是
  「旺柴」—— 那是微信表情（正文里是 `[旺柴]`），他一个字都没说。
  「他常把旺柴挂在嘴边、说过 52 次」是一句错的话。
  `[图片]` 这类**消息类型占位符**也排掉了，否则发图多的人
  「最常用的表情」会是「图片」
- **和谁 @ 得最多**，不叫「聊得最多」：群消息的回复关系卡在上游，
  手上只有 @。写成「聊得最多」就是一句我们答不上来的话 ——
  两个人天天对着聊、一次没 @ 过，那里会说他们不熟
- **称号摆上主页**，佩戴中的排第一。整套系统早就在（获取、购买、
  续期、结算、过期），唯独主页上不显示 —— 一个只有本人在设置页里
  看得见的荣誉，等于没有荣誉

### 三件差点做错的事

- **这是隐私开关的第六个出口。** 同群的人翻聊天记录本来就看得到他
  说过什么，但**「他最爱说 X」是一句结论，而结论是翻记录翻不出来的**
  —— 聚合出来的画像比原始内容更进一步。和关键词雷达当初漏接开关
  是同一件事。接上之后同步改了开关的说明文案，否则那个开关在说谎。
  ⚠ 被点名的**那一方**也要过开关：一句结论里有两个人
- **36 秒的一步挂在每 5 分钟的定时器上。** 线上实测一轮 33–36 秒
  （11 个群、206 人够门槛），而健康检查每 5 分钟跑一次 ——
  不节流就是一直烧 CPU，算的还是一个几个月不会变的东西。
  单看那一步是对的，单看那个定时器也是对的，**错的是两者的组合**
- **只按 lift（比别人多几倍）排会得到噪声。** 一个说了 5 次、
  别人恰好一次没说的偏僻片段 lift 能到 13，而说了 300 次的真口头禅
  只有 2.7 —— 于是「量子隧穿」打败「卧槽」。改成
  **使用率 × log(lift)**（语料库语言学算 keyness 的常规做法）：
  光看频率得到虚词，光看独特性得到噪声

## 成员目录：从一列名字到一屋子人

- **每个人下面多一句「常说『哈哈哈』」**。一本通讯录和一个社区的区别，
  就在于你翻的时候能不能想起这个人是谁 —— 简介是他自己写的场面话，
  而那个词是他真的在群里的样子
- **做得很轻**（小字、引号、不加底色）：它是风味，不是又一个字段。
  做重了这一行就变成五条信息并排，**而并排的信息等于没有信息**
- ⚠ **目录里只放词，不放 wx_id**。目录那一层专门不把 wx_id 放进
  给客户端的结构里（会被序列化进 RSC 载荷、出现在网页源码里，
  而拿着 wx_id 就能在微信里直接加人）。查是在服务端按 wx_id 查的，
  **过去的是词** —— 这是那条边界的第二个入口

### 顺带把一处老重复清掉了

加这个功能时 N+1 守卫红了：目录从 15 条查询涨到 21 条。查下去发现
一多半不是新功能的：

- 「我在哪几个群」被 `peersOf` 和 `sharedGroupCounts` **各查了一遍**
- 「这个人有没有隐私豁免权」被 `leaderboardHiddenWxIds` 和
  `unsearchableWxIds` **各判了一遍**，而每次判定本身要跑两条查询

前者抽成 `myGroupIds` 算一次传下去；后者合成 `hiddenWxIds()`
一次取两份名单。**两个开关的语义一个字都没改** —— 只是少问了两遍
同一个问题。清完之后目录回到 15 条以内，新功能等于白送。

这类重复不会让任何东西出错，只会让「这一页要跑多少条查询」这个数字
慢慢往上爬 —— 而爬到某一天，就没有人记得每一条是为什么了。

## GitHub 生态整个是关着的，而没有任何地方说这件事

查「为什么一个人都没绑 GitHub」时发现的：线上**根本没配 OAuth**
（三个环境变量一个都没有），于是绑定入口不渲染、`/api/auth/github/start`
返回 404 —— 主页项目展示、「要不要发帖分享」整块都不会出现。
绑定 0 人、仓库缓存 0 条，不是没人想用，是入口根本没出现过。

「不配就整个消失」这个设计本身是对的（半套配置比没配置更糟：
按钮照常出现、点下去走到一半才在 GitHub 那边失败，而用户会以为
是自己的 GitHub 有问题）。**问题是它安静到站长看不出来。**

一个「做了但没人看得见」的功能和没做，唯一的区别就是
有没有一个地方说得出它是关着的。现在健康页有 `github` 这一项：

- 没配 → 说清缺哪三个变量、以及因此少了哪些功能
- 配了但没人绑 → 也说一句（那多半是入口埋得太深）
- 配好了 → 报绑定人数、展示人数，以及有没有只读 token

**状态是 `degraded` 不是 `down`**：站长可能就是不想接 GitHub。
报成 down 会让总状态一直红着，而一个一直红着的仪表盘会让真出事那次
也没人看 —— 和 frp 那次是同一个道理。

## 「一般什么时候说话」：一列写了两年没人读的数据，和它的开关

主页上多了一条：24 根条子（一根一小时）＋ 一句「深夜型 / 早起型」，
底下标着 0/6/12/18/24 的刻度 —— 没有刻度的话那排条子只是好看，
读不出几点。

**这一条是代码自己留的作业。** `daily_stats.hour_histogram` 同步时
逐条消息在写，写了七百多天，而在此之前**没有任何地方读它来展示**。
守着它的隐私开关 `hide_activity_hours` 当年被删掉，理由正是
「它守的东西不存在」—— 那条注释同时留了一句：

> 哪天真做了热力图，再把它加回来 —— **它暴露的是一个人的作息**。

所以这次一起做了：开关回来了（第四个），`hide_activity_hours`
从死列名单上划掉。

### 几个不肯多说的地方

- **给的是三小时的窗口，不是时间点。** 「21:00–24:00 最多」说的是习惯，
  「他 23:47 还在线」说的是行踪 —— 后一种没有人会想挂在主页上
- **窗口要绕过零点。** 不绕的话 23:00–1:00 说话的人被切成两半、
  两边都不突出，判成「作息很散」—— 而他恰恰是最规律的那一类
- **作息散的人只给图，不给标签。** 三小时占全天 12.5% 是完全平均；
  低于 30% 就闭嘴。线上实测五个最活跃的人是 27% / 44% / 29% / 23% / 38%
  —— 门槛卡在这儿，两个人拿到标签、三个人只有图。
  **一句「夜猫子」摆在主页上，看的人不会去想它是根据几条消息算的**
- **这个开关的说明写得比别的更直白**：别的开关暴露的是「你说过什么」，
  这一条暴露的是**你什么时候醒着** —— 几点睡、几点起、是不是上夜班、
  哪天开始作息变了。那些是同群的人翻聊天记录也拼不出来的东西

### 已做完
- **「frp 恢复了页面提示没恢复」** —— 探测失败时写 `frp_tunnel: down`、
  成功时写的却是 `upstream_api: ok`，**没有任何路径把 frp_tunnel 写回 ok**。
  隧道断过一次就永远卡在 down，而总状态取最差的组件。
  这比没做更糟：它让健康状态变成一个**学会撒谎的仪表盘**，
  看过两次「明明好了还说坏」之后，真出事那次也不会有人信

## GitHub 从「个人资料的一栏」变成「社区的一层」（8-12）

`ROADMAP.md` 第九节剩下的几条。上一轮做完的是「贴出来的 repo / issue / PR
会说话」，这一轮做的是**剩下两种链接**、**把项目变成一个能点进去的东西**。

### `owner/repo#123` 自动成链

- **改的是 markdown 源文，产出一条普通的 `[文字](地址)`**
  （`lib/github/shorthand.ts`）。于是消毒、`target=_blank`、
  `rel=nofollow` 全部由已有的那条管线管，而且「这篇提到的」那套卡片
  **自动就认得它** —— 不用为简写再造一条从源文捞 ref 的路。
  造了就是两份，两份迟早对不上
- **裸 `#123` 一条都不认**，而且这不是「以后再说」：要把它变成链接，
  就得替作者猜一个仓库出来，而猜错一次的结果是**在别人的正文里插进
  一条指向陌生仓库的链接**，落款是这个站
- **前面那个 lookbehind 是这条功能的全部风险所在**。
  `https://evil.com/a/b#123` 里也有这个形状 —— 认了它就等于把作者写的
  那条地址重写成 github.com 上的另一件东西。所以前一个字符不许是
  单词字符 / `.` / `/` / `@` / `:` / `-` / `]` / `[` / `(` / `#`。
  `(` 和 `[` 一起挡掉意味着 `(a/b#1)` 这种英文括号认不出来 ——
  故意的：markdown 链接的目标前面正好是 `(`，在那儿动手会把作者写好的
  链接**改成一段乱码**，而少认一条什么都不会发生
- **编号不许有前导零**。`a/b#0123` 只能链到 123 号 ——
  屏幕上写着 #0123、点过去是 #123，和把 `/issues/1e3` 认成 1000 号
  是同一类错。URL 那条路没有这个问题（两头都归一到 `#123`），
  所以没跟着收紧

### commit 展开 / 代码永久链接展开成代码块

- **commit 只取 message 的第一行**。正文部分动辄十几行还带着
  `Signed-off-by` —— 整段贴上去，一张「一句话说清这是什么」的卡片
  会变成比正文还长的一块。**message 读不出来就整条不写**：
  只剩一个 `owner/repo@sha` 的卡片等于把链接抄了一遍还占了块地方
- **不带行号的代码链接不展开**。截前 20 行是**替作者选了一段他没选的
  代码**，而读者会以为那就是他要说的地方
- **少给了就要说出来**：作者写 `#L10-L200`、我们只显示 20 行时，
  卡片上明写「还有 N 行没展开」。不说的话，讨论会照着一段被截断的
  代码进行下去
- **高亮在取回来的那一刻做完，存进 `github_facts.body`**。
  这是「不许把东西烤进正文」那条规则唯一站得住的例外：解析层
  **只认带 40 位 sha 的**代码链接，而一个 sha 指向的内容不可能变。
  既然结果永远相同，就没有理由让每一个读者的那次打开都重跑一遍 shiki
- **存进去的 HTML 走的是帖子正文那同一个 `sanitizeHtml`**，
  不是另写一份白名单 —— 何况这段 HTML 的原料来自**别人的仓库**
- **一篇帖子最多展开两段代码**。`MAX_MENTIONS` 管的是「一共几张卡片」，
  而代码块的高度是别的卡片的十倍
- **顺带查出一个全站范围的哑巴 bug**：消毒器那条 style 白名单只列了
  `color` / `background-color` 那几个属性名，而 shiki 在双主题模式下
  **一个都不写** —— 它写 `--shiki-light` / `--shiki-dark`，
  再由 `globals.css` 里 `var()` 出来。于是每一条声明都不匹配、
  整个 style 被删掉，**全站每一个代码块都在无声地掉色**。
  而 `markdown.test.ts` 里那条「shiki 用的配色 style 被保留」
  测的是 `color:#ff0000` —— 一段 shiki 从来不产出的东西，
  于是它一直是绿的

### 帖子关联项目 + 项目页 + 项目目录

- **一篇只能关联一个**（`forum_posts.repo_ref`）。能挂三个的话它就变成
  第二套标签，而标签已经有了。这一栏回答的是更窄的问题：
  **这篇帖子属于哪个项目**，而「属于」只能有一个答案
- **归一到小写**。GitHub 那边仓库名不区分大小写，不归一的话同一个项目
  会因为两个人打字习惯不同**裂成两个项目页**，两边各聊各的 ——
  而那正是这条功能要解决的问题本身
- **认不出来当没填，不报错**：它是可选的补充信息，为它把一篇写好的帖子
  挡回去，是让一个次要字段决定主要动作能不能完成。代价由输入框补上 ——
  那里当场显示认没认出来，用的是**和服务端同一个 `parseRepoRef`**
- **反查走 `listPosts` 的一个选项**，不是项目页自己写的一条 SQL。
  另写一条就是可见性判定的第二份，而这个仓库最贵的几次错误
  都是同一条规则在两个地方各写了一遍
- **项目页在没有任何站内绑定时也打得开**。有人聊一个跟社区毫无关系的
  上游仓库很正常 —— 而那恰恰是「站里聊过它的」最有价值的一种情况

### 隐私口径：三道门，一道都不能省

这一页把「站内某个人」和「某个 GitHub 账号」摆在同一行上。
那些仓库本来就在 GitHub 上公开着，**而这条对应关系是这个站拼出来的**。

- ① **他自己打开了「在主页展示 GitHub」**。绑定 ≠ 同意公开 ——
  有人绑定只是为了那条「要不要发帖分享」的提醒，默认就是关的
- ② **他没有隐身**（`users.directory_hidden`）。这一条最容易漏，
  因为它看起来是「成员目录」那一页的事 —— 而那个开关自己的原话是
  「不出现在成员列表和搜人结果里」，**一个按语言可筛的项目目录
  就是一份换了个维度的成员名册**：照着人找项目和照着项目找人
  是同一件事。漏掉它，刚把自己从成员目录里摘出去的人，
  会在另一页上带着名字和头像重新出现
- ③ **要登录**（`/projects` 进了 `PROTECTED_PREFIXES`，拦在 proxy 那一层）。
  只在页面里 redirect 的话，爬虫拿到的是 200 加一个已经渲染过的壳
- **过了这三道之后，可见范围和成员主页上那一栏完全一样** ——
  这一页因此没有制造出任何新的暴露面，它只是把已经公开的东西换了个排法。
  **新页面不新增可见性**，这是整个口径的落点
- **代价说清楚**：它因此**不是**「最适合对外展示的一张脸」。
  真要对外，得单独做一次「愿意公开露出」的表态，而不是把这一页放开

### 「谁提过 PR」主动没做

手上唯一的 PR 数据是 `github_share_prompts` —— 那是为了给**本人**发一条
「要不要发帖分享」而攒的，他从没同意过把它变成一份公开名册。
而且它只覆盖最近九十天（走的是公开动态流）、还只覆盖没关掉提醒的人。
**一份注定不全的名单会被读成「只有这几个人参与过」**，
而漏掉的那些人没有任何办法让自己出现在上面。
