# 论坛规格

> **读者**：要动论坛可见性或管理动作的人
> **性质**：**现状**（代码把它当规格引用）
> **最后核对**：2026-08-10

论坛是这个站的**核心**，不是附属功能。

它承担的是群聊承担不了的事：**沉淀**。微信群里聊出来的东西刷走就没了，
论坛是让它留下来、被检索到、被后来的人读到的地方。
所以标准不是「有个能发帖的地方」，而是**现代论坛该有的都得有**。

交互一律按 Apple HIG。HIG 的核心不是圆角和毛玻璃，是**尊重用户**：
可撤销优于二次确认、乐观更新优于等待转圈、保留上下文优于粗暴跳转。

---

# 一、内容创作

## 1.1 编辑器

| 能力 | 说明 |
|---|---|
| Markdown | 实时预览（桌面双栏 / 移动切换），工具栏 + 快捷键（⌘B / ⌘I / ⌘K） |
| 代码块 | 语法高亮、语言选择、行号、一键复制。**AI Agent 社区的刚需** |
| 数学公式 | LaTeX（`$...$` / `$$...$$`），讨论算法时会用到 |
| 图表 | Mermaid 流程图与时序图 |
| 图片 | 点击 / 拖拽 / **粘贴**上传，自动压缩，插入即预览 |
| 附件 | 限类型与大小，病毒扫描留接口 |
| 表格 | 可视化编辑，不逼用户手写 Markdown 表格 |
| @提及 | 输入 `@` 弹出成员补全，按活跃度与互动频次排序 |
| #话题 | 输入 `#` 补全已有标签，可新建（受权限约束） |
| Emoji | 选择器 + `:smile:` 简写补全 |
| 折叠块 | spoiler / 详情展开，长内容不刷屏 |
| 链接卡片 | 粘贴链接自动抓标题与摘要，可切回纯链接 |

## 1.2 不丢内容（这一条决定用户敢不敢在这写长文）

- **本地草稿自动保存**：每 3 秒存 IndexedDB，关掉浏览器也在
- **服务端草稿**：登录状态下同步，换设备能接着写
- 离开页面有未保存内容时拦截并提示
- 提交失败保留全文，不清空
- 编辑冲突检测：别人改过了给出 diff，不静默覆盖

## 1.3 发布

- 帖子类型：**讨论 / 问答 / 投票 / 展示 / 公告**
- 定时发布
- 发布前预览（所见即所得）
- 首次发帖引导（版块规则、格式提示）

---

# 二、帖子功能

## 2.1 结构

- 标题、正文、标签（多选）、封面（可选）
- 版块归属，可移动
- 可见性（见第四节）
- 编辑历史**公开可查**，带版本 diff —— 悄悄改内容是论坛信任的头号杀手

## 2.2 互动

| 功能 | 细节 |
|---|---|
| 反应 | 不止点赞：👍 有用 / 💡 有启发 / 🎯 精准 / ❤️ 喜欢。多维反应比单一点赞信息量大得多 |
| 收藏 | 支持分组（收藏夹），可私密 |
| 订阅 | 关注帖子后有新回复通知；也可关注作者、标签、版块 |
| 分享 | 生成短链 + 精美卡片图（微信里发出去有预览） |
| 打赏 | 用积分给作者打赏，给积分一个消耗出口 |
| 举报 | 分类原因 + 补充说明 |

## 2.3 问答类专属

- 悬赏积分（发布时设定，可追加）
- **采纳最佳答案**，采纳后置顶该回答并发放积分
- 「已解决 / 未解决」筛选
- 未被采纳但高赞的答案也上浮

## 2.4 投票类专属

- 单选 / 多选、匿名投票、截止时间
- 结果可设为「投票后可见」
- 实时结果图表

## 2.5 管理动作

加精 · 置顶（版块内 / 全站） · 锁定 · 移动版块 · 合并 · 拆分 ·
软删除与恢复 · 编辑他人内容（留痕）

---

# 三、回复

- **两种视图**：楼层（论坛式）与树形（讨论式），用户可切换，版块可设默认
- 引用回复（引用片段而非整楼）
- 楼中楼，超过 N 条折叠
- **折叠低质回复**（被踩多 / 极短 / 举报命中），默认收起可展开
- 只看楼主 / 只看某人
- 跳转到指定楼层（`#12` 锚点）并高亮
- 楼主可管理自己帖子下的回复（删除、折叠），但要留痕
- 回复也有草稿保护

---

# 四、权限分级

这是用户特别强调的部分。权限判定一律走 `can()`，不写 `if (role === ...)`。

## 4.1 版块级配置

```
visible_to          谁能看到这个版块（含未登录）
post_permission     谁能发帖
reply_permission    谁能回复
default_visibility  新帖默认可见性
max_visibility      封顶，该版块任何帖子都不能超过
post_min_level      发帖最低等级
allow_anonymous     是否允许匿名发帖
require_tags        是否强制打标签
```

## 4.2 内容可见性（六级）

| 级别 | 谁能看 | 搜索引擎 |
|---|---|---|
| `public` | 所有人含未登录 | ✅ 索引 |
| `unlisted` | 所有人含未登录 | ❌ noindex |
| `member` | 已绑定成员 | ❌ |
| `role` | 指定身份组 | ❌ |
| `group` | 指定群的成员 | ❌ |
| `private` | 仅作者与管理员 | ❌ |

**三条硬约束**（写死在代码里，不可配置绕过）：

1. 群聊消息永远不可 `public` / `unlisted`
2. 群聊转帖默认锁定在 `group`，提升需管理员审核 **+ 所有被引用消息的原作者同意**，且单独留痕
3. `external` 与未登录用户永远看不到任何群消息派生内容

> 「一键成帖」和「未登录可看论坛」放在一起，是这套设计里最容易出事的地方。
> 一次误操作就能把私密群聊送上公网，而且不可撤回（已被抓取）。

## 4.3 角色能力

| 角色 | 范围 | 能力 |
|---|---|---|
| 访客 | — | 浏览 public 内容 |
| 成员 | 自己 | 发帖回帖、编辑删除自己的、反应收藏 |
| 版主 | **指定版块** | 该版块内审核：删/锁/精/置顶/移动/折叠 |
| 管理员 | 全站 | 全部内容操作 + 版块管理 |
| 楼主 | 自己的帖子 | 管理本帖回复、采纳答案、锁定本帖 |

## 4.4 反滥用（新人限制）

- 新绑定用户前 N 天不能发外链（防广告，阈值可配）
- 发帖 / 回帖频率限制（滑动窗口）
- 等级门槛：L2 才能发帖、L3 才能建话题（可配）
- 同内容重复发布检测
- 敏感词库：拦截 / 送审 / 替换三档

---

# 五、浏览与发现

- **排序**：最新回复 / 最新发布 / 最热 / 回复最多 / 无人回复 / 精华
- **筛选**：版块 / 标签 / 作者 / 时间范围 / 已解决 / 有悬赏
- **全文搜索**：复用现有 FTS5（CJK 逐字切分那套），标题加权
- **未读标记**：哪些帖子有新回复，一眼看得出
- **跳到上次阅读位置** —— 长帖回来不用重新翻
- 阅读进度条
- 相关帖子推荐（同标签 + 内容相似）
- 热榜（时间衰减加权，不是纯点赞数）
- 无限滚动**保留位置**：返回时回到原处，不是滚回顶部

---

# 六、通知

| 触发 | 渠道 |
|---|---|
| @我 | 站内 + 邮件 |
| 我的帖子有新回复 | 站内 |
| 我关注的帖子有新回复 | 站内 |
| 我的回复被引用 | 站内 |
| 被加精 / 被采纳 | 站内 + 邮件 |
| 收到打赏 | 站内 |
| 内容被管理员处理 | 站内 + 邮件（必须告知，不能悄悄删） |

- 通知中心：未读聚合、一键已读、按类型筛选
- 每类通知可单独开关渠道
- **聚合**：同一帖子的多条回复合并成一条「3 人回复了你的帖子」，不刷屏

---

# 七、交互设计（Apple HIG）

HIG 的核心是尊重用户。以下每一条都有具体做法。

## 7.1 可撤销 > 二次确认

**删除回复不弹「确定要删除吗」**，直接删掉并在底部浮出
「已删除 · 撤销」的 Toast，5 秒内可恢复。

二次确认弹窗打断心流，而且用户会养成无脑点确定的习惯，
真正危险的操作反而被忽略。只有**不可逆**的操作（删号、清空数据）才用确认。

## 7.2 乐观更新

点赞、收藏、订阅立刻变色，请求在后台发。失败了再回滚并提示。
让用户等一个转圈才变色，是把网络延迟转嫁给用户。

## 7.3 手势（移动端）

- 回复项**左滑**：引用 / 收藏 / 举报
- **右滑边缘**返回上一页（与 iOS 一致）
- **下拉刷新**，带阻尼与回弹
- **长按预览**帖子（Peek），松手回到原处
- 关键操作触发**触觉反馈**（`navigator.vibrate` 降级处理）

## 7.4 加载状态

- 用**骨架屏**而不是转圈：骨架屏让人感觉更快，也避免布局跳动
- 分页加载保留滚动位置
- 图片有明确占位尺寸，加载完不推挤内容（CLS = 0）

## 7.5 键盘（桌面）

`⌘K` 全局搜索 · `⌘Enter` 发布 · `J/K` 上下条 · `R` 回复 ·
`E` 编辑 · `?` 快捷键帮助

## 7.6 空状态

每个空状态都要说清楚**为什么空**和**下一步做什么**，
不能只画个灰色图标写「暂无数据」。

## 7.7 无障碍

- 完整键盘可达，焦点环清晰
- 语义化标签 + ARIA
- 对比度 ≥ WCAG AA
- 尊重 `prefers-reduced-motion`
- 字号跟随系统（Dynamic Type）

## 7.8 移动端编辑器

- 键盘弹出时工具栏吸附在键盘上方，不被遮挡
- 输入区自动滚动到可视范围
- 安全区适配

---

# 八、数据模型补充

在 `SCHEMA.md` 第六节基础上增加：

```
forum_boards       + visible_to, post_permission, reply_permission,
                     default_visibility, max_visibility, post_min_level,
                     allow_anonymous, require_tags, view_mode

forum_posts        + type(discussion|question|poll|showcase|announcement)
                   + visibility, visibility_role_id, visibility_group_id,
                     visibility_locked
                   + solved_reply_id, bounty_points
                   + scheduled_at, anonymous
                   + edit_count, last_edited_at
                   + og_image, share_code

post_revisions     id, post_id, editor_id, title, content,
                   change_note, created_at        -- 编辑历史公开可查

post_tags          post_id, tag_id
tags               id, name, slug, description, color, post_count,
                   created_by, locked

reactions          + kind(useful|insight|precise|love)   -- 多维反应

replies            + floor(楼层号), quoted_reply_id, quoted_excerpt,
                     collapsed, collapse_reason, accepted

bookmarks          + folder_id
bookmark_folders   id, user_id, name, private

subscriptions      + target_type(post|user|tag|board)

post_views         post_id, user_id, last_read_reply_id, read_at
                   -- 未读标记与「跳到上次阅读位置」

polls              id, post_id, multi, anonymous, closes_at, hide_until_voted
poll_options       id, poll_id, text, votes
poll_votes         poll_id, option_id, user_id

tips               id, post_id, from_user, to_user, points, ledger_id

drafts             id, user_id, target_type, target_id, title, content,
                   updated_at                     -- 服务端草稿
```

---

# 九、实现顺序

| 阶段 | 内容 |
|---|---|
| 1 | 版块 + 发帖 + 回帖 + Markdown 编辑器 + 权限判定接入 |
| 2 | 可见性分层 + 未登录浏览 + 三条硬约束 |
| 3 | 反应 / 收藏 / 订阅 / 通知 |
| 4 | 问答类（悬赏 + 采纳）+ 标签 + 搜索 |
| 5 | 版主工具 + 举报 + 申诉 + 编辑历史 |
| 6 | **群聊一键成帖 + 原作者同意流程** |
| 7 | 投票 / 打赏 / 分享卡片 |
| 8 | 手势、骨架屏、快捷键、无障碍走查 |

每一阶段都要写测试。可见性判定必须有**矩阵测试**：
6 个可见性级别 × 5 类身份 = 30 种组合逐一断言，
这是整个论坛最不能出错的地方。
