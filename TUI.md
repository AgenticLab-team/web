# 终端客户端（ash）

> **读者**：要实现、review 或部署终端客户端的人
> **性质**：**现状**（代码都在仓库里且能跑通；线上还没部署）
> **最后核对**：2026-08-13

把这个站搬进终端：`curl -Ls agenticlab.sh | bash` 装一个本地二进制，
或者 `ssh anyuser@agenticlab.sh` 直接进来。里面是群聊和论坛，
以及**网页上有的每一样东西**。

---

## 一、为什么是两个入口，而不是一个

一个够用的话就不会有两个。这两个覆盖的是两拨完全不同的人：

| 入口 | 给谁 | 代价 |
|---|---|---|
| `curl -Ls agenticlab.sh \| bash` | 有自己的机器、能装东西的人 | 要下载、要能写 `~/.local/bin` |
| `ssh anyuser@agenticlab.sh` | **Windows、公司电脑、iPad、网吧、别人的机器** | 什么都装不了，但 ssh 客户端到处都是 |

第二条不是「顺手也做一下」。这个群里相当一部分人只有一台装不了东西的
Windows 工作机 —— 对他们来说，**没有 SSH 入口就等于没有终端客户端**。
Windows 10 起自带 OpenSSH 客户端，所以 `ssh` 这条路在那台机器上
恰好是唯一不需要管理员权限的路。

两个入口跑的是**同一个 Bubble Tea 程序**（见下一节），
所以「SSH 版功能少一点」这种事在结构上不可能发生。

---

## 二、为什么是 Go

站里其余全是 TypeScript，多引一门语言要有很硬的理由。理由有两条，
第二条才是决定性的。

**① 分发。** `curl | bash` 要在一台什么都不知道的机器上跑起来。
静态二进制没有运行时依赖 —— 不用探测 Node、不用装 Node、
不用担心对方的 Node 是 16。TypeScript 那条路要么要求 Node 22+，
要么打一个 50 MB 的 SEA，两个都比 12 MB 的静态二进制差。

**② 同一个程序既是本地二进制又能被 ssh 进来。**
Charm 的 [Wish](https://github.com/charmbracelet/wish) 让一个
Bubble Tea 的 `Model` 直接当成 SSH 服务端的会话处理器：

```
cmd/ash/       main() → tea.NewProgram(ui.New(...)).Run()
cmd/ash-sshd/  main() → wish.NewServer(bubbletea.Middleware(handler))
                        handler(s ssh.Session) → ui.New(...)   ← 同一个 New
```

**两个入口共用 `internal/ui` 里那一个 model。** 这是选 Go 唯一
不可替代的地方 —— 别的语言都要为 SSH 那侧另写一套伪终端服务端，
而「另写一套」在这个仓库里的下场已经反复见过（发帖规则、分页边界、
图标表，每一次都是第二份实现少判了一样东西）。

代价说在前面：**Go 这侧不能直接 import `src/lib/**/*-rules.ts`。**
web 和 TUI 的一致性因此不能靠共享代码保证，只能靠第五节那套机制。

---

## 三、SSH 入口落在哪：一个明确的取舍

`DEPLOY.md` 里写着两条现状：源站躲在 Cloudflare 后面、只对 CF 网段
开 80/443，而且 **「CF 不代理 SSH」**。这两条和 `ssh anyuser@agenticlab.sh`
不可能同时成立 —— 一个被 CF 代理的 apex 记录解析出来的是 CF 的 IP，
22 端口上没有任何东西在听。

三条路，站长选了第三条：

| | 结果 | 代价 |
|---|---|---|
| 独立网关 + `ssh.agenticlab.sh`（灰云） | 源站保密性完全不受影响 | 广告语变长 |
| Cloudflare Spectrum 走 apex | 字面满足，且不暴露任何 IP | 任意 TCP 基本是 Enterprise 档 |
| **apex 灰云指向网关**（已选） | **`ssh anyuser@agenticlab.sh` 字面成立** | **主站失去 CF 代理** |

选了第三条，就要把它的后果写清楚，而不是让它悄悄躺在 DNS 面板里：

- `agenticlab.sh` 的 A 记录直接指向**网关**的公网 IP。
  主站的 DDoS 防护、WAF、缓存、Bot 拦截**从此都不在链路上**。
- 网关同时跑 `sshd:22` 和 `nginx:443`，443 反代到源站。
  真实客户端 IP 的那套 `set_real_ip_from` 要跟着从 CF 网段
  改成网关地址，否则**按 IP 限流当场失效、审计日志可以随便伪造**
  （`DEPLOY.md` 里那条坑的原文）。

### 网关不知道什么

源站地址仍然是秘密 —— 网关是个**新的、可以随时重装的**盒子，
它只需要知道：

```
SITE_URL=https://agenticlab.sh     # 就这一个
```

它调 API 走公网 HTTPS，和任何一个第三方客户端**没有区别**。
网关上没有数据库、没有 `.env.local`、没有上游 frp 隧道、
没有 `.deploy-host`。被打穿的后果是「一台跑着开源 TUI 的空盒子丢了」，
不是「站没了」。

这条不是建议，是网关能存在的前提：一旦有人为了图快在网关上
放一份数据库连接或者源站内网地址，上面整段就全部作废。

### 网关持有的唯一敏感物：别人的令牌

SSH 进来的人在网关上完成登录，令牌**存在网关上**（第四节末尾详述）。
这是这套东西最大的一处风险集中，所以它单独有三条约束，写在第四节。

---

## 四、登录：设备码

### 它和 `docs/OAUTH-PROVIDER.md` 里被否掉的 device flow 不是一回事

那份文档第七节写着「不做 device flow」。这里做了一个长得很像的东西，
所以必须说清楚区别，否则下一个人会以为其中一份过期了。

那一条否掉的是**给第三方应用的** device flow：让一段我们没 review 过的
代码，用一串码换到「以某个成员的身份行事」的能力。否掉的理由是
钓鱼 —— OAuth 的钓鱼不靠伪造页面，靠伪造应用。

这里的设备码只发给**第一方客户端**，而且：

- **没有 `client_id`，也就没有「哪个应用」这个概念可以被伪造。**
  确认页上写死的是「Agentic Lab 终端客户端」，不接受任何应用名。
- 因此它不是 OAuth 提供方的一部分，`oauth_apps` 那张表在这里
  不出现。哪天真的实现了第三方 OAuth，这两套各走各的。

代价是诚实的：**它挡不住「攻击者把自己屏幕上的码念给你听」**
这一类社工。这是所有设备码流程的共同弱点，缓解手段全在确认页上
（下面「确认页要说什么」）。

### 流程

```
① 终端里按下「登录」
   POST /api/v1/auth/device/start
     { fingerprint: "MacBook Pro · zsh · 120x40", scopes: [...] }
   → { user_code: "WXYZ-7Q2M", verification_uri: "https://agenticlab.sh/link",
       device_code: "<不显示给人的长随机串>", interval: 5, expires_in: 600 }

② 终端上显示 user_code 和二维码（终端画得出二维码，这一步不该让人手抄网址）

③ 人在**任何一个已经登录的浏览器**里打开 /link，输入 WXYZ-7Q2M
   没登录 → 跳 /login?next=/link   ← 绝不建号、绝不发会话

④ 确认页 → 同意

⑤ 终端一直在轮询
   POST /api/v1/auth/device/poll  { device_code }
   → 等待中: { error: "authorization_pending" }
     太快了:  { error: "slow_down" }      ← 间隔翻倍
     成功:    { access_token: "al_…", scopes: [...], expires_at }
```

拿到的是一把**和自己在「我的 → 开放 API」里手建的一模一样的 `al_` 令牌**。
这一条抄的是 `docs/OAUTH-PROVIDER.md` 第三节那个决定，理由也一样：
`authenticate()` 验令牌和 scope、`sendAllowance()` 按人限流、
`recordSend()` 留痕、撤销和日志的界面 —— 全都已经在了。
另起一套的话，这些每一条都要重写一遍，而重写出来的那份迟早少判一样东西。

`api_tokens` 因此只加两列：`source`（`manual` / `device` / `ssh`）
和 `device_label`。验令牌那条路一个字不改。

### 确认页要说什么

`/link` 是唯一挡在社工和用户之间的东西，所以它的内容是安全设计，不是文案。

- **你正在登录的是哪台设备** —— 终端报上来的指纹（机器名、终端类型、
  窗口尺寸），以及**请求是从哪个 IP 发起的**。
  「如果这几行不是你现在坐在的这台机器，关掉这一页。」
- **`ssh` 来的要额外标出来**：来源写「通过 SSH 网关」，
  并说明令牌会存在网关上。这是它和本地二进制唯一的实质差别，
  不能让人以为两者一样。
- 逐条列出 scope 的人话说明，用现有 `SCOPES` 里的 `label`/`detail`，
  `danger: 1` 以上单独标出来，`danger: 2` 的默认不勾。
- 「拒绝」和「同意」等大等重，不做视觉诱导。
- 同意和拒绝都留审计。

### SSH 网关上的令牌

网关要替人调 API，所以它**必然**持有令牌明文。三条约束把这件事的
爆炸半径压住：

1. **绑死 SSH 公钥指纹。** 令牌按 `sha256(公钥)` 存。换一把钥匙进来
   就是一个新的登录 —— 网关上没有「用户名」这个概念可以被冒充。
   （这也是为什么用户名可以是 `anyuser`：它不参与任何判定。）
2. **短期 + 可一键全撤。** 网关签出的令牌 `source = 'ssh'`、7 天到期，
   并且在「我的 → 开放 API」里单独成一组，一个按钮撤掉全部。
   站长那边还有一个「撤销所有 SSH 网关令牌」的后台动作 ——
   网关被怀疑失守时，那是第一个要按的按钮。
3. **`groups:send` 在 SSH 入口默认不给。** 它是唯一一个「泄漏的后果
   由整个社区承担」的 scope（`lib/api-tokens/rules.ts` 顶上那段）。
   在一台公开可连、且持有他人令牌的机器上默认打开它，
   等于把那段注释里的风险乘以在线人数。要开得在确认页上单独勾。

---

## 五、「网页有的 TUI 都有」怎么保证不烂掉

这是整件事**最容易失败**的地方，而失败的样子是可预测的：
第一版对齐得很好，三个月后网页加了两个页面、改了一个规则，
终端这边没人记得跟，于是它慢慢退化成一个只能看群聊的玩具。

靠自觉守不住。所以有一张表和一条测试。

### `src/lib/tui/surface.ts` —— 唯一真源

站里每一个「面」在这里有且只有一条记录：

```ts
{
  key: "forum.post",
  label: "帖子正文",
  web: "/forum/p/[id]",           // 网页在哪
  api: ["GET /api/v1/posts/{id}"], // 终端靠哪些端点做到同一件事
  tui: "forum/post",               // 终端里的屏幕 id
  scopes: ["forum:read"],
}
```

### `tests/tui-parity.test.ts` —— 守卫

每一条都对应一种「悄悄退化」的方式：

1. **网页有、表里没有** → 红。扫 `src/app/(app)/**/page.tsx`、
   `lib/nav.ts` 和 `lib/admin/nav.ts`，任何一个路由在 `surface.ts` 里
   找不到就失败。这条保证的是「加了新页面必须当场决定终端怎么办」——
   可以决定不做（填 `tui: null` 并写原因），但不能**没想过**。
2. **表里声明的端点不存在** → 红。对着 `lib/api-tokens/catalog.ts` 查。
   反方向也查：一个谁也没用的端点意味着要么它该删，要么终端漏做了一个面。
3. **表里声明的屏幕 Go 那边没注册** → 红，反过来也红。
   靠搜 `tui/internal/ui/screens/registry.go` 里的字符串做跨语言核对。
4. **倒给 Go 的那份数据过期了** → 红。改了表忘了 `npm run tui:gen` 的话，
   TS 这侧的守卫全绿（它读的是真源），而终端里少一格。
5. **`tui: null` 必须带 `why`** → 红。一个没有理由的缺口
   六个月后没有人敢补，因为不知道当初为什么不做。
6. **写操作要声明它要哪个 scope**，且必须选清楚是
   「缺了打不开这一屏」还是「缺了只是这个动作不可用」——
   群聊是后者（大多数人没有 `groups:send`，但都要看群聊）。

第 1 条是这几条里唯一真正要紧的那条：**缺的功能人看得出来，
而一个没人想过的缺口不会有任何症状**（`ARCHITECTURE.md`
第二节讲死开关那段说的是同一件事）。

---

## 六、聊天：两条改不掉的物理限制

终端里的群聊看起来像 IRC，但它底下不是 IRC。有两条限制必须
**在界面上直说**，因为它们不符合任何人对聊天的预期。

### ① 收到的消息最多晚 2 分钟

群消息是**镜像**：`agenticlab-sync.timer` 每 2 分钟从上游拉一次。
上游那 27 个端点里**没有 webhook、没有长连接**（`lib/nekobot/client.ts`
是纯轮询），所以这个延迟不是我们的实现问题，是数据来源的形状。

界面上的做法：消息区底部常驻一行 `上次同步 1 分 12 秒前 · 每 2 分钟`。
不写的话，人发一条、等了 90 秒没看见回应，会以为是自己网络断了 ——
**而他会去按重连，那什么也解决不了**。

（如果哪天要做到秒级，唯一的办法是让同步频率跟着「有没有人正在
终端里看这个群」走。那是另一个改动，不在这一版里。）

### ② 每条发出去的消息都带署名，去不掉

```
大家好
本消息由「张三」使用 AgenticLab.sh 代发
```

因为消息是**机器人账号**发出去的，群里看到的是机器人在说话。
`withAttribution()` 是发送路径上唯一拼正文的地方，
终端这侧连绕过它的接口都没有。

界面上的做法：输入框下面**实时显示拼好之后的整条**，
包括那行署名和剩余字数（上限 500 字，署名占掉的那几十个字
已经从预算里扣掉了）。人在按回车之前就知道群里会看到什么。

### ③ 还有一条不是限制，是权限

能往哪个群发，由站长逐群授权（`group_send_grants`）。
没有授权的群，输入框不显示成一个能打字然后被拒绝的框 ——
显示成一行说明：「这个群没有开代发。要开的话找站长说一声，
他给的是**这一个群**的发言权，不是一个身份。」

---

## 七、布局

Discord 式三栏，窄终端逐级折叠。断点按**列数**，不按「是不是手机」：

```
≥ 120 列                    80–119 列              < 80 列
┌──┬────┬──────────┬────┐  ┌────┬──────────┐      ┌──────────┐
│服│频道│   主区    │成员│  │频道│   主区    │      │   主区    │
│务│    │          │/详 │  │    │          │      │          │
│条│    │          │情  │  │    │          │      │          │
└──┴────┴──────────┴────┘  └────┴──────────┘      └──────────┘
                            成员栏进 Tab 切换       频道栏进 Ctrl+K
```

- **服务器条**（最左那一竖）是五个分区：群聊、论坛、社区、我的、管理。
  每个分区下有哪几屏，来自 `surface.ts` 那张表 ——
  网页侧栏加了一项，守卫会要求这里也加。
- **主区**现在按 JSON 树铺（服务端多返回一个字段，这里就多显示一行 ——
  这是「网页加了东西终端跟得上」的底层保证）。帖子正文该走 Glamour，
  还没做，见第十二节。
- **命令面板 `Ctrl+K`** 是窄终端下唯一完整的导航，也是宽终端下最快的那条。

### 跨屏跳转是这套东西好不好用的分水岭

「聊着聊着想看这个人的主页，从主页进他的帖子，从帖子进 GitHub」——
这条路径要一路按回车走下去，且每一步都能按 `Esc` 原路退回。

实现上是一个**导航栈**（`internal/ui/router`），每一屏都能声明
自己身上有哪些「可跳转的东西」：

```
群聊消息 ─@某人→ 成员主页 ─他的帖子→ 帖子正文 ─作者绑定→ GitHub 项目
   ↑                                                          │
   └────────────────── Esc 一路退回 ─────────────────────────┘
```

GitHub 那一步落在站内的项目页（`/projects/[owner]/[repo]` 的终端版），
不是把浏览器打开 —— 打开浏览器就把人赶出终端了。
真要去 github.com，`o` 键调系统默认打开方式，且**SSH 会话里
这个键不出现**（网关上没有浏览器，按下去只会静默失败）。

---

## 八、终端适配

「自适应各种不同终端」拆成四件互相独立的事，各自单独降级：

| 探测什么 | 怎么探 | 探不到就 |
|---|---|---|
| 颜色深度 | `COLORTERM` / `TERM` / OSC 查询 | 24 位 → 256 → 16 → 无色，四套调色板都要好看 |
| 背景明暗 | OSC 11 查询，超时 300 ms | 当作暗色（终端的多数派） |
| Unicode 宽度 | 画一个已知宽度的字符测光标位移 | 全部退到 ASCII 边框，不用 emoji |
| 尺寸 | `SIGWINCH` / SSH 的 `window-change` | 按 80×24 排版 |

**无色那一套不是应付了事的兜底。** 管道、`script` 录制、
CI 日志、以及一部分 Windows 终端会落到这里，而在无色下唯一
还能表达层次的只有留白和字符密度 —— 布局本身要在没有颜色时
依然读得懂，不能靠「反正没人用」蒙过去。

---

## 九、安装与自更新

### `curl -Ls agenticlab.sh | bash`

裸域名上要同时供两种东西：浏览器要 HTML，`curl` 要脚本。
靠 `User-Agent` 与 `Accept` 内容协商：`Accept` 里没有 `text/html`
且 UA 像 curl/wget，就返回 `install.sh`。

判据放在**应用里**（`src/lib/tui/install-rules.ts`）而不是 nginx 配置里，
理由和这个仓库里其它判定一样：nginx 配置没有测试，
而这一条判错的后果是**有人 `curl` 一下拿到一整页 HTML 灌进 bash**。

脚本本身做六件事，每一件都要能在失败时说清楚是哪一件：
探测 os/arch → 下载 → **校验 sha256** → 装进 `~/.local/bin`
→ 注册 `ash` 和 `agenticlab.sh` 两个名字 → 装 shell 补全。

两个名字都要：`ash` 是每天敲的那个（三个字母），
`agenticlab.sh` 是**别人在群里贴出来的那个** —— 一个人看到
`curl -Ls agenticlab.sh | bash` 之后，最可能敲的下一个命令就是
`agenticlab.sh`。它不在的话，他会以为装失败了。

> `ash` 这个名字在少数系统上和 Almquist shell 撞（Alpine 的 `/bin/ash`）。
> 装的时候检测到 `$PATH` 里已经有 `ash`，就只装 `agenticlab.sh`
> 并明确告诉人为什么 —— 不静默覆盖别人的 shell。

### 自更新

启动时**非阻塞**问一次 `/api/v1/release`，有新版就在后台下载、
校验、原子替换，下次启动生效。不在启动路径上等 —— 一次网络卡顿
换来的是「这个工具启动要五秒」，而那是人放弃它的最常见理由。

**SSH 网关模式下自更新整个关掉**：那台机器上的版本由部署流程管，
让一个会话去改别人正在跑的二进制是另一类事故。

---

## 十、这套东西**不**做什么

- **不做本地缓存数据库。** 所有数据现取现用。缓存意味着
  「终端里看到的和网页上不一样」，而那正是这套东西最不该有的毛病。
  （只缓存令牌和上次的窗口尺寸。）
- **不做离线模式。** 同上。
- **不在终端里改站点设置以外的管理动作走捷径。**
  后台写操作在终端里仍然是 `requireWritableAdmin` + `audit` 那一套，
  一步不少 —— 终端不是绕开规则的近路，`catalog.ts` 里那句
  「令牌不是绕开规则的近路」在这里同样成立。
- **不做第三方 OAuth。** 那是 `docs/OAUTH-PROVIDER.md` 的事，
  和这里各走各的。

---

## 十一、代码在哪，怎么跑起来

```
tui/
  cmd/ash/             本地二进制
  cmd/ash-sshd/        SSH 网关（和上面共用 internal/ui 那一个 model）
  internal/surface/    生成的面清单（`npm run tui:gen` 从 surface.ts 倒过来）
  internal/theme/      终端能力探测与四套调色板
  internal/api/        开放 API 客户端
  internal/auth/       设备码登录与令牌保管
  internal/update/     版本比较、下载、校验、原子替换
  internal/ui/         外壳（三栏、导航栈、命令面板）
  internal/ui/screens/ 每一屏，以及**对齐守卫读的那张注册表**
  internal/ui/kit/     JSON 树、按显示宽度截断
```

服务端那一侧：

```
src/lib/tui/surface.ts        面的唯一真源
src/lib/tui/device*.ts        设备码登录
src/lib/tui/install-*.ts      curl|bash 的判据与脚本
src/lib/tui/release-rules.ts  发布清单的校验
src/lib/admin/api-registry.ts 三十个后台分区共用的那套
src/app/api/v1/**             开放 API
src/app/(app)/link/page.tsx   设备码的确认页
```

### 本地跑一遍

```bash
npm run tui:gen                  # 表改了之后要重新生成
cd tui && go build ./cmd/ash
./ash --site http://localhost:3000
```

`--site` 或 `ASH_SITE` 指向哪个站都行。换站会当成没登录 ——
一把 A 站的令牌拿去调 B 站只会拿到一句指向错方向的「令牌无效」。

### 发一个版本

1. 交叉编译四个平台，算 sha256
2. 传到对象存储
3. 在后台把清单写进 `tui.release` 这个设置项

```json
{
  "version": "1.2.3",
  "releasedAt": 1786646856204,
  "notes": "群聊里能翻历史了",
  "assets": [
    { "platform": "linux-amd64", "url": "https://…/ash_1.2.3_linux_amd64",
      "sha256": "…", "size": 11641595 }
  ]
}
```

清单写错了就当**没有发布**（`validateManifest` 在读的时候校验）——
一个下载不下来的更新，比一个「暂时没有更新」难查得多。

### 部署 SSH 网关

`ops/ash-sshd.service`。三条不能省的：

- `User=ash`，不是 root。22 端口靠 `AmbientCapabilities=CAP_NET_BIND_SERVICE`
- `UMask=0077` —— 那个目录里是**别人的令牌**，
  漏了这一条同机上任何用户都读得到
- `ASH_SITE` 是唯一要配的东西。这台机器上出现第二个配置项时，
  先问一句它是不是把源站地址带过来了

---

## 十二、还没做的

写在这儿而不是留白，因为「没做」和「没想过」在读者眼里一样。

这份清单有一半是**机器盯着**的：`tests/tui-parity.test.ts` 里那条
「声明要用的端点，Go 那侧真的在调」会逐条核对，
接上了却忘了从名单上划掉，它同样会红。

- **导出数据刻意留在网页上**。不是没做完 —— 那一步的意义是让人
  读一遍「这份文件里有别人说的话」，而终端里做成一个按钮的话，
  那句话就成了没人看的文案。
- **发布流水线是半自动的**。`bash scripts/tui-release.sh <版本> <地址前缀>`
  会交叉编译四个平台、算好 sha256、打出一份能直接贴的清单 ——
  但上传和「贴进后台」还是手动的。后者是**故意**的：
  那一步是发布，该有一个人在按，而且该留一条设置历史。
